package lecture02.v1;

//@ La classe TNT e` la piu` semplice possibile, al momento serve solo per definire un nome (`TNT`), non contiene ne` comportamento (metodi) ne stato (campi) rappresenta solo il concetto di `TNT`
//@ La keyword `public` e` un modificatore di accesso che dice che la classe `TNT` e` visibile a tutto il progetto. Parleremo dei modificatori di accesso in dettaglio piu` avanti.
public class TNT {

}
