\documentclass[12pt, a4paper]{article}

\def\islight{true} 

\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath, amssymb}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{eurosym}
\usepackage{graphicx}
\usepackage{hyphenat}
\usepackage{tocloft}
\usepackage{xcolor}
\usepackage{hyperref}

\usepackage{datetime} 
\usepackage{array}
\usepackage{float}
\newcounter{domandateoria}

\newcommand{\myfootertext}{}
\newcommand{\myicon}{}

\ifx\islight\undefined
    \definecolor{lightergray}{gray}{0.85}
    \definecolor{tocsec}{gray}{0.85}
    \definecolor{tocsubsec}{gray}{0.75}
    \renewcommand{\myicon}{logo_unitn_scuro.png}
    \renewcommand{\myfootertext}{Appunti redatti per gli studenti del DISI. \\ Eventuali errori possono essere segnalati su \href{https://github.com/mirconegri/University/tree/main/2nd_semester/funzionale/schemi/}{GitHub}.}
    \pagecolor{black}
    \color{white}
\else
    \definecolor{lightergray}{gray}{0.35}
    \definecolor{tocsec}{gray}{0.35}
    \definecolor{tocsubsec}{gray}{0.45}
    \renewcommand{\myicon}{logo_unitn_chiaro.png}
    \renewcommand{\myfootertext}{Versione Light Mode. Disponibile anche la \href{https://github.com/mirconegri/University/tree/main/2nd_semester/funzionale/schemi/}{versione Dark Mode}.}    \pagecolor{white}
    \color{black}
\fi

\hypersetup{
    colorlinks=true,
    linkcolor=lightergray,
    urlcolor=cyan,
    pdftitle={Appunti di Funzionale},
    pdfauthor={Mirco Negri}
}

\renewcommand{\cftsecfont}{\color{tocsec}\bfseries}
\renewcommand{\cftsecpagefont}{\color{tocsec}\bfseries}
\renewcommand{\cftsubsecfont}{\color{tocsubsec}}
\renewcommand{\cftsubsecpagefont}{\color{tocsubsec}}
\renewcommand{\cftsubsecleader}{\color{tocsubsec}\cftdotfill{\cftdotsep}}

\geometry{margin=2.5cm}

\begin{document}

\begin{titlepage}
    \centering
    
    
    
    \begingroup
    \hypersetup{hidelinks}
    \href{https://www.unitn.it/}{%
        \begin{tabular}{@{}c@{}}
            {\includegraphics[width=0.25\textwidth]{\myicon}} \\[1cm]
            {\scshape\LARGE Università degli Studi di Trento} \\[0.4cm]
            {\large Dipartimento di Ingegneria e Scienza dell'Informazione}
        \end{tabular}%
    }\par
    \endgroup
    
    \vspace{2cm}

    \begingroup
    \hypersetup{hidelinks}
    \href{https://github.com/mirconegri/University}{%
        \begin{tabular}{@{}c@{}}
            {\color{cyan}\rule{\textwidth}{1pt}} \\[0.5cm]
            {\Huge\bfseries Appunti di Programmazione}\\ [0.3cm] {\Huge\bfseries Funzionale}  \\[0.4cm]
            {\color{cyan}\rule{\textwidth}{1pt}}
        \end{tabular}%
    }\par
    \endgroup

    \vspace{2.5cm}
    
    {\Large A cura di: \par}
    \vspace{0.3cm}
    {\Large \textbf{\href{https://github.com/mirconegri}{Mirco Negri}} \par}
    
    \vfill
    
    {\small \textit{\myfootertext} \par}
    \vspace{0.8cm}
    
    {\large \monthname\ \the\year \par}
\end{titlepage}

\newpage
\begingroup
\hypersetup{hidelinks}
\tableofcontents
\endgroup
\newpage

\section{Introduzione alla Programmazione Funzionale}

\subsection{Spiegazione teorica}
La prima lezione del corso è introduttiva e mira a fornire una panoramica sugli argomenti trattati, le modalità di svolgimento e un inquadramento storico dei linguaggi di programmazione.

Il paradigma funzionale si distingue da quello imperativo e orientato agli oggetti per il suo approccio matematico alla computazione, basato sulla valutazione di espressioni e sull'uso di funzioni pure. 

I punti chiave affrontati in questa introduzione includono:
\begin{itemize}
    \item \textbf{Obiettivi del corso:} Comprendere i principi base della programmazione funzionale, studiare il sistema dei tipi, le funzioni di ordine superiore, l'astrazione dei dati e il polimorfismo. Verranno inoltri introdotti il Lambda Calcolo, la programmazione logica (Prolog) e accenni a Scala.
    \item \textbf{Storia dei linguaggi funzionali e logici:}
    \begin{itemize}
        \item \textbf{Anni '50 - '70:} Nascita del LISP (lista processing), capostipite dei linguaggi funzionali (untyped), seguito dalle sue evoluzioni come Scheme e Racket.
        \item \textbf{Anni '70 - '80:} Sviluppo dei theorem prover (LCF) da cui deriva la famiglia ML (Meta Language), caratterizzata da \textit{call-by-value}, tipizzazione forte e polimorfismo. Nascono Caml e Miranda.
        \item \textbf{Anni '90 - oggi:} Consolidamento di Standard ML, OCaml e nascita di Haskell (linguaggio funzionale puro \textit{lazy}). Più recentemente, la programmazione funzionale ha influenzato linguaggi moderni come Scala e F\#.
        \item Sviluppo di strumenti basati sui tipi dipendenti (come Coq) nati negli anni '80.
    \end{itemize}
\end{itemize}

Trattandosi di una lezione introduttiva sul contesto e sulla storia del paradigma, non sono stati affrontati esercizi pratici o costrutti sintattici del linguaggio ML.

\section{Espressioni e tipi}

\subsection{Introduzione a ML}
ML (Meta Language) è un linguaggio di programmazione general-purpose, nato originariamente come metalinguaggio per sistemi di verifica formale (come il theorem prover LCF negli anni '70). Esistono diversi dialetti derivati, tra cui Standard ML, OCaml, F\#, ed elementi che hanno influenzato linguaggi come Scala e Haskell.  

\textbf{Perché studiare ML?}
Studiare la programmazione funzionale impone un cambio di paradigma: i linguaggi mainstream si concentrano sulla gestione degli "stati", mentre la programmazione funzionale si concentra sui \textbf{valori}. ML è un linguaggio pratico e compatto le cui idee possono rendere programmatori migliori in qualsiasi linguaggio.  

\textbf{Caratteristiche principali della Programmazione Funzionale in ML:}
\begin{enumerate}

\newpage

\item \textbf{Linguaggio funzionale:} Il modello di calcolo di base si fonda sulla definizione e sull'applicazione di funzioni. Le funzioni sono trattate come valori (cittadini di prima classe) e possono essere passate come parametri ad altre funzioni, supportando nativamente le \textit{funzioni di ordine superiore} (Higher-order functions).
\item \textbf{Ricorsione:} Fortemente incoraggiata rispetto ai cicli \texttt{while} (costrutti iterativi sono disponibili, ma secondari).
\item \textbf{Programmazione basata su regole:} Costrutti come \texttt{if-then-else} sono spesso implementati o sostituiti dal \textit{pattern matching}.
\item \textbf{Nessun side effect (effetti collaterali):} La computazione procede per valutazione di espressioni, non tramite l'assegnazione di valori a variabili che mutano lo stato. Se in C \texttt{a = b + c} modifica lo stato di \texttt{a}, in ML \texttt{b + c} crea un nuovo elemento. Gli effetti collaterali (es. stampare a video) sono permessi ma non sono il mezzo principale di calcolo.
\item \textbf{Strong typing (Tipizzazione forte):} Tutti i valori e le variabili hanno tipi determinabili a "tempo di compilazione" (\textit{compile-time}). Rileva errori semantici preziosi; inoltre, non è solitamente necessaria la dichiarazione esplicita del tipo grazie alla \textit{Type Inference}.
\item \textbf{Polimorfismo:} Una funzione può accettare argomenti di tipi differenti (es. una funzione di ordinamento generica).
\item \textbf{Abstract Data Types (Strutture):} Sistema di tipi elegante con la capacità di costruire nuovi tipi complessi.
\end{enumerate}  

\textbf{Eseguire ML:}
L'interprete interattivo PolyML si avvia da linea di comando digitando \texttt{poly} (o \texttt{rlwrap poly} in Linux per avere la cronologia dei comandi). All'interno dell'ambiente interattivo (indicato dal prompt \texttt{>}), si possono valutare le espressioni chiudendole con il punto e virgola \texttt{;}. Per caricare un file esterno si usa \texttt{use "foo";}. Per uscire \texttt{CTRL+D}, per interrompere \texttt{CTRL+C}.  

\subsection{Espressioni e Comandi}
Vi è una netta separazione tra espressioni e comandi:
\begin{itemize}
\item \textbf{Espressioni:} Entità sintattiche la cui valutazione restituisce un valore o non termina (espressione indefinita). Sono composte da costanti, variabili o operatori applicati a ulteriori espressioni. Notazioni utilizzabili: infissa (\texttt{a + b}), prefissa o polacca (\texttt{+ a b}), suffissa (\texttt{a b +}).
\item \textbf{Comandi:} Entità sintattiche la cui valutazione non restituisce necessariamente un valore, ma provoca un effetto collaterale (modifica dello stato). Tipici dei linguaggi imperativi, sono del tutto assenti nei paradigmi puramente funzionali o logici.
\end{itemize}  

\newpage

\textbf{Strategie di valutazione delle espressioni:}
\begin{itemize}
\item \textbf{Eager evaluation (Valutazione ansiosa):} Si valutano prima tutti gli operandi, poi si applica l'operatore ai risultati.
\item \textbf{Lazy evaluation (Valutazione pigra):} Gli operandi vengono valutati solo se e quando il loro valore è effettivamente necessario per il calcolo. Questo approccio previene errori in espressioni come \texttt{a == 0 ? b : b/a} (se \texttt{a} è 0, valutare ansiosamente \texttt{b/a} causerebbe una divisione per zero). Viene usata per operatori booleani (short-circuit evaluation), dove il risultato finale può essere noto prima di valutare tutti gli operandi.
\end{itemize}
In ML si utilizza principalmente la \textbf{eager evaluation}, tranne che per alcuni specifici costrutti come \texttt{andalso}, \texttt{orelse} e \texttt{if-then-else} che adottano una valutazione \textit{lazy}.  

\subsection{Tipi di dato e astrazione}
Un \textbf{tipo di dato} è una collezione di valori (omogenei) dotata di un insieme di operatori applicabili.
I tipi servono a tre scopi principali:
\begin{enumerate}
\item \textbf{Livello concettuale (Design):} Organizzare l'informazione e documentare il codice (es. distinguere un \texttt{prezzo} da un \texttt{numero di stanza}).
\item \textbf{Livello di programma (Correctness):} Identificare ed evitare errori. I compilatori eseguono il type-checking: assegnare una stringa a un intero (\texttt{3 + "pippo"}) solleverà un errore semantico.
\item \textbf{Livello implementativo:} Permettere al compilatore ottimizzazioni. Conoscere il tipo permette di sapere quanta memoria allocare (un booleano occupa meno di un double) e calcolare gli offset di memoria per l'accesso ai campi di una struct.
\end{enumerate}  

\subsection{Espressioni, tipi e operatori in ML}
In ML, le espressioni possono essere valori o funzioni applicate a valori.
Ogni espressione in PolyML termina con \texttt{;}.
Esempio:
\begin{verbatim}  

    1 + 2 * 3;
    val it = 7: int
    \end{verbatim}

\newpage

    In questo output: \texttt{val} indica il valore dell'espressione, \texttt{it} è il nome automatico associato al risultato, e \texttt{int} è il tipo dedotto automaticamente.  

\textbf{Tipi Base in ML:}
\begin{itemize}
\item \texttt{int}: Interi positivi e negativi. L'operatore unario per il negativo è la tilde \texttt{\~{}} (es. \texttt{\~{}3}). Supporta esadecimali (es. \texttt{0x124}).
\item \texttt{real}: Numeri reali. Richiedono il punto decimale o la notazione esponenziale \texttt{E}/\texttt{e} (es. \texttt{\~{}123.0}, \texttt{3.14e12}).
\item \texttt{bool}: \texttt{true} o \texttt{false} (rigorosamente minuscoli, ML è case-sensitive).
\item \texttt{char}: Singolo carattere indicato dal prefisso \texttt{\#} (es. \texttt{\#"a"}).
\item \texttt{string}: Doppi apici (es. \texttt{"foo"}). Caratteri speciali supportati: \texttt{\textbackslash n} (newline), \texttt{\textbackslash t} (tab), \texttt{\textbackslash\textbackslash}, \texttt{\textbackslash"}.
\item \texttt{unit}: Possiede un unico valore \texttt{()}, utilizzato per indicare le espressioni che non restituiscono un valore utile (analogo a \texttt{void}).
\end{itemize}  

\textbf{Operatori:}
\begin{itemize}
\item \textbf{Aritmetici:} \texttt{+}, \texttt{-}, \texttt{*} per int e real. La divisione per reali è \texttt{/}, mentre per gli interi si usano \texttt{div} (divisione troncata all'intero inferiore) e \texttt{mod} (resto).
\item \textbf{Forma Infissa/Prefissa:} In ML, un operatore come \texttt{+} è di fatto una funzione prefissa che riceve in input una tupla numerica \texttt{+(1,2)}, ma consente la notazione infissa per comodità.
\item \textbf{Stringhe:} La concatenazione avviene tramite l'operatore \texttt{\^} (es. \texttt{"house" \^{} "cat"} → \texttt{"housecat"}).
\item \textbf{Confronto:} \texttt{=}, \texttt{<}, \texttt{>}, \texttt{<=}, \texttt{>=}, \texttt{<>} (diverso). Valgono per interi, caratteri e stringhe (ordine lessicografico). \textbf{Attenzione:} in ML non è permesso testare l'uguaglianza (o la disuguaglianza \texttt{<>}) tra due valori \texttt{real}, per prevenire problemi di approssimazione \textit{floating-point}.
\item \textbf{Logici:} \texttt{not}, \texttt{andalso} (AND lazy), \texttt{orelse} (OR lazy). \texttt{andalso} ha precedenza su \texttt{orelse}.
\end{itemize}  

\textbf{Costrutto If-then-else:}
\begin{verbatim}
if  then  else ;
\end{verbatim}
Poiché in ML tutto è un'espressione, l'utilizzo di \texttt{if} richiede \textbf{obbligatoriamente} la presenza del ramo \texttt{else}. Inoltre, per preservare la \textit{type safety}, \texttt{} e \texttt{} devono avere lo stesso tipo ben definito. Un'espressione come \texttt{if 5<6 then 5 else 6.0;} solleva un \textit{Type mismatch error} statico in compilazione.  

\subsection{Sistemi di tipi: Sicurezza e Controlli}
I linguaggi di programmazione adottano strategie diverse per il \textit{Type Checking}:
\begin{itemize}
\item \textbf{Static Type Checking:} Effettuato a tempo di compilazione (es. C, Java, Haskell, ML) prima dell'esecuzione. Vantaggi: maggiore efficienza a runtime e blocco preventivo di errori. Svantaggi: design del linguaggio più complesso e regole spesso molto rigide.
\item \textbf{Dynamic Type Checking:} Eseguito durante l'esecuzione del codice (es. Python, JavaScript, Lisp). Vantaggi: massima flessibilità, non servono dichiarazioni di tipo. Svantaggi: gli errori di tipo vengono identificati solo a runtime, e causano perdita di efficienza dovuta ai continui controlli dinamici.
\end{itemize}  

Inoltre, i linguaggi si dividono in:
\begin{itemize}
\item \textbf{Strongly typed (Tipizzazione forte):} Molto severi sui tipi. Il tipo di un valore non cambia in modo inaspettato. Le conversioni di tipo implicite (coercion) sono vietate (es. ML).
\item \textbf{Weakly typed (Tipizzazione debole):} Molto flessibili, permettono la conversione implicita tra tipi differenti a runtime (es. JavaScript: \texttt{4 + "2" = "42"}, PHP, C/C++).
\end{itemize}
ML, a garanzia della sua \textbf{Type Safety}, è un linguaggio \textbf{fortemente e staticamente tipizzato} (statically and strongly typed). Tenta di risolvere l'inconveniente della verbosità grazie al potente meccanismo della \textit{Type Inference} (in cui il compilatore deduce il tipo in automatico), rifiutandosi categoricamente di effettuare coercion nascoste (come \texttt{1 + 2.0} che genererà un errore).

\section{Sistemi di tipi, variabili e funzioni}

\subsection{Spiegazione teorica}
Il sistema dei tipi di un linguaggio è definito dai suoi tipi predefiniti, dai meccanismi per crearne di nuovi, dalle regole per determinare se il controllo è statico o dinamico e dalle regole di equivalenza, compatibilità e inferenza.

\textbf{Static e Dynamic Type Checking:}
\begin{itemize}
    \item \textbf{Controllo Statico:} Avviene a tempo di compilazione (es. ML, Java, C). Previene l'esecuzione di codice con errori di tipo ed è molto efficiente a runtime, sebbene renda la progettazione e la scrittura del codice più rigida.
    \item \textbf{Controllo Dinamico:} Avviene durante l'esecuzione (es. Python, JavaScript). Garantisce grande flessibilità, ma gli errori vengono scoperti solo a runtime, impattando sulle prestazioni.
\end{itemize}

I linguaggi si dividono inoltre in \textbf{fortemente tipizzati} (strongly typed), dove il tipo di un valore non cambia in modo inaspettato (niente conversioni implicite, come in ML), e \textbf{debolmente tipizzati} (weakly typed), dove le conversioni implicite (coercion) sono permesse (es. \texttt{4 + "2" = "42"} in JavaScript). ML assicura un alto livello di \textit{Type Safety}, ovvero previene la presenza di errori di tipo inosservati a runtime.

\textbf{Equivalenza e Compatibilità:}
Due tipi possono essere considerati equivalenti per \textbf{nome} (se hanno letteralmente lo stesso nome) o per \textbf{struttura} (se, analizzando le definizioni, la struttura di base è identica). ML utilizza principalmente l'equivalenza strutturale (fatta eccezione per i tipi definiti con \texttt{datatype}).

In ML \textbf{non esistono conversioni automatiche} (coercion). Se si valuta \texttt{1 + 2.0}, il compilatore non converte automaticamente l'intero in reale, ma restituisce un errore di tipo statico. È necessario eseguire una conversione esplicita tramite apposite funzioni di sistema.

\textbf{Nomi, Variabili e Ambiente (Environment):}
L'\textbf{Environment} è l'insieme delle associazioni tra identificatori (nomi) e valori. In ML, a differenza dei linguaggi imperativi come il C, \textbf{le variabili non sono modificabili}. Una variabile è semplicemente un identificatore legato a un valore matematico.
Tramite il costrutto \texttt{val}, non si assegna un nuovo valore a una locazione di memoria, ma si crea un nuovo legame (binding) nell'ambiente. Se si riutilizza lo stesso nome, la nuova dichiarazione oscura la precedente (\textit{shadowing}), ma il valore originale non viene sovrascritto.

\textbf{Tipi Complessi: Tuple e Liste:}
\begin{itemize}
    \item \textbf{Tuple:} Aggregati di dimensione fissa che possono contenere tipi eterogenei. Esempio: \texttt{(4, 5.0, "sei")} è di tipo \texttt{int * real * string}. Si accede all'n-esimo elemento tramite l'operatore \texttt{\#n}.
    \item \textbf{Liste:} Aggregati di dimensione variabile, ma rigorosamente omogenei (tutti gli elementi devono avere lo stesso tipo). Esempio: \texttt{[1, 2, 3]} è di tipo \texttt{int list}. 
\end{itemize}

\textbf{Funzioni come valori:}
In ML le funzioni sono trattate come "cittadini di prima classe", ovvero come normali valori. Possono essere anonime tramite il costrutto \texttt{fn} (che riflette il lambda calcolo), o associate a un nome con il costrutto \texttt{fun}. Una particolarità di ML è che \textbf{tutte le funzioni accettano esattamente un solo parametro}. Per simulare più parametri, si passa semplicemente una singola tupla contenente i vari argomenti.

\newpage

\subsection{Implementazione del codice}

\subsubsection{Conversioni di tipo esplicite in ML}
\begin{verbatim}
> real(4);           (* Da intero a reale *)
val it = 4.0: real

> floor(3.5);        (* Arrotondamento per difetto *)
val it = 3: int

> ceil(3.5);         (* Arrotondamento per eccesso *)
val it = 4: int

> round(3.5);        (* Arrotondamento al pari più vicino *)
val it = 4: int

> trunc(3.5);        (* Troncamento *)
val it = 3: int

> ord(#"a");         (* Da char ad ASCII *)
val it = 97: int

> chr(97);           (* Da ASCII a char *)
val it = #"a": char

> str(#"a");         (* Da char a string *)
val it = "a": string
\end{verbatim}

\subsubsection{Ambiente e Shadowing}
\begin{verbatim}
> val pi = 3.14159;
val pi = 3.14159: real

> val pi = 3; 
val pi = 3: int
(* Crea un nuovo 'pi' di tipo intero che nasconde il precedente, 
   ma non lo sovrascrive fisicamente *)
\end{verbatim}

\subsubsection{Manipolazione di Liste e Stringhe}
\begin{verbatim}
> val L = [2, 3, 4];
> hd(L);             (* Head: estrae il primo elemento *)
val it = 2 : int

> tl(L);             (* Tail: estrae il resto della lista *)
val it = [3, 4] : int list

> [1, 2] @ [3, 4];   (* Concatenazione di liste, stesso tipo! *)
val it = [1, 2, 3, 4] : int list

> 1 :: [2, 3];       (* Cons: aggiunge l'elemento in testa alla lista *)
val it = [1, 2, 3] : int list

> explode("abcd");   (* Da stringa a lista di caratteri *)
val it = [#"a", #"b", #"c", #"d"] : char list

> implode([#"a", #"b"]); (* Da lista di caratteri a stringa *)
val it = "ab" : string
\end{verbatim}

\subsubsection{Dichiarazione e uso di funzioni}
\begin{verbatim}
(* Sintassi della funzione anonima associata a una variabile *)
> val increment = fn n => n + 1;
val increment = fn: int -> int

(* Zucchero sintattico (più comune) *)
> fun increment n = n + 1;
val increment = fn: int -> int

(* Funzione a finti "parametri multipli" (prende in realtà una tupla) *)
> fun max3(a:real, b, c) = 
    if a > b then 
        if a > c then a else c
    else 
        if b > c then b else c;
val max3 = fn: real * real * real -> real
\end{verbatim}


\section{Laboratorio ML 1}

Nel primo laboratorio si mette in pratica la sintassi base affrontata nelle lezioni teoriche, concentrandosi sulla correzione dei "Type Mismatch" e sulla familiarizzazione con i tipi complessi (\texttt{list}, \texttt{tuple} e relative combinazioni).

\subsection{Correzione di Type Errors (Esercizi 1.3 - 1.5)}
ML è un linguaggio in cui la correttezza del tipo (\textit{Type Safety}) viene imposta in modo rigoroso. Analizziamo come correggere espressioni sintatticamente o logicamente invalide.

\newpage

\begin{verbatim}
(* ERRORE: ceil accetta solo real, non int *)
ceil(4); 
(* CORREZIONE *)
ceil(4.0);

(* ERRORE: i rami dell'if devono restituire esattamente lo stesso tipo *)
if true then 5 + 6 else 7.0; 
(* CORREZIONE (converto l'intero in reale) *)
if true then real(5 + 6) else 7.0;

(* ERRORE: la condizione dell'if deve essere strettamente di tipo bool *)
if 0 then 1 else 2; 
(* CORREZIONE *)
if false then 1 else 2;

(* ERRORE: ord si applica ai caratteri (prefisso #), non alle stringhe *)
ord("a"); 
(* CORREZIONE *)
ord(#"a");

(* ERRORE: explode prende in input una singola stringa, non una lista *)
explode ["bar"]; 
(* CORREZIONE *)
explode("bar");

(* ERRORE: implode accetta una lista di char, non una tupla *)
implode (#"a", #"b"); 
(* CORREZIONE (sostituisco le parentesi tonde con le quadre) *)
implode ([#"a", #"b"]);

(* ERRORE: :: (cons) unisce UN elemento a una LISTA. 
   Qui a sinistra c'è una lista e a destra un'altra lista *)
["r"]::["a","t"]; 
(* CORREZIONE: (rimuovo le parentesi a sinistra) *)
"r"::["a","t"];
\end{verbatim}

\newpage

\subsection{Istanze di Tipi Complessi (Esercizio 1.6 - 1.7)}
Si richiede di creare valori legali per le seguenti firme di tipo, dimostrando di saper annidare correttamente tuple (parentesi tonde) e liste (parentesi quadre).

\begin{verbatim}
(* TIPO: int list list list *)
(* Una lista che contiene liste di liste di interi *)
val es1 = [[[1, 2]], [[3]]];

(* TIPO: (int * char) list *)
(* Una lista di tuple formate da intero e carattere *)
val es2 = [(1, #"a"), (2, #"b")];

(* TIPO: string list * (int * (real * string)) *)
(* Una macro-tupla contenente una lista di stringhe e un'altra tupla *)
val es3 = (["ciao", "mondo"], (42, (3.14, "pi")));

(* TIPO: ((int * int) * (bool list) * real) * (real * string) *)
val es4 = (((1, 2), [true, false], 5.0), (3.14, "test"));
\end{verbatim}

\section{Regole di scope}

\subsection{Funzioni e inferenza di tipi}
In ML, le funzioni sono considerate valori (cittadini di prima classe) a tutti gli effetti. Sono rappresentate tramite espressioni parametrizzate.
\begin{itemize}
    \item \textbf{Sintassi base (\texttt{fn}):} La definizione di una funzione anonima si basa sulla sintassi \texttt{fn <parametro> => <espressione>}, che corrisponde concettualmente all'astrazione $\lambda$ nel lambda calcolo. Esempio: \texttt{fn n => n + 1}.
    \item \textbf{Dichiarazioni con nome:} È possibile associare una funzione a un identificatore usando la parola chiave \texttt{val} (es. \texttt{val increment = fn n => n + 1}) , ma ML offre anche un costrutto semplificato (zucchero sintattico) estremamente comune: \texttt{fun increment n = n + 1}.
    \item \textbf{Singolo parametro e Tuple:} In ML, \textbf{tutte le funzioni hanno esattamente un solo parametro}. Tuttavia, se abbiamo bisogno di passare molteplici argomenti logici, il parametro può essere strutturato come una tupla. Ad esempio, \texttt{fun max3(a:real, b, c) = ...} accetta in input un'unica tupla contenente tre elementi di tipo \texttt{real}.
\end{itemize}

\newpage

\textbf{Type Inference (Inferenza dei tipi):}
Il compilatore di ML deduce automaticamente i tipi delle variabili basandosi sul contesto delle espressioni:
\begin{itemize}
    \item In un'espressione aritmetica come \texttt{(a + b) * 2.0}, sapendo che \texttt{2.0} è reale e l'operatore \texttt{*} impone operandi compatibili, il risultato di \texttt{a+b} e, di conseguenza, le variabili \texttt{a} e \texttt{b} vengono forzate e dedotte come tipo \texttt{real}.
    \item In presenza di costrutti condizionali, le espressioni associate ai rami \texttt{then} ed \texttt{else} e all'intera espressione condizionale stessa devono rigorosamente possedere lo stesso tipo.
    \item Qualora non vi siano operatori che forzino un tipo specifico e non vi sia alcun modo per determinarne il tipo, il compilatore assegna un \textbf{tipo generico} (indicato per esempio con \texttt{'a} e \texttt{'b}).
\end{itemize}

\subsection{Ambiente (Environment) e Blocchi}
L'\textbf{Environment} (ambiente) è la collezione delle associazioni tra identificatori (nomi) e oggetti denotabili che si viene a creare a runtime in uno specifico momento del programma. Una \textit{dichiarazione} è il meccanismo che crea tale associazione all'interno dell'ambiente.
L'ambiente può essere logicamente suddiviso in:
\begin{itemize}
    \item \textbf{Ambiente locale:} Variabili dichiarate internamente e parametri formali creati all'ingresso di un blocco.
    \item \textbf{Ambiente non-locale:} Associazioni ereditate da blocchi gerarchicamente esterni a quello corrente.
    \item \textbf{Ambiente globale:} Costituisce quella porzione dell'ambiente non-locale che contiene associazioni e dichiarazioni visibili in tutti i blocchi.
\end{itemize}
I \textbf{blocchi} (in ML definiti dal costrutto \texttt{let ... in ... end}) gestiscono l'organizzazione dell'ambiente. Una dichiarazione locale in un blocco è visibile nel blocco stesso e in tutti i blocchi annidati all'interno, a patto che in questi ultimi non vi sia un'ulteriore dichiarazione con il medesimo nome che ne occulta (shadowing) la precedente.

\subsection{Scoping Statico vs Dinamico}
Come viene risolto il riferimento a una variabile "non locale"? La letteratura dei linguaggi di programmazione definisce due approcci fondamentali:
\begin{itemize}
    \item \textbf{Scoping Statico (o Lexical):} Il riferimento viene risolto nel blocco che \textbf{include testualmente (sintatticamente)} la chiamata. Si basa esclusivamente sulla struttura del codice scritto ed è indipendente dai nomi locali del blocco chiamante. I vantaggi dello scoping statico includono una maggiore facilità di lettura da parte del programmatore, migliore efficienza a runtime, e la possibilità per il compilatore di effettuare stringenti controlli di correttezza e ottimizzazioni. È adottato in linguaggi come C, Java, Pascal e dallo stesso ML.

\newpage
    
    \item \textbf{Scoping Dinamico:} Il riferimento a un nome non-locale viene risolto all'interno del blocco che \textbf{è stato attivato per ultimo} durante il flusso di esecuzione (a runtime) e non ancora terminato. Tale approccio consente di alterare il comportamento di una funzione semplicemente modificando l'ambiente del chiamante, ma genera architetture fragili in cui i programmi diventano ostici da comprendere e meno efficienti.
\end{itemize}

\section{Laboratorio ML 2}

Nel secondo laboratorio si prendono i primi passi pratici scrivendo funzioni pure per elaborare primitive di ML e strutture complesse quali Tuple e Liste, verificandone il corretto type checking.

\subsection{Esercizi base sulle Tuple}

\textbf{Esercizio L2.2 - Minimo di una tupla:}
Scrivere una funzione \texttt{min3} che restituisca l'elemento più piccolo all'interno di una tupla composta da tre numeri interi.
\begin{verbatim}
fun min3 (a: int, b: int, c: int) =
    if a < b andalso a < c then a
    else if b < c then b
    else c;
\end{verbatim}

\textbf{Esercizio L2.4 - Inversione di Tupla:}
Scrivere una funzione \texttt{reverse} che prenda in ingresso una tupla di 3 elementi invertendone gli oggetti (la funzione deve poter accettare Tuple eterogenee, es. \texttt{reverse(1.0, 2, "a")}).
\begin{verbatim}
fun reverse (a, b, c) = (c, b, a);
\end{verbatim}

\textbf{Esercizi L2.7 e L2.8 - Identificazione ed Ordinamento:}
Scrivere una funzione \texttt{min\_max\_pair} che data una tupla di 3 interi restituisca in uscita una nuova tupla (coppia) limitata al valore più piccolo e al valore più grande estratti. Successivamente, implementare una funzione \texttt{sort} in grado di restituire una lista ordinata degli elementi.
\begin{verbatim}
fun min_max_pair (a:int, b, c) =
    let
        val minimo = if a<b andalso a<c then a else if b<c then b else c
        val massimo = if a>b andalso a>c then a else if b>c then b else c
    in
        (minimo, massimo)
    end;

fun sort (a:int, b, c) =
    let
        val (minimo, massimo) = min_max_pair(a, b, c)
        val mediano = if (a <> minimo andalso a <> massimo) then a 
                      else if (b <> minimo andalso b <> massimo) then b 
                      else c
    in
        [minimo, mediano, massimo]
    end;
\end{verbatim}

\subsection{Esercizi base sulle Liste e sulle Stringhe}

\textbf{Esercizio L2.3 - Terzo elemento:}
Estrarre il terzo elemento da una lista di dati. L'esercizio prevede l'uso in sequenza delle primitive \texttt{hd} (head) e \texttt{tl} (tail) senza la necessità, in questa fase, di far operare correttamente la funzione su liste inferiori alla lunghezza prevista.
\begin{verbatim}
fun third L = hd(tl(tl(L)));
\end{verbatim}

\textbf{Esercizio L2.6 - Cycle:}
Scrivere una funzione \texttt{cycle} che prende una lista originaria \texttt{[a1, ..., an]} ed esegue un "giro", producendo come risultato la dislocazione \texttt{[a2, ..., an, a1]} (l'esercizio assume liste non vuote).
\begin{verbatim}
fun cycle L = tl(L) @ [hd(L)];
\end{verbatim}

\textbf{Esercizio L2.10 - Rimuovere il secondo elemento:}
Scrivere la funzione \texttt{rem} in grado di accettare in ingresso una lista e ritornare la struttura inalterata privata del secondo elemento (ad esempio, la lista \texttt{[1,2,3,4]} si traduce in \texttt{[1,3,4]}).
\begin{verbatim}
fun rem L = hd(L) :: tl(tl(L));
\end{verbatim}

\section{Astrazione del controllo e ricorsione}

\subsection{Spiegazione teorica}
L'astrazione del controllo riguarda i meccanismi che i linguaggi di programmazione offrono per gestire il flusso di esecuzione (come blocchi condizionali, cicli e chiamate a sottoprogrammi). In ML, essendo un linguaggio funzionale, il controllo del flusso si basa pesantemente sulla \textbf{valutazione delle espressioni} e sulla \textbf{ricorsione}, a differenza dei linguaggi imperativi che si basano su costrutti iterativi (come il \texttt{while} o il \texttt{for}).

\textbf{Metodi di passaggio dei parametri:}
Quando una funzione viene invocata, l'ambiente locale deve essere popolato associando i parametri formali ai parametri attuali. Esistono storicamente diverse strategie:
\begin{itemize}
    \item \textbf{Call by value (Passaggio per valore):} I parametri attuali vengono valutati \textit{prima} che la funzione venga chiamata. Il risultato viene copiato nel parametro formale. È un meccanismo sicuro e senza side-effect diretti sulle variabili esterne, ed è la strategia di default adottata da ML (eager evaluation).
    \item \textbf{Call by reference (Passaggio per riferimento):} Viene passato l'indirizzo di memoria del parametro attuale. Modificare il parametro formale nella funzione modifica la variabile originale (side-effect). Non è il meccanismo predefinito nei linguaggi funzionali puri.
    \item \textbf{Call by name / Call by need:} Il parametro non viene valutato finché non è strettamente necessario all'interno del corpo della funzione (lazy evaluation). Se non viene mai usato, non viene mai valutato, risparmiando computazione e prevenendo errori (come la divisione per zero).
\end{itemize}

\newpage

\textbf{Ricorsione:}
In assenza di cicli iterativi standard, la ricorsione è l'unico modo per compiere iterazioni in un linguaggio puramente funzionale. Il programmatore definisce un caso base (condizione di terminazione) e un passo ricorsivo (in cui la funzione invoca se stessa su un input ridotto).
Un concetto fondamentale legato all'ottimizzazione è la \textbf{Tail Recursion} (Ricorsione in coda): si ha quando la chiamata ricorsiva è l'ultimissima operazione eseguita nel corpo della funzione. Questo permette al compilatore di riutilizzare lo stesso record di attivazione (Stack Frame) senza saturare la memoria (evitando lo Stack Overflow).

\textbf{Mutua Ricorsione:}
Si verifica quando due o più funzioni si chiamano a vicenda. Nei linguaggi con regole di scope rigide (dove un nome deve essere definito prima di essere usato), definire due funzioni \texttt{A} e \texttt{B} che si chiamano a vicenda solleverebbe un errore: se dichiaro prima \texttt{A}, essa non conoscerà \texttt{B}, e viceversa.
ML risolve elegantemente questo problema introducendo la parola chiave \textbf{\texttt{and}}, che permette di dichiarare e valutare le due funzioni contemporaneamente nello stesso blocco.

\subsection{Implementazione del codice}

\subsubsection{Mutua Ricorsione in ML}
Vogliamo scrivere due funzioni: \texttt{take}, che estrae gli elementi in posizione dispari da una lista, e \texttt{skip}, che salta gli elementi in posizione pari (concettualmente le due operazioni si alternano).
\begin{verbatim}
(* L'uso di 'and' permette la mutua ricorsione. 
   Il compilatore valuta le firme di entrambe contemporaneamente. *)

fun take (L) =
    if L = nil then nil
    else hd(L) :: skip(tl(L))
and skip (L) =
    if L = nil then nil
    else take(tl(L));

(* Esempio di esecuzione *)
> take ([1, 2, 3, 4, 5]);
val it = [1, 3, 5] : int list
\end{verbatim}

\section{Laboratorio ML 3}

Nel terzo laboratorio si consolida l'utilizzo della ricorsione semplice per risolvere problemi matematici e per manipolare liste, costruendo algoritmi iterativi attraverso chiamate a funzione ricorsive in stile dichiarativo.

\newpage

\subsection{Esercizi base sulla Ricorsione Matematica}

\textbf{Esercizio 3.1 - Fattoriale:}
Scrivere una funzione \texttt{fact} che calcola il fattoriale di un numero $n$ (con $n \ge 1$). L'esercizio non richiede, in questa fase primaria, di gestire in modo sicuro i numeri negativi.
\begin{verbatim}
fun fact(n) =
    if n = 1 then 1
    else n * fact(n - 1);

(* Utilizzo: *)
> fact 10;
val it = 3628800 : int

(* Nota: In ML un "fact 100" genera una Exception-Overflow 
   in quanto supera il valore massimo immagazzinabile nel tipo int.
   Per ovviare a questo limite, si può utilizzare la struttura IntInf 
   per operare con interi a precisione arbitraria. *)
\end{verbatim}

\textbf{Esercizio 3.11 - Potenza (Power):}
Scrivere una funzione \texttt{power} che prenda in ingresso una tupla formata da un numero intero $x$ e un esponente $i$ (assunto non negativo) e ne calcoli la potenza $x^i$. Notare l'utilizzo del pattern matching semplificato nella firma della funzione.
\begin{verbatim}
fun power (x, 0) = 1
  | power (x, i) = x * power (x, i - 1);

(* Utilizzo: *)
> power (4, 3);
val it = 64 : int
\end{verbatim}

\subsection{Esercizi base sulla Ricorsione e Liste}

\textbf{Esercizio 3.10 - Duplicazione elementi (duplicatep):}
Scrivere una funzione che, data una lista di elementi di tipo qualsiasi, restituisca una nuova lista in cui ogni elemento originale è ripetuto consecutivamente due volte (es. da \texttt{[1,2]} a \texttt{[1,1,2,2]}). Questo esercizio introduce un'applicazione molto comune del pattern matching su liste.
\begin{verbatim}
(* L'uso dei pattern semplifica la scrittura rispetto all'uso degli 
   if-else e di hd() / tl() *)
fun duplicatep (nil) = nil
  | duplicatep (x::xs) = x :: x :: duplicatep(xs);

(* Utilizzo: *)
> duplicatep [1, 2, 3, 4];
val it = [1, 1, 2, 2, 3, 3, 4, 4] : int list
\end{verbatim}

\newpage

\textbf{Esercizio 3.12 - Massimo di una Lista (maxList):}
Scrivere una funzione per trovare l'elemento reale più grande all'interno di una lista (assunta non vuota). 
Questa funzione utilizza l'istruzione \texttt{let ... in ... end} per salvare il risultato della chiamata ricorsiva, evitandone il ricalcolo multiplo e ottimizzando le prestazioni (concetto propedeutico per strutture più avanzate).
\begin{verbatim}
fun maxList ([x:real]) = x
  | maxList (x::xs) =
    let 
        val m = maxList(xs)
    in 
        if x > m then x else m
    end;
\end{verbatim}

\section{Pattern ed environment locale in ML}

\subsection{Spiegazione teorica}
In ML, oltre alla dichiarazione di funzioni tramite costrutti condizionali classici \\(\texttt{if-then-else}), è possibile (e fortemente consigliato) utilizzare il \textbf{Pattern Matching}. Questo meccanismo ricorda i costrutti \texttt{switch-case} dei linguaggi procedurali, ma è molto più potente perché permette di "destrutturare" i dati e assegnare variabili al volo.

\begin{itemize}
    \item \textbf{Sintassi base dei pattern:} La definizione di una funzione procede elencando una sequenza di casi. Il primo pattern che corrisponde (esegue il "match") con l'argomento fornito determina quale espressione verrà valutata.
    \begin{verbatim}
fun f (pattern_1) = espressione_1
  | f (pattern_2) = espressione_2
  | f (pattern_n) = espressione_n;
    \end{verbatim}
    \item \textbf{Esaustività:} La lista delle alternative dovrebbe sempre coprire tutti i casi possibili. Se i pattern non sono esaustivi, il compilatore di ML genera un \textit{warning}. Se a runtime viene passato un argomento che non fa match con alcun pattern dichiarato, viene sollevata un'eccezione critica (\texttt{Exception- Match raised}).
    \item \textbf{Pattern validi e non validi:}
    \begin{itemize}
        \item \textit{Consentiti:} Costanti (es. \texttt{nil}, \texttt{0}, stringhe), costruttori di liste come l'operatore cons (es. \texttt{x::xs}, \texttt{x::y::xs}), tuple (es. \texttt{(x, y, z)}) e il carattere jolly o \textit{wildcard} \texttt{\_} (usato per i valori "anonimi" che non ci interessa memorizzare né usare).
        \item \textit{Non consentiti:} Operatori aritmetici (non si può scrivere \texttt{fun square(x+1) = ...}), concatenazione di liste tramite l'operatore \texttt{@}, numeri reali (\texttt{real}) e l'uso multiplo della stessa variabile in un singolo pattern (es. \texttt{fun uguali(n, n) = true} è illegale, la variabile \texttt{n} è già associata). L'identità logica va testata nel corpo con \texttt{if}.
    \end{itemize}
\end{itemize}

\textbf{Il costrutto \texttt{as}:}
Spesso è utile destrutturare un parametro (ad esempio separando testa e coda di una lista) ma al tempo stesso mantenere un riferimento "globale" all'intero oggetto originale per non doverlo ricostruire. La keyword \texttt{as} risolve questa necessità sintattica: scrivendo \texttt{L as x::xs}, la variabile \texttt{L} rappresenta la lista intera, mentre \texttt{x} e \texttt{xs} ne catturano rispettivamente testa e coda.

\textbf{Costrutti \texttt{case} e \texttt{fn}:}
La sintassi del pattern matching si applica in modo identico anche alle funzioni anonime (\texttt{fn}) e al costrutto interno \texttt{case}:
\begin{verbatim}
case x of
    pattern_1 => espressione_1
  | pattern_2 => espressione_2;
\end{verbatim}
La differenza architetturale è che il \texttt{case} permette di effettuare pattern matching sul risultato momentaneo di un'espressione qualsiasi nel bel mezzo di una funzione, e non soltanto sui parametri di ingresso della stessa.

\textbf{Ambiente locale (\texttt{let ... in ... end}):}
Per definire variabili temporanee, utilissime per memorizzare sotto-espressioni ricorrenti o decomporre i complessi risultati in uscita da una funzione, ML offre il costrutto \texttt{let}.
\begin{verbatim}
let 
    val x = calcolo_pesante()
    val (a, b) = scorpora_tupla() 
in 
    a + b + x
end
\end{verbatim}
I "binding" (assegnazioni) creati tra \texttt{let} e \texttt{in} arricchiscono l'ambiente locale ma restano visibili esclusivamente all'interno del blocco di codice delimitato tra \texttt{in} ed \texttt{end}.

\subsection{Implementazione del codice}

\subsubsection{Merge e uso di \texttt{as}}
Un esempio didattico eccellente in cui \texttt{as} semplifica la stesura è l'unione ordinata di due liste.
\begin{verbatim}
fun merge (nil, M) = M
  | merge (L, nil) = L
  | merge (L as x::xs, M as y::ys) =
      if x < y then x :: merge(xs, M)
      else y :: merge(L, ys);
\end{verbatim}
Senza la keyword \texttt{as}, nel ramo \texttt{else} avremmo dovuto ricostruire a mano e sprecare risorse per ricreare la prima lista scrivendo \texttt{merge(x::xs, ys)}.

\subsubsection{Utilizzo del \texttt{case} e \textit{wildcard}}
\begin{verbatim}
val is_one = fn x =>
    case x of
        1 => "one"
      | _ => "anything else";
\end{verbatim}


\section{Laboratorio ML 4}

Il quarto laboratorio e i relativi esercizi di tutorato impongono l'abbandono delle funzioni primitive \texttt{hd} e \texttt{tl} viste all'inizio del corso, in favore dell'uso sistematico e ricorsivo del pattern matching. Le tecniche apprese qui costituiscono la base per affrontare le prove d'esame.

\subsection{Destrutturazione avanzata di liste e tuple}

\textbf{Esercizio L4.1 - Flip alternato:}
Scrivere una funzione \texttt{flip} che inverte a due a due gli elementi di una lista e, se dispari, lascia l'ultimo elemento inalterato alla fine.
\begin{verbatim}
fun flip ([]) = []
  | flip ([x]) = [x]
  | flip (x::y::zs) = y :: x :: flip(zs);
\end{verbatim}

\textbf{Esercizio L4.4 - Flip di tuple in lista:}
Applicazione combinata di pattern matching su liste e tuple che sfrutta il costrutto \texttt{as} per risparmiare allocazioni di memoria.
\begin{verbatim}
fun flip (nil) = nil
  | flip ((x as (a:int, b)) :: xs) = 
      if a < b then x :: flip(xs) 
      else (b, a) :: flip(xs);
\end{verbatim}
\textbf{Nota per l'esame:} Se la tupla \texttt{(a,b)} è già posizionata nell'ordine corretto (\texttt{a < b}), inseriamo nella chiamata ricorsiva direttamente \texttt{x} (l'intera tupla catturata inalterata con \texttt{as}) senza sprecare risorse della macchina per ricreare inutilmente una nuova tupla \texttt{(a,b)}.

\textbf{Esercizio dal Tutorato 3 - Separazione e Let-Val:}
Un grande classico d'esame: dividere una lista in due liste separate (una per i pari, una per i dispari), mantenendo l'ordine, esplorandola una sola volta.
\begin{verbatim}
fun separaPariDispari (nil) = (nil, nil)
  | separaPariDispari (x::xs) =
    let
        (* Decompone elegantemente la tupla restituita dalla ricorsione *)
        val (pari, dispari) = separaPariDispari (xs)
    in
        if x mod 2 = 0 then (x::pari, dispari)
        else (pari, x::dispari)
    end;
\end{verbatim}
In questo frammento logico, il \texttt{let} evita la disastrosa pratica di calcolare due volte la ricorsione \texttt{separaPariDispari(xs)} (una volta per la testa e una volta per la coda).

\newpage

\subsection{Insiemi Logici tramite Liste (Esercizi L4.6 - L4.8)}
Nel corso non esistono strutture dati pre-fabbricate per gli "Insiemi" (Set). Devono essere simulati e controllati manualmente garantendo l'assenza di duplicati logici.

\textbf{Appartenenza (member) ed Eliminazione (delete):}
L'uso della \textit{wildcard} \texttt{\_} per il parametro non utilizzato ("x") nel caso base previene i \textit{warning} di tipo.
\begin{verbatim}
fun member (_, nil) = false
  | member (x, y::ys) = (x = y orelse member(x, ys));

fun delete (a, []) = []
  | delete (b, c::ys) = 
      if b = c then ys 
      else c :: delete(b, ys);
\end{verbatim}

\textbf{Inserimento con controllo di univocità (insert):}
Anche qui l'efficienza si ottiene combinando l'ambiente locale tramite l'identificatore strutturato.
\begin{verbatim}
fun insert (x, nil) = [x]
  | insert (x, S as y::ys) = 
      if x = y then S 
      else y :: insert(x, ys);
\end{verbatim}


\section{Input-output, eccezioni e funzioni polimorfe}

\subsection{Spiegazione teorica}
Questa sezione affronta la gestione delle operazioni di input/output, il sistema di controllo e recupero degli errori a runtime tramite le eccezioni, e i fondamenti del polimorfismo nel sistema di tipi di ML.

\textbf{Input e Output in ML:}
A differenza della valutazione pura di espressioni matematiche, le operazioni di I/O introducono nel linguaggio il concetto di \textbf{effetto collaterale} (side-effect), modificando lo stato dei canali di comunicazione standard o dei file memorizzati su disco.
\begin{itemize}
    \item \textbf{Output basico:} La funzione predefinita \texttt{print} accetta come argomento una stringa e la stampa sullo standard output. La funzione possiede la firma \texttt{string -> unit}; restituisce l'unico valore del tipo unit, ovvero la tupla vuota \texttt{()}, indicando che l'espressione ha esaurito la sua computazione producendo unicamente un effetto collaterale a video.
    \item \textbf{Espressioni composte:} Per eseguire una sequenza ordinata di operazioni che causano effetti collaterali, ML permette l'uso di espressioni composte separate dal punto e virgola e racchiuse tra parentesi tonde: \texttt{(exp1; exp2; ...; expn)}. Il tipo e il valore finale dell'intera macro-espressione sono determinati unicamente dall'ultima sotto-espressione valutata (\texttt{expn}). Le istruzioni intermedie servono esclusivamente per i loro effetti collaterali e l'ultima istruzione non necessita del punto e virgola di chiusura.

\newpage
    
    \item \textbf{Gestione dei File (\texttt{TextIO}):} ML offre supporto linguistico all'interazione con il file system tramite la struttura standard \texttt{TextIO}. I canali di lettura vengono gestiti tramite oggetti di tipo \texttt{TextIO.instream}. Le funzioni principali includono:
    \begin{itemize}
        \item \texttt{TextIO.openIn(filename)}: Apre un file di testo in modalità lettura e restituisce un canale \texttt{instream}.
        \item \texttt{TextIO.endOfStream(infile)}: Restituisce un valore booleano che indica se si è raggiunta la fine del file.
        \item \texttt{TextIO.inputN(infile, n)}: Legge una quantità fissa pari a \texttt{n} caratteri dal file, restituendoli sotto forma di stringa.
        \item \texttt{TextIO.input(infile)}: Legge l'intero contenuto rimanente del file come un'unica stringa.
        \item \texttt{TextIO.inputLine(infile)}: Legge una riga di testo comprensiva del carattere di newline \texttt{\textbackslash n}. Restituisce un tipo speciale costruttore denominato \textbf{\texttt{option}}.
    \end{itemize}
\end{itemize}

\textbf{Il tipo costruttore \texttt{option}:}
In presenza di funzioni che possono non restituire un valore valido (come la lettura oltre la fine di un file o la ricerca in un dizionario), ML evita l'uso di puntatori nulli fragili introducendo il tipo parametrico \texttt{'a option}. Un oggetto di tipo option può assumere due forme:
\begin{itemize}
    \item \texttt{SOME v}: Contiene il valore effettivo \texttt{v} di tipo \texttt{'a}.
    \item \texttt{NONE}: Indica l'assenza di un valore valido (es. fine del file raggiunta).
\end{itemize}
Per estrarre il valore da una variabile option forzandone il tipo da \texttt{'a option} ad \texttt{'a}, si utilizza la funzione di sistema \texttt{valOf}. Se applicata su un elemento che contiene \texttt{SOME v}, restituisce \texttt{v}; se applicata su \texttt{NONE}, interrompe il programma sollevando l'eccezione \texttt{Option}.

\textbf{Eccezioni in ML:}
Le eccezioni sono costrutti linguistici semantici per interrompere il flusso normale del programma in presenza di errori a runtime.
\begin{itemize}
    \item \textbf{Eccezioni di sistema:} Sollevate automaticamente dalla macchina astratta di ML. Esempi comuni sono \texttt{Div} (divisione per zero), \texttt{Empty} (applicazione di \texttt{hd} o \texttt{tl} su liste vuote) e \texttt{Chr} (conversione ASCII fuori range).
    \item \textbf{Eccezioni utente e parole chiave:} Il programmatore può definire nuove eccezioni nell'ambiente tramite la keyword \texttt{exception Nome}. Le eccezioni possiedono un tipo primitivo dedicato chiamato \texttt{exn}. Per innescare materialmente un errore si usa la keyword \texttt{raise Nome}.
    \item \textbf{Eccezioni con parametri:} È possibile associare un contenuto informativo all'eccezione definendola tramite la sintassi \texttt{exception Nome of tipo}. In questo caso l'identificatore agisce da costruttore (es. \texttt{exception Errore di int}, lanciata con \texttt{raise Errore(42)}).
    \item \textbf{Gestione delle eccezioni (\texttt{handle}):} Per intercettare un errore e ripristinare il flusso si appone l'operatore \texttt{handle} a valle di un'espressione, implementando una sintassi analoga al pattern matching: \texttt{<espressione> handle PatternEccezione => espressione\_di\_recupero}.
\end{itemize}

\textbf{Funzioni polimorfe in ML:}
Il termine polimorfismo indica la capacità di un frammento di codice di operare su molteplici forme e tipi di dato. ML adotta un approccio statico rigoroso (\textit{compile-time strong typing}), ma implementa il polimorfismo parametrico: se una funzione non esegue operazioni esclusive di un tipo specifico, il compilatore le assegna una variabile di tipo generica indicata con un singolo apice iniziale (\texttt{'a}, \texttt{'b}).

\textbf{Interazione tra Operatori e Polimorfismo:}
\begin{itemize}
    \item \textbf{Operatori che restringono il polimorfismo:} Gli operatori aritmetici classici (\texttt{+}, \texttt{-}, \texttt{*}), gli operatori di divisione (\texttt{/}, \texttt{div}, \texttt{mod}), i confronti di disuguaglianza (\texttt{<}, \texttt{>}), i connettivi booleani e le funzioni di conversione esplicita forzano i parametri ad assumere tipi primitivi rigidi (es. \texttt{int} o \texttt{real}), inibendo il polimorfismo.
    \item \textbf{Operatori che preservano il polimorfismo:} Gli operatori sulle tuple (creazione e proiezioni \texttt{\#n}), gli operatori nativi sulle liste (\texttt{::}, \texttt{@}, \texttt{hd}, \texttt{tl}, \texttt{nil}) e gli operatori di uguaglianza logica consentono l'uso di tipi generici parametrici.
    \item \textbf{Tipi di uguaglianza (Equality types):} Gli operatori \texttt{=} e \texttt{<>} richiedono che i tipi messi a confronto supportino la verifica di uguaglianza. Sono considerati tipi di uguaglianza gli interi, i booleani, i caratteri, le stringhe e le tuple/liste composte dagli stessi. Sono strutturalmente \textbf{esclusi} i numeri reali (\texttt{real}) e tutte le funzioni. 
    Le variabili di tipo generico vincolate a supportare esclusivamente l'uguaglianza vengono identificate dal compilatore con un \textbf{doppio apice iniziale} (\texttt{''a}). Cercare di applicare funzioni con vincolo \texttt{''a} su numeri reali genera un errore statico di unificazione del tipo.
    \item \textbf{Osservazione critica sull'inversione di liste:} Se si scrive una funzione di inversione testando la fine della lista tramite il controllo booleano \texttt{if L = nil then ...}, l'uso dell'operatore \texttt{=} costringe il compilatore ad assegnare alla funzione il tipo vincolato \texttt{''a list -> ''a list}. Di conseguenza, la funzione fallirà il controllo dei tipi se applicata a liste di numeri reali o di funzioni. Per preservare il polimorfismo puro (\texttt{'a list -> 'a list}), si deve ricorrere al pattern matching sui costruttori della lista o utilizzare la funzione predefinita \texttt{null(L)}.
\end{itemize}

\newpage

\subsection{Implementazione del codice}

\subsubsection{Esempio di I/O e gestione dei flussi di lettura da File}
\begin{verbatim}
(* Funzione ricorsiva ausiliaria che consuma lo stream carattere 
 per carattere *)
 
fun makeList1 (infile, NONE) = nil
  | makeList1 (infile, SOME c) = 
        c :: makeList1(infile, TextIO.input1(infile));

(* Inizializza la lettura estraendo il primo carattere *)
fun makeList (infile) = makeList1(infile, TextIO.input1(infile));

(* Utilizzo completo con apertura e chiusura del canale *)
fun readFileAndConvert (filename) =
    let
        val instream = TextIO.openIn(filename)
        val charList = makeList(instream)
    in
        TextIO.closeIn(instream); (* Effetto collaterale di chiusura *)
        charList                  (* Restituisce la lista di caratteri *)
    end;
\end{verbatim}

\subsubsection{Eccezioni personalizzate e meccanismi di recupero (\texttt{handle})}
\begin{verbatim}
exception OutOfRange of int * int;

fun comb1 (n, m) =
    if n <= 0 then raise OutOfRange(n, m)
    else if m < 0 orelse m > n then raise OutOfRange(n, m)
    else if m = 0 orelse m = n then 1
    else comb1(n - 1, m) + comb1(n - 1, m - 1);

(* La funzione comb fa da wrapper gestendo l'eccezione tramite handle *)
fun comb (n, m) = 
    comb1(n, m) 
    handle OutOfRange(0, 0) => 1
         | OutOfRange(n, m) => 
            (print("Error: values out of range. n=" ^ Int.toString(n) ^ 
                   " m=" ^ Int.toString(m) ^ "\n"); 
             0);
\end{verbatim}

\newpage

\subsubsection{Polimorfismo puro vs Polimorfismo di uguaglianza}
\begin{verbatim}
(* Funzione con polimorfismo puro: accetta qualsiasi tipo *)
fun identity (x) = x;
(* Tipo assegnato: val identity = fn: 'a -> 'a *)

(* Funzione con polimorfismo vincolato all'uguaglianza *)
fun identity_eq (x) = if (x = x) then x else x;
(* Tipo assegnato: val identity_eq = fn: ''a -> ''a *)

(* Funzione di inversione che preserva il polimorfismo totale *)
fun rev_polymorphic (nil) = nil
  | rev_polymorphic (x::xs) = rev_polymorphic(xs) @ [x];
(* Tipo assegnato: val rev_polymorphic = fn: 'a list -> 'a list *)
\end{verbatim}

\section{Laboratorio ML 5}

Il quinto laboratorio si concentra sull'ottimizzazione delle funzioni ricorsive tramite l'uso efficiente dell'ambiente locale (\texttt{let val}), sull'analisi delle espressioni di controllo condizionale annidate (\texttt{case}) e sull'implementazione pratica di funzioni che generano effetti collaterali tramite la stampa a video.

\subsection{Ottimizzazione dell'Ambiente Locale (\texttt{let val})}

\textbf{Esercizio L5.4 - Insieme delle parti (powerSet) efficiente:}
Rispetto alla versione base in cui la chiamata ricorsiva sulla coda della lista veniva calcolata due volte raddoppiando la complessità a ogni passo, l'introduzione di un legame temporaneo nel \texttt{let} riduce drasticamente i tempi di calcolo memorizzando il risultato parziale.
\begin{verbatim}
(* Funzione di supporto per inserire un elemento in tutte le sotto-liste *)
fun insertAll (x, nil) = nil
  | insertAll (x, y::ys) = (x::y) :: insertAll(x, ys);

fun powerSet (nil) = [nil]
  | powerSet (x::xs) =
    let
        val tailPowerSet = powerSet(xs) (* Calcolata una sola volta *)
    in
        tailPowerSet @ insertAll(x, tailPowerSet)
    end;
\end{verbatim}

\newpage

\textbf{Esercizio L5.7 - Computazione efficiente di $x^{2^i}$ (\texttt{doubleExp}):}
Scrivere un programma che calcoli la potenza doppia effettuando una sola chiamata ricorsiva per ogni decremento dell'indice $i$.
\begin{verbatim}
fun doubleExp (x: real, 0) = x
  | doubleExp (x, i) =
    let
        val y = doubleExp(x, i - 1)
    in
        y * y
    end;
\end{verbatim}

\textbf{Esercizio L5.3 - Destrutturazione di Tuple senza Pattern Matching diretto:}
Scrivere la funzione \texttt{split} estraendo i valori della tupla risultante tramite l'uso esplicito delle funzioni di proiezione \texttt{\#1} e \texttt{\#2} all'interno del blocco locale.
\begin{verbatim}
fun split (nil) = (nil, nil)
  | split ([a]) = ([a], nil)
  | split (a::b::cs) =
    let
        val tuplaRisultato = split(cs)
        val M = #1 tuplaRisultato
        val N = #2 tuplaRisultato
    in
        (a::M, b::N)
    end;
\end{verbatim}

\subsection{Uso delle espressioni \texttt{case}}

\textbf{Esercizio L5.1 - Simulazione dell'if-then-else tramite \texttt{case}:}
Implementare la funzione \texttt{is\_lower\_than5} verificando la validità della condizione logica booleana tramite pattern matching interno.
\begin{verbatim}
fun is_lower_than5 n =
    case (n < 5) of
        true => 1
      | false => 2;
\end{verbatim}

\subsection{Esercizi di Input/Output e Stampa}

\textbf{Esercizio L5.9 - Stampa di una lista (\texttt{printList}):}
Scrivere una funzione che accetti una lista di interi e la stampi riga per riga sullo standard output.
\begin{verbatim}
fun printList (nil) = ()
  | printList (x::xs) =
    (
        print(Int.toString(x));
        print("\n");
        printList(xs)
    );
\end{verbatim}

\textbf{Esercizio L5.10 - Calcolo e Formattazione Combinazioni (\texttt{comb}):}
Scrivere una funzione che calcoli il coefficiente binomiale stampando a video in modo strutturato i parametri inseriti e il risultato finale ottenuto.
\begin{verbatim}
fun factorial 0 = 1
  | factorial n = n * factorial(n - 1);

fun comb (n, m) =
    (
        print("n is ");
        print(Int.toString(n));
        print("\n");
        print("m is ");
        print(Int.toString(m));
        print("\n");
        print("Result is ");
        print(Int.toString(factorial(n) div (factorial(m) * factorial(n - m))));
        print("\n")
    );
\end{verbatim}

\section{Funzioni di ordine superiore e curried}

\subsection{Spiegazione teorica}
In ML le funzioni sono trattate come "cittadini di prima classe", il che significa che possono essere assegnate a variabili, passate come argomenti ad altre funzioni o restituite come risultato. Una funzione che accetta o restituisce un'altra funzione prende il nome di \textbf{Funzione di Ordine Superiore} (Higher-Order Function).

\textbf{Gestione dell'Ambiente e Binding:}
Quando si passa una funzione come parametro o la si restituisce in uscita, sorge un problema architetturale: quale ambiente non-locale deve essere utilizzato durante la sua futura esecuzione?
\begin{itemize}
    \item \textbf{Deep binding:} L'ambiente non-locale viene "congelato" al momento della \textit{creazione} (o passaggio) della funzione. È l'approccio adottato in accoppiata allo \textit{Scoping Statico}.
    \item \textbf{Shallow binding:} L'ambiente non-locale viene valutato solo al momento in cui la funzione viene effettivamente \textit{chiamata}.
\end{itemize}
Come già visto, ML utilizza rigorosamente lo \textbf{Scoping Statico} abbinato al \textbf{Deep binding}. Questa combinazione viene tecnicamente implementata attraverso le \textit{Closure}, ovvero strutture dati nascoste che memorizzano il codice della funzione e l'ambiente al momento della sua definizione, garantendone la robustezza e la prevedibilità.

\newpage

\textbf{Funzioni Curried (Currying):}
A livello formale e sintattico, \textbf{ogni funzione in ML accetta uno e un solo argomento}. Fino a questo momento abbiamo aggirato questo limite matematico passando argomenti multipli impacchettati all'interno di una tupla (es. \texttt{fun max(x, y)}).
Un approccio alternativo, proprio dei linguaggi puramente funzionali, è la \textbf{forma Curried} (in onore del logico Haskell Curry). Una funzione a due argomenti viene progettata in modo tale da prendere il primo argomento e \textit{restituire una nuova funzione} che attenderà il secondo.
\begin{itemize}
    \item \textit{Sintassi Tupla:} \texttt{fun exp1(x, y) = ...} ha tipo \texttt{real * int -> real}.
    \item \textit{Sintassi Curried:} \texttt{fun exp2 x y = ...} ha tipo \texttt{real -> int -> real} (valutato a destra come \texttt{real -> (int -> real)}).
    \item \textbf{Vantaggio: Applicazione Parziale (Partial Instantiation).} Le funzioni curried permettono di fornire soltanto una parte degli argomenti richiesti, generando istantaneamente una nuova funzione "specializzata" da riutilizzare a runtime. Ad esempio, potremmo fissare la base di una potenza a 3.0 e usare la nuova funzione creata solo per far variare l'esponente.
\end{itemize}

\subsection{Implementazione del codice}

\subsubsection{Funzioni di Ordine Superiore Classiche (Implementate a mano)}
Costruiamo le versioni semplificate delle classiche funzioni funzionali per comprendere il loro comportamento interno (il linguaggio ML dispone già di versioni built-in altamente ottimizzate).

\begin{verbatim}
(* 1. simpleMap: applica la funzione F a tutti gli elementi di una lista *)
fun simpleMap (F, nil) = nil
  | simpleMap (F, x::xs) = F(x) :: simpleMap(F, xs);
(* Tipo: ('a -> 'b) * 'a list -> 'b list *)

(* 2. filter: mantiene solo gli elementi che soddisfano il predicato P *)
fun filter (P, nil) = nil
  | filter (P, x::xs) = 
        if P(x) then x :: filter(P, xs) 
        else filter(P, xs);
(* Tipo: ('a -> bool) * 'a list -> 'a list *)

(* 3. reduce: collassa la lista applicando ricorsivamente la funzione F *)
exception EmptyList;
fun reduce (F, nil) = raise EmptyList
  | reduce (F, [a]) = a
  | reduce (F, x::xs) = F(x, reduce(F, xs));
(* Tipo: ('a * 'a -> 'a) * 'a list -> 'a *)
\end{verbatim}

\newpage

\subsubsection{Currying e Applicazione Parziale}
\begin{verbatim}
(* Funzione in forma Curried, notare l'assenza di virgole e parentesi *)
fun exponent2 x 0 = 1.0
  | exponent2 x y = x * exponent2 x (y - 1);

(* Possiamo chiamarla fornendo subito entrambi i parametri... *)
> exponent2 3.0 4;
val it = 81.0 : real

(* ...oppure possiamo fornire solo il primo parametro (la base 3.0),
   ottenendo in ritorno una NUOVA funzione pronta a ricevere un esponente *)
> val powerOfThree = exponent2 3.0;
val powerOfThree = fn : int -> real

> powerOfThree 4;
val it = 81.0 : real
\end{verbatim}


\section{Laboratorio ML 6}

Nel sesto laboratorio si fondono le nozioni di I/O (File e testuali) introdotte marginalmente nella Lezione 7, assieme alle Funzioni di Ordine Superiore, per costruire elaboratori dati puramente funzionali.

\subsection{Elaborazione di Flussi di Testo (I/O)}
Viene esplorata l'interazione con l'esterno usando la struttura \texttt{TextIO}, supportata dai tipi costruttori come \texttt{option} (per gestire letture di fine file che restituiranno \texttt{NONE}) e dal pattern matching per il disinnesco degli errori.

\textbf{Esercizio base: Primitive \texttt{TextIO}}
\begin{verbatim}
(* 1. Aprire un file in lettura (restituisce un instream) *)
val input = TextIO.openIn("file.txt");

(* 2. Leggere i successivi 5 caratteri (restituisce string) *)
TextIO.inputN(input, 5);

(* 3. Leggere una riga intera (restituisce string option) *)
TextIO.inputLine(input); 

(* 4. Leggere il prossimo carattere senza consumarlo dal flusso *)
TextIO.lookahead(input);

(* 5. Chiudere rigorosamente il flusso per liberare la risorsa *)
TextIO.closeIn(input);
\end{verbatim}

\subsection{Esercizi dal Tutorato: I/O Sicuro ed Eccezioni}
Nel tutorato viene richiesto di costruire dei gestori di file incapsulati che intercettano gli errori di sistema per lanciare eccezioni di dominio semantiche. 

\textbf{Esercizio: Leggere la prima riga in sicurezza}
Scrivere una funzione che tenti di leggere la prima riga di un file. Se il file non esiste, bisogna catturare l'eccezione di sistema di basso livello (\texttt{Io}) per sollevarne una propria (\texttt{FileNotFound}). Se il file esiste ma è vuoto, lanciare \texttt{EmptyFile}.

\begin{verbatim}
exception FileNotFound of string;
exception EmptyFile;
fun readFirstLine(filename) =
    let 
        (* Tentativo di apertura controllato tramite handle *)
        val file = TextIO.openIn(filename) 
                   handle Io => raise FileNotFound(filename)
        val lineOpts = TextIO.inputLine(file)
    in
        TextIO.closeIn(file);
        (* Valutazione polimorfica dell'option per l'esito della lettura *)
        case lineOpts of
            NONE => raise EmptyFile
          | SOME(line) => line
    end;
\end{verbatim}

\subsection{Combinare I/O e Ordine Superiore}

\textbf{Esercizio L6.9 - Lettura e Somma (readAndSum):}
Scrivere una funzione che legga un file di testo contenente un numero (stringa) per ogni riga, ne esegua il parsing ad intero e ne calcoli la somma totale. Si richiede l'uso della funzione nativa \texttt{Int.fromString} che trasforma stringhe in interi, restituendo un \texttt{int option} di sicurezza.

\begin{verbatim}
fun readAndSum1 file =
    if TextIO.endOfStream(file) then 0
    else (* valOf forza l'estrazione "scartando" il costruttore SOME, 
           estraendo il valore reale. Si usa due volte: 
           per l'inputLine e per fromString. Nota: l'uso di valOf è 
           considerato "unsafe" (può lanciare eccezioni); in produzione 
           si predilige sempre l'uso del pattern matching o del case.*)
           
        valOf(Int.fromString(valOf(TextIO.inputLine(file)))) 
        + readAndSum1(file);
fun readAndSum filename =
    let
        val f = TextIO.openIn(filename)
        val res = readAndSum1(f)
    in
        TextIO.closeIn(f); (* Chiusura garantita *)
        res
    end;
\end{verbatim}

\textbf{Esercizio L6.10 - Lista di funzioni curried (applyList):}
Un eccellente esercizio in forma curried: scrivere una funzione che accetti prima una lista di funzioni (es. elevamento al quadrato, al cubo) e in seguito un valore \texttt{a}, restituendo una lista in cui ogni funzione è stata iterativamente applicata al valore di partenza.

\begin{verbatim}
(* Funzione Curried: prende prima la lista Fs, poi si "congela"
   aspettando che venga fornito il valore 'a' *)
fun applyList nil a = nil
  | applyList (F::Fs) a = F(a) :: (applyList Fs a);

(* Esempio di utilizzo con istanziazione finale: *)
> applyList [fn x => x * 2, fn x => x * x * x] 4;
val it = [8, 64] : int list
\end{verbatim}

\section{Tipi di dati e composizione di funzioni}

\subsection{Spiegazione teorica}
In questa lezione si conclude la panoramica sulle funzioni di ordine superiore esaminando le primitive di libreria, l'operatore di composizione e, soprattutto, l'architettura per la definizione di nuovi tipi di dato e strutture ricorsive.

\textbf{Funzioni di Ordine Superiore di Sistema (Built-in):}
Le funzioni di ordine superiore fornite nativamente da ML (come \texttt{map}, \texttt{foldr} e \texttt{foldl}) sono \textbf{curried}, ovvero si aspettano gli argomenti passati in sequenza (separati da spazio) e non raggruppati in una singola tupla.
\begin{itemize}
    \item \textbf{map:} Applica una determinata funzione a ciascun elemento di una lista, restituendo una nuova lista con i risultati.
    \item \textbf{foldr (Fold Right):} "Collassa" una lista in un singolo valore combinando gli elementi da destra verso sinistra, partendo da un valore iniziale base. Corrisponde logicamente all'applicazione annidata: $f(x_1, f(x_2, \dots f(x_n, c)))$.
    \item \textbf{foldl (Fold Left):} Applica l'aggregazione partendo da sinistra verso destra. È spesso più efficiente poiché strutturabile tramite \textit{tail recursion}. Corrisponde a: $f(x_n, f(x_{n-1}, \dots f(x_1, c)))$.
\end{itemize}

\textbf{Composizione di Funzioni:}
La composizione matematica di due funzioni, $H(x) = G(F(x))$, viene supportata in ML dall'operatore infisso \textbf{\texttt{o}} (una semplice lettera 'o' minuscola). La sintassi \texttt{G o F} crea un'unica nuova funzione in cui l'output di \texttt{F} viene reindirizzato come input di \texttt{G}. È una tecnica dichiarativa fondamentale per concatenare manipolazioni di liste ed elaborazioni dati in catene di montaggio algoritmiche (\textit{pipeline}).

\textbf{Il Sistema dei Tipi in ML:}
Per estendere i tipi predefiniti (\texttt{int}, \texttt{real}, \texttt{string}, liste e tuple), ML mette a disposizione due costrutti principali:
\begin{itemize}
    \item \textbf{\texttt{type} (Ridenominazione e Alias):} Permette di creare un alias per un tipo già esistente o per un'associazione complessa, con lo scopo di rendere il codice più leggibile per il programmatore. Non genera veri e propri tipi nuovi per la macchina. \\
    Sintassi base: \texttt{type signal = int list;} oppure \texttt{type ('a, 'b) mapping = ('a * 'b) list;}
    \item \textbf{\texttt{datatype} (Tipi e Costruttori Strutturati):} Introduce tipologie di dato interamente nuove. Costituisce la base per i \textit{Tipi Unione} (\textit{Union Types} o ADT). 
    Un \texttt{datatype} definisce un \textbf{costruttore di tipo} (il nome generale) e uno o più \textbf{costruttori di dato} (i valori validi, separati dal pipe \texttt{|}). I costruttori di dato possono anche "trasportare" un parametro, permettendo di incapsulare valori (es. \texttt{S of 'a}).
\end{itemize}

\textbf{Tipi di dato Ricorsivi:}
Poiché i costruttori possono riferirsi al \texttt{datatype} stesso che stanno definendo, questo strumento linguistico è ideale per implementare \textbf{Alberi Binari}, liste concatenate custom o altre strutture dati dinamiche non lineari. 
Per generare \textbf{tipi mutuamente ricorsivi} (come un Albero la cui definizione dipende da una Foresta e viceversa), si connettono le rispettive dichiarazioni utilizzando la keyword \texttt{and}.

\subsection{Implementazione del codice}

\subsubsection{Composizione e Primitive List}
\begin{verbatim}
(* Utilizzo di map per estrarre la lunghezza delle stringhe *)
> map size ["La", "lezione", "nove"];
val it = [2, 7, 4] : int list

(* Utilizzo combinato di foldr per riprodurre un Logical AND su lista *)
> val andb = foldr (fn (x,y) => x andalso y) true;
val andb = fn : bool list -> bool
> andb [true, false, true];
val it = false : bool

(* Composizione elegante: converte i char in stringhe e li concatena *)
> val implode = (foldr (op ^) "") o (map str);
val implode = fn : char list -> string
\end{verbatim}

\subsubsection{Costrutti Datatype e Union Types}
\begin{verbatim}
(* Tipo Unione Base: Enumerazione *)
> datatype fruit = Apple | Pear | Grape;

(* Uso pratico dell'Unione col Pattern Matching *)
> fun isApple (x) = (x = Apple);
val isApple = fn : fruit -> bool

(* Tipo Elemento Generico: può contenere un Pair o un Valore Singolo *)
> datatype ('a, 'b) element = P of 'a * 'b | S of 'a;
> P("a", 1);
val it = P ("a", 1) : (string, int) element

(* Somma sicura ignorando gli elementi singoli S *)
fun sumElList (nil) = 0
  | sumElList (S(x)::L) = sumElList(L)
  | sumElList (P(x,y)::L) = y + sumElList(L);
\end{verbatim}


\section{Laboratorio ML 7}

Nel settimo laboratorio si sperimenta materialmente l'utilizzo delle funzioni curried e la definizione algoritmica di esplorazione degli Alberi Binari creati attraverso i \texttt{datatype}.

\subsection{Currying e Composizione Pratica (Esercizio L7.1 - L7.4)}
Scrivere una funzione che prende una funzione non curried (una che lavora sulle tuple) e la trasforma nella sua versione curried, separandone gli argomenti in chiamate ad anello.

\begin{verbatim}
(* Definizione di un generalizzatore Curry (arità 3) *)
fun curry F x1 x2 x3 = F(x1, x2, x3);

(* Utilizzo del trasformatore *)
> curry (fn (x,y,z) => x * y * z);
val it = fn : int -> int -> int -> int -> int

(* Assegnazione tramite Applicazione Parziale *)
> val G = curry (fn (x,y,z) => x * y * z) 1 2;
> G 3;
val it = 6 : int
\end{verbatim}

\subsection{Tipi di dato per gli Alberi Binari (Esercizi L7.5 - L7.10)}
L'albero binario generico per tutti gli esercizi di questa sezione viene così codificato:
\begin{verbatim}
datatype 'a btree = Empty 
                  | Node of 'a * 'a btree * 'a btree;
\end{verbatim}

\textbf{Esercizi L7.5 e L7.6 - Visita PostOrder e InOrder:}
Implementare le canoniche funzioni di esplorazione degli alberi riordinando le chiamate ricorsive e ricomponendo i rami tramite concatenazione di liste (\texttt{@}).
\begin{verbatim}
fun postOrder (Empty) = nil
  | postOrder (Node(a, left, right)) = 
      postOrder(left) @ postOrder(right) @ [a];

fun inOrder (Empty) = nil
  | inOrder (Node(a, left, right)) = 
      inOrder(left) @ [a] @ inOrder(right);
\end{verbatim}

\newpage

\textbf{Esercizio L7.8 e L7.10 - Calcolo e Manipolazione (sumTree e doubleTree):}
Sommare le componenti o alterare l'intero strato dei nodi clonando strutturalmente l'albero. Nel primo esempio si assume un albero che contiene tuple di tipo \texttt{string * int} come etichette dei nodi (\textit{mapTree}).
\begin{verbatim}
(* Somma la seconda componente (b) delle coppie di label *)
fun sumTree Empty = 0
  | sumTree (Node((a, b), left, right)) = 
      b + sumTree(left) + sumTree(right);

(* Raddoppia i valori producendo un albero binario della medesima forma *)
fun doubleTree Empty = Empty
  | doubleTree (Node(x, left, right)) = 
      Node(x * 2, doubleTree(left), doubleTree(right));
\end{verbatim}

\subsection{Esercizi e approfondimenti dal tutorato: Analisi di Struttura sugli Alberi}
Le simulazioni d'esame e le sessioni di tutorato (es. schede sugli Alberi Binari) arricchiscono le capacità di investigare la topologia dell'albero e valutarne gli attributi matematici (Alberi Bilanciati e Perfetti).

\textbf{Esercizio: Altezza e Albero Perfetto (\texttt{isPerfect})}
Un albero è perfetto se tutte le foglie giacciono sulla stessa identica profondità e ogni nodo non foglia ha precisamente due figli. Si deve valutare coerentemente l'uguaglianza tra i rami sottostanti.
\begin{verbatim}
(* Funzione di supporto vitale per l'analisi della topologia *)
fun height Empty = 0
  | height (Node(_, l, r)) = 1 + Int.max(height(l), height(r));

fun isPerfect Empty = true
  | isPerfect (Node(_, l, r)) = 
      height(l) = height(r) andalso isPerfect(l) andalso isPerfect(r);
\end{verbatim}

\newpage

\textbf{Esercizio: Albero Bilanciato (\texttt{isBalanced})}
Un albero è bilanciato se la profondità dei due sotto-alberi relativi a un qualsiasi nodo differisce in altezza, al massimo, per un valore assoluto di 1 (analogia con gli \textit{Alberi AVL}). La soluzione proposta previene uno scorrimento inefficiente adottando la restituzione di \texttt{\~{}1} come codice di errore di sbilanciamento (Fail-Fast pattern interno).
\begin{verbatim}
fun isBalanced t =
    let
        (* Restituisce l'altezza, o ~1 nel caso un ramo fosse sbilanciato *)
        fun checkBal Empty = 0
          | checkBal (Node(_, l, r)) =
            let
                val hl = checkBal(l)
                val hr = checkBal(r)
            in
                if hl = ~1 orelse hr = ~1 orelse abs(hl - hr) > 1 then ~1
                else 1 + Int.max(hl, hr)
            end
    in
        checkBal(t) <> ~1
    end;
\end{verbatim}
\textbf{Nota di ottimizzazione architetturale per l'esame:} Calcolare sistematicamente \texttt{height(l)} e poi chiamare di nuovo \texttt{isBalanced(l)} provocherebbe un costo esponenziale sull'attraversamento (l'albero verrebbe scorso ripetutamente ai livelli bassi). Questa funzione annidata incapsula l'esplorazione restituendo entrambe le informazioni in una \textit{singola} passata post-order, scalando la complessità ad $O(N)$.

\textbf{Esercizio: Albero Specchio (\texttt{mirrorTree})}
Data una radice asimmetrica, restituire il corrispettivo speculare ribaltando sistematicamente l'orientamento foglia-per-foglia.
\begin{verbatim}
fun mirrorTree Empty = Empty
  | mirrorTree (Node(v, l, r)) = Node(v, mirrorTree(r), mirrorTree(l));
\end{verbatim}

\section{Abstract Data Types in ML e Funtori}

\subsection{Spiegazione teorica}
Per costruire sistemi software scalabili e sicuri, i linguaggi moderni offrono meccanismi di \textbf{Astrazione dei Dati} (Data Abstraction). L'obiettivo è separare nettamente l'\textit{interfaccia} (cosa fa un tipo di dato e quali operazioni supporta) dalla sua \textit{implementazione} (come i dati sono materialmente rappresentati in memoria). 

In ML, questo incapsulamento (simile al concetto di classe e interfaccia in Java) viene realizzato attraverso tre costrutti fondamentali: \textbf{Signature}, \textbf{Structure} e \textbf{Functor}.

\textbf{1. Signatures (Firme):}
Una \textit{signature} agisce come un contratto pubblico o un'interfaccia. Definisce i tipi, le eccezioni e le firme delle funzioni che una struttura dovrà obbligatoriamente implementare.
\begin{itemize}
    \item Sintassi: \texttt{signature NOME\_SIG = sig ... end}
    \item Non contiene alcuna logica o codice eseguibile, ma solo dichiarazioni (\texttt{val}, \texttt{type}, \texttt{exception}).
\end{itemize}

\textbf{2. Structures (Strutture):}
Una \textit{structure} è l'implementazione concreta (il modulo) che fornisce il codice per i tipi e i metodi. Si collega a una signature per garantirne la validità strutturale (Ascription).
\begin{itemize}
    \item \textbf{Ascription Trasparente (\texttt{:})}: \texttt{structure S : SIG = struct ... end}. Il compilatore controlla che \texttt{S} rispetti \texttt{SIG}. Tuttavia, l'implementazione interna dei tipi rimane visibile all'esterno (violando parzialmente l'incapsulamento).
    \item \textbf{Ascription Opaca (\texttt{:>})}: \texttt{structure S :> SIG = struct ... end}. È l'astrazione dei dati per eccellenza. Tutto ciò che non è esplicitamente definito nella signature viene nascosto. Se il tipo interno è implementato come \texttt{int}, all'esterno verrà visto solo come tipo astratto inaccessibile, impedendo all'utente di farvi operazioni aritmetiche illecite.
\end{itemize}

\textbf{3. Functors (Funtori):}
Un funtore è una "struttura parametrizzata", ovvero una funzione che opera a livello di moduli. Prende in input una o più \textit{structure} (che rispettano determinate \textit{signature}) e genera in output una nuova \textit{structure}.
\begin{itemize}
    \item Sono il meccanismo principale in ML per la riusabilità del codice. Ad esempio, si può scrivere un funtore che genera un Albero Binario di Ricerca (BST) generico, passando in input una struttura che definisce semplicemente la funzione logica di ordinamento (es. alfabetico o numerico).
\end{itemize}

\newpage

\subsection{Implementazione del codice: Trasparenza vs Opacità}
Il seguente frammento (analizzato nel Lab 9) dimostra la potenza dell'ascription opaca per proteggere l'invariante di un tipo astratto.

\begin{verbatim}
(* 1. La Firma nasconde l'implementazione del tipo 't' *)
signature MY_INT = sig
    type t
    val crea: int -> t 
end;

(* 2. Struttura Trasparente: espone i dettagli *)
structure Transparent : MY_INT = struct
    type t = int
    fun crea x = x 
end;

(* 3. Struttura Opaca (ADT Puro): blinda i dettagli *)
structure Opaque :> MY_INT = struct
    type t = int
    fun crea x = x 
end;

(* Test di violazione dell'astrazione: *)
> val a = Transparent.crea(5);
> val b = a + 1; (* OK! Il compilatore sa che 'a' è un int *)

> val c = Opaque.crea(5);
> val d = c + 1; 
(* ERRORE FATALE! Il compilatore blocca l'operazione. 
   Per l'esterno, 'c' è di tipo astratto 't', non un 'int', 
   quindi l'operatore '+' non è applicabile. *)
\end{verbatim}


\section{Laboratorio ML 8 - Alberi Binari di Ricerca}

Il Laboratorio 8 e le relative schede di tutorato applicano le strutture astratte all'implementazione del \textbf{Binary Search Tree (BST)}, una struttura dati cruciale per la ricerca efficiente. La regola invariante di un BST stabilisce che per ogni nodo, tutti i valori nel sotto-albero sinistro devono essere strettamente minori del nodo, e tutti quelli nel sotto-albero destro strettamente maggiori.

\newpage

\subsection{Creazione di un BST tramite Functor (MakeBST)}

Per rendere l'albero riutilizzabile per interi, stringhe o record personalizzati, lo si genera tramite un funtore che riceve in input l'operatore di confronto \texttt{lt} (less than).

\begin{verbatim}
(* Step 1: Signature che definisce il contratto di ordinamento *)
signature TOTALORDER = sig
    type t
    val lt : t * t -> bool
end;

(* Step 2: Struttura concreta per stringhe *)
structure StringOrder : TOTALORDER = struct
    type t = string
    fun lt (a, b) = a < b
end;

(* Step 3: Funtore per costruire l'albero *)
functor MakeBST (Order : TOTALORDER) = struct
    datatype bst = Empty 
                 | Node of Order.t * bst * bst
                 
    fun insert (x, Empty) = Node(x, Empty, Empty)
      | insert (x, T as Node(y, left, right)) =
          if Order.lt(x, y) then 
              Node(y, insert(x, left), right)
          else if Order.lt(y, x) then 
              Node(y, left, insert(x, right))
          else T (* L'elemento esiste già, l'albero resta inalterato *)
end;

(* Step 4: Istanziazione della struttura finale *)
structure StringBST = MakeBST(StringOrder);
\end{verbatim}

\newpage

\subsection{Esercizi e approfondimenti dal tutorato (Strutture e BST)}

Nelle simulazioni d'esame è frequentissima la richiesta di definire una coda o un dizionario proteggendone lo stato tramite \texttt{signature}, oppure di manipolare un BST.

\textbf{Esercizio: Progettazione ADT della Coda (Queue)}
Definire una \textit{signature} e un'implementazione opaca per una Coda FIFO.
\begin{verbatim}
signature QUEUE = sig
    type 'a queue
    exception EmptyQueue
    val emptyQueue: 'a queue
    val enqueue: 'a -> 'a queue -> 'a queue
    val dequeue: 'a queue -> 'a * 'a queue
end;

structure Queue :> QUEUE = struct
    type 'a queue = 'a list
    exception EmptyQueue
    
    val emptyQueue = []
    
    (* Inserimento in coda (inefficiente su liste lunghe, ma didattico) *)
    fun enqueue x [] = [x]
      | enqueue x (y::ys) = y :: enqueue x ys
      
    (* Estrazione dalla testa *)
    fun dequeue [] = raise EmptyQueue
      | dequeue (x::xs) = (x, xs)
end;
\end{verbatim}

\textbf{Esercizio: Estrazione del Minimo in un BST (\texttt{deletemin})}
Poiché nel BST i valori più piccoli collassano a sinistra, per trovare e rimuovere il minimo bisogna scendere lungo il ramo sinistro finché non si trova un nodo senza figli a sinistra.
\begin{verbatim}
exception EmptyTree;

fun deletemin Empty = raise EmptyTree
  (* Caso base: nessun figlio sinistro, questo è il minimo! 
     Ritorno il valore e 'aggancio' l'eventuale ramo destro *)
  | deletemin (Node(y, Empty, right)) = (y, right)
  
  (* Caso ricorsivo: scendo a sinistra *)
  | deletemin (Node(w, left, right)) =
    let
        val (y, new_left) = deletemin(left)
    in
        (y, Node(w, new_left, right))
    end;
\end{verbatim}

\newpage

\textbf{Esercizio: Ricerca di minimi e massimi in un'unica passata}
Esercizio avanzato del tutorato che sfrutta le tuple nell'ambiente locale per esplorare simultaneamente i bordi di un albero binario.
\begin{verbatim}
fun find_min_max Empty = raise EmptyTree
  | find_min_max (Node(v, Empty, Empty)) = (v, v)
  
  (* Ha solo il figlio sinistro *)
  | find_min_max (Node(v, left, Empty)) =
    let val (min_sx, max_sx) = find_min_max left
    in (Int.min(v, min_sx), Int.max(v, max_sx)) end
    
  (* Ha solo il figlio destro *)
  | find_min_max (Node(v, Empty, right)) =
    let val (min_dx, max_dx) = find_min_max right
    in (Int.min(v, min_dx), Int.max(v, max_dx)) end
    
  (* Ha entrambi i figli *)
  | find_min_max (Node(v, left, right)) =
    let 
        val (min_sx, max_sx) = find_min_max left
        val (min_dx, max_dx) = find_min_max right
    in 
        (Int.min(v, Int.min(min_sx, min_dx)), 
         Int.max(v, Int.max(max_sx, max_dx))) 
    end;
\end{verbatim}


\section{Lambda Calcolo}

\subsection{Spiegazione teorica}
Il \textbf{Lambda calcolo ($\lambda$-calcolo)} è un sistema formale logico-matematico sviluppato da Alonzo Church negli anni '30 per investigare i fondamenti della computazione. Sebbene sia nato decenni prima dei computer moderni, costituisce il cuore teorico e il modello di calcolo di tutti i linguaggi di programmazione funzionali (come LISP, Haskell, ML e Scala).

\textbf{1. Sintassi del Lambda Calcolo:}
L'universo del lambda calcolo puro è estremamente essenziale. Non esistono numeri, booleani, stringhe o costrutti di base; esiste solo il concetto di "funzione anonima". 
Le espressioni (o $\lambda$-termini) si definiscono induttivamente tramite tre sole regole:
\begin{itemize}
    \item \textbf{Variabile:} Un identificatore $x, y, z \dots$ è un $\lambda$-termine.
    \item \textbf{Astrazione (Creazione di una funzione):} Se $e$ è un $\lambda$-termine e $x$ è una variabile, allora $\lambda x.e$ è un $\lambda$-termine. Indica la definizione di una funzione anonima il cui parametro formale è $x$ e il cui corpo è $e$ (equivalente a \texttt{fn x => e} in ML).
    \item \textbf{Applicazione (Invocazione di una funzione):} Se $e_1$ ed $e_2$ sono $\lambda$-termini, l'affiancamento $(e_1\; e_2)$ è un $\lambda$-termine. Rappresenta l'applicazione della funzione $e_1$ all'argomento $e_2$. L'applicazione è associativa a sinistra: $x\;y\;z$ equivale a $(x\;y)\;z$.
\end{itemize}

\textbf{2. Variabili Libere e Legate:}
In un'astrazione $\lambda x.e$, il simbolo $\lambda$ funge da operatore di \textit{binding} (legame). 
\begin{itemize}
    \item La variabile $x$ è detta \textbf{legata} (bound) all'interno del corpo $e$.
    \item Una variabile che compare in un'espressione senza essere vincolata da alcun $\lambda$ è detta \textbf{libera} (free).
    \item Esempio: In $(\lambda x.x\; y)$, $x$ è legata dal $\lambda x$, mentre $y$ è libera.
\end{itemize}

\textbf{3. Le Regole di Riduzione (La Computazione):}
Il calcolo progredisce attraverso riscritture sintattiche (riduzioni).
\begin{itemize}
    \item \textbf{Alpha-conversione ($\alpha$-riduzione):} È il rinominare in modo coerente le variabili legate per evitare "collisioni" o "catture" accidentali di nomi. 
    Esempio: $\lambda x.x$ (la funzione identità) è $\alpha$-equivalente a $\lambda y.y$ o $\lambda z.z$.
    \item \textbf{Beta-riduzione ($\beta$-riduzione):} È il vero motore della computazione. Definisce l'esecuzione di un'applicazione sostituendo il parametro formale con l'argomento attuale. \\
    Sintassi formale: $(\lambda x.e_1)\; e_2 \rightarrow_{\beta} e_1[e_2/x]$ \\
    (Si sostituiscono tutte le occorrenze \textit{libere} di $x$ all'interno di $e_1$ con il termine $e_2$, prestando attenzione ad applicare preventivamente un'$\alpha$-conversione se vi è rischio di sovrapposizioni di nomi).
\end{itemize}

\textbf{4. Forma Normale:}
Un $\lambda$-termine si definisce in \textbf{forma normale} se non contiene più alcun \textit{redex} (radicale da ridurre). In altre parole, non è più possibile applicare alcuna $\beta$-riduzione. La forma normale rappresenta il "risultato" finale del calcolo.

\textbf{5. Codifiche di Church (Encodings):}
Poiché nel $\lambda$-calcolo puro non ci sono tipi di dato, questi vengono "emulati" strutturalmente usando solo funzioni:
\begin{itemize}
    \item \textbf{Booleani:} \\
    $Vero \equiv \lambda x.\lambda y.x$ (prende due argomenti e restituisce il primo)\\
    $Falso \equiv \lambda x.\lambda y.y$ (prende due argomenti e restituisce il secondo)
    \item \textbf{Numeri Naturali (Church Numerals):} I numeri sono codificati come funzioni di ordine superiore che applicano una certa funzione $f$ un numero $n$ di volte a un argomento $x$. \\
    $0 \equiv \lambda f.\lambda x.x$ \\
    $1 \equiv \lambda f.\lambda x.f\; x$ \\
    $2 \equiv \lambda f.\lambda x.f\; (f\; x)$
\end{itemize}


\subsection{Implementazione e Riduzioni Logiche}

Vediamo alcuni esempi classici di $\beta$-riduzione. 
\textit{Nota notazionale:} Negli snippet e negli esercizi pratici seguenti, per comodità di formattazione ASCII (tipica degli interpreti o dei file di testo originali), l'operatore matematico $\lambda$ viene rappresentato sintatticamente tramite il carattere backslash (\texttt{\textbackslash}).

\subsubsection{Esempio 1: Applicazione di Base}
Valutazione di un'applicazione semplice:
\begin{verbatim}
Termine:    (\x.(\z.x z)) y
Regola:     Il parametro formale è x, l'argomento è y. 
            Sostituiamo tutte le occorrenze di x nel corpo (\z.x z) con y.
Risultato:  \z.y z
(Il risultato è in forma normale in quanto non ci sono altre applicazioni).
\end{verbatim}

\subsubsection{Esempio 2: Valutazione di un Booleano con operatore}
Modelliamo il costrutto \texttt{if True then A else B}:
\begin{verbatim}
Definizione If:    \b.\x.\y. b x y
Definizione True:  \u.\v. u

Valutazione di (If True A B):
1. (\b.\x.\y. b x y) (\u.\v. u) A B
2. Sostituiamo b: (\x.\y. (\u.\v. u) x y) A B
3. Sostituiamo x con A: (\y. (\u.\v. u) A y) B
4. Sostituiamo y con B: (\u.\v. u) A B
5. Sostituiamo u con A: (\v. A) B
6. Sostituiamo v con B: A 
Risultato corretto: Il ramo "True" ha restituito A.
\end{verbatim}

\subsection{Esercizi e approfondimenti dal tutorato: Computazione in Forma Normale}
All'esame viene spesso fornito un lungo e complesso termine di Lambda Calcolo e viene richiesto di mostrarne passo-passo la riduzione fino all'ottenimento della Forma Normale. L'accortezza fondamentale risiede nell'evitare le collisioni applicando correttamente le parente e la precedenza (associativa a sinistra).

\subsubsection{Esercizio L11.12: Riduzione annidata}
Si riduca il seguente termine a forma normale:
\begin{verbatim}
(\x. x z x) ((\y. y y x) z)
\end{verbatim}

\textbf{Svolgimento Passo-Passo:}
\begin{enumerate}
    \item Notiamo che c'è un'applicazione annidata sulla destra. Conviene prima ridurre l'argomento (\textit{Applicative Order}):
    \begin{verbatim}
    Sotto-termine destro: (\y. y y x) z
    Sostituisco y con z:  z z x
    \end{verbatim}
    \item Il nostro termine principale è ora semplificato:
    \begin{verbatim}
    (\x. x z x) (z z x)
    \end{verbatim}
    \item Attenzione alle collisioni! C'è un rischio di cattura di variabile per la $x$ finale. Per procedere in assoluta pulizia formale (anche se qui le $x$ combaciano come variabili libere di contesto), sostituiamo l'intero blocco $(z z x)$ al posto delle $x$ legate dal primo $\lambda$:
    \begin{verbatim}
    (z z x) z (z z x)
    \end{verbatim}
\end{enumerate}
\textbf{Risultato Finale:} $(z z x) z (z z x)$. L'espressione non contiene più alcun simbolo di astrazione $\lambda$ su cui effettuare applicazioni, pertanto è in forma normale.

\subsubsection{Esercizio L11.13: Riduzione con Catture Evitate}
Si riduca il seguente termine a forma normale:
\begin{verbatim}
Termine Iniziale:
(\x. x y) (\t. t z) ((\x. \z. x y z) y x)
\end{verbatim}

\textbf{Svolgimento Passo-Passo:}
\begin{enumerate}
    \item Isoliamo il blocco destro e risolviamolo per semplificare il lavoro. Notiamo l'applicazione di due argomenti in cascata (\texttt{y} e poi \texttt{x}).
    \begin{verbatim}
    Blocco destro: ((\x. \z. x y z) y) x
    Applico y alla x: (\z. y y z) x
    Applico x alla z: y y x
    \end{verbatim}
    \item Il termine intero diventa ora un'applicazione sequenziale:
    \begin{verbatim}
    (\x. x y) (\t. t z) (y y x)
    \end{verbatim}
    \item Il Lambda calcolo è associativo a sinistra. Applichiamo quindi il primo termine al secondo:
    \begin{verbatim}
    Sotto-termine sinistro: (\x. x y) (\t. t z)
    Sostituisco (\t. t z) al posto di x: (\t. t z) y
    Applico y al posto di t: y z
    \end{verbatim}
    \item Reinseriamo il risultato nell'equazione principale, unendo i due blocchi:
    \begin{verbatim}
    (y z) (y y x)
    \end{verbatim}
\end{enumerate}
\textbf{Risultato Finale:} $(y z) (y y x)$. Tutte le funzioni astratte sono state risolte.

\section{Programmazione Logica e Prolog}

\subsection{Spiegazione teorica}
Il paradigma logico si discosta radicalmente sia dalla programmazione imperativa (che descrive \textit{come} eseguire un calcolo modificando lo stato) sia da quella funzionale (che valuta espressioni e funzioni). La programmazione logica è **dichiarativa**: il programmatore descrive la struttura logica del problema (le relazioni tra le entità) e il motore interno del linguaggio si occupa di trovare la soluzione.

\newpage

\textbf{Fondamenti del Paradigma Logico:}
\begin{itemize}
    \item \textbf{Relazioni vs Funzioni:} In matematica, una funzione $f(x) = y$ mappa un input a un output. Una relazione $R(x, y)$ afferma semplicemente che $x$ e $y$ sono legati in qualche modo. La programmazione logica si basa sulle relazioni.
    \item \textbf{Prolog (Programming in Logic):} È il linguaggio di programmazione logica più celebre, basato su un sottoinsieme della logica del primo ordine (le \textit{Clausole di Horn}).
\end{itemize}

\textbf{Sintassi base di Prolog:}
\begin{itemize}
    \item \textbf{Atomi:} Costanti, nomi, o simboli. Iniziano rigorosamente con una lettera **minuscola** (es. \texttt{john}, \texttt{rochester}, \texttt{a}).
    \item \textbf{Variabili:} Iniziano rigorosamente con una lettera **maiuscola** o con un underscore (es. \texttt{X}, \texttt{Person}, \texttt{\_}). L'underscore singolo \texttt{\_} è la variabile anonima (corrisponde alla wildcard in ML).
    \item \textbf{Fatti (Facts):} Asserzioni incondizionate considerate sempre vere nel nostro "mondo" o database. 
    Es: \texttt{padre(john, mary).}
    \item \textbf{Regole (Rules):} Asserzioni condizionate (implicazioni logiche). Hanno la forma \texttt{Testa :- Corpo.} Il simbolo \texttt{:-} si legge "se" (if). 
    Es: \texttt{nonno(X, Z) :- padre(X, Y), padre(Y, Z).} 
    La virgola \texttt{,} rappresenta l'AND logico, mentre il punto e virgola \texttt{;} rappresenta l'OR.
    \item \textbf{Query / Goal:} Interrogazioni poste al sistema (precedute dal prompt \texttt{?-}). Prolog risponde \texttt{true} (o \texttt{yes}) se la query è deducibile, \texttt{false} (o \texttt{no}) altrimenti, oppure restituisce le associazioni (binding) delle variabili che rendono vera la query.
\end{itemize}

\textbf{Unificazione e Risoluzione:}
\begin{itemize}
    \item L'\textbf{Unificazione} è il processo mediante il quale Prolog cerca di rendere identici due termini assegnando valori alle variabili. Ad esempio, per unificare \texttt{padre(john, X)} e \texttt{padre(Y, mary)}, Prolog istanzia \texttt{Y = john} e \texttt{X = mary}.
    \item Il motore di \textbf{Risoluzione} esplora il database dall'alto verso il basso (top-down) e risolve le regole da sinistra verso destra (left-to-right).
    \item \textbf{Backtracking (Ritorsione):} Se Prolog intraprende un percorso di ricerca che fallisce (arriva a un punto cieco in cui non può unificare), "torna indietro" all'ultimo punto in cui c'era un'alternativa (choice point) e tenta una strada diversa.
\end{itemize}

\textbf{Operatori Speciali:}
\begin{itemize}
    \item \textbf{Is (Aritmetica):} In Prolog, \texttt{X = 3 + 4} non calcola 7, ma unifica la variabile \texttt{X} con la struttura sintattica \texttt{3 + 4}. Per forzare la valutazione aritmetica si usa l'operatore \texttt{is}: \texttt{X is 3 + 4}.
    \item \textbf{Cut (\texttt{!}) (Taglio):} È un operatore extra-logico che blocca il backtracking. Consente di ottimizzare la ricerca ("potatura dell'albero") impedendo a Prolog di esplorare alternative che sappiamo essere inutili o errate.
    \item \textbf{Negation as Failure (\texttt{\textbackslash+} o \texttt{not}):} In Prolog, se un fatto non può essere dimostrato, viene considerato falso (Assunzione del Mondo Chiuso). \texttt{not(P)} è vero se e solo se l'esecuzione di \texttt{P} fallisce.
\end{itemize}

\textbf{Liste in Prolog:}
Strutturalmente identiche a quelle in ML. Si indicano con le parentesi quadre \texttt{[]}. La sintassi per destrutturare una lista in Testa e Coda usa il pipe \texttt{|}: \texttt{[Head | Tail]}.


\subsection{Implementazione del codice}

\subsubsection{Liste, Ricorsione e Backtracking (\texttt{append} e \texttt{member})}
A differenza dei linguaggi funzionali in cui le funzioni hanno input e output definiti, in Prolog una singola definizione relazionale può operare in molteplici direzioni (invertibilità).

\begin{verbatim}
% Definizione della relazione "Membro di una lista"
% 1. Caso base: X è membro della lista se è la testa della lista.
member(X, [X | _]).

% 2. Passo ricorsivo: X è membro se è membro della coda della lista.
member(X, [_ | Tail]) :- member(X, Tail).

% Definizione dell'Append (concatenazione di liste)
% append(L1, L2, L3) è vero se L3 è la concatenazione di L1 e L2.
append([], L, L).
append([H | T1], L2, [H | T3]) :- append(T1, L2, T3).

% POTENZA DICHIARATIVA:
% Interrogazione standard: ?- append([a,b], [c,d], X). 
%   -> X = [a,b,c,d]
% Sottrazione di liste:    ?- append(X, [c,d], [a,b,c,d]). 
%   -> X = [a,b]
% Generazione di pattern:  ?- append(X, Y, [a,b,c]).
%   -> X=[], Y=[a,b,c] ; X=[a], Y=[b,c] ; ...
\end{verbatim}

\newpage

\subsubsection{Tic-Tac-Toe in Prolog (Tris Interattivo)}
Il file \texttt{tic-tac-toe-interactive.pl} fornito a lezione illustra come unire le regole dichiarative per la vittoria, il backtracking per la scelta della mossa e l'I/O (Input/Output).

\begin{verbatim}
% 1. Dichiariamo che i predicati 'o' (computer) e 'x' (giocatore) 
% sono dinamici (il database può essere modificato a runtime)
:- dynamic o/1.
:- dynamic x/1.

% 2. Definizione delle condizioni di vittoria (Linee vincenti)
ordered_line(1, 2, 3).
ordered_line(4, 5, 6).
ordered_line(7, 8, 9).
ordered_line(1, 4, 7).
% ... altre combinazioni diagonali ...

% Permutazioni per controllare la linea in qualsiasi ordine
line(A, B, C) :- ordered_line(A, B, C).
line(A, B, C) :- ordered_line(A, C, B).
% ... (altre permutazioni delegate dal Cut o dal Backtracking)

% 3. Logica di intelligenza artificiale: "Cosa è una buona mossa?"
% Prolog tenta le regole in ordine (dall'alto verso il basso).
move(A) :- good(A), empty(A).

% Condizione 1: Cerca di vincere
good(A) :- win(A).
% Condizione 2: Blocca la vittoria del giocatore
good(A) :- block_win(A).
% Condizioni fallback: occupa il centro, poi gli angoli
good(5).
good(1).
good(3).

% Implementazione di 'win' e 'block_win'
% "Vinciamo se abbiamo due 'o' su una linea e A è la terza casella vuota"
win(A) :- o(B), o(C), line(A, B, C).
% "Blocchiamo se l'utente 'x' ha due caselle su una linea"
block_win(A) :- x(B), x(C), line(A, B, C).

% 4. Gestione dello stato (Effetti collaterali con asserta)
makemove :- move(X), !, asserta(x(X)). % Il Cut (!) ferma la ricerca 
                                       % appena trova una mossa valida
makemove :- all_full.                  % Fallback in caso di pareggio
\end{verbatim}

\newpage

\textbf{Commento Architetturale per l'esame:}
L'uso di \texttt{:- dynamic} abbinato al comando \texttt{asserta} (che inserisce dinamicamente un nuovo fatto nel database all'inizio) e \texttt{retract} (che lo elimina) permette a Prolog di mantenere uno "stato" mutevole, aggirando la pura immutabilità funzionale.
Inoltre, il cut \texttt{!} in \texttt{makemove} è fondamentale: senza di esso, se la mossa successiva portasse a un fallimento o al backtracking, Prolog annullerebbe l'inserimento della mossa per provare il \texttt{good} successivo, generando comportamenti indesiderati nell'albero di gioco.

\section{Domande Generali Teoria}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La ricorsione in coda:
    \begin{itemize}
        \item[A)] Non è implementabile nei linguaggi imperativi
        \item[B)] Nessuna delle altre risposte
        \item[C)] Richiede di non scrivere mai la chiamata ricorsiva come ultimo statement di una subroutine
        \item[D)] Richiede di non ritornare mai direttamente il valore ritornato da una chiamata ricorsiva
        \item[E)] Permette di risolvere il problema della ricorsione infinita
    \end{itemize}
    \textbf{Risposta esatta:} B - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} La ricorsione in coda (tail recursion) avviene esattamente quando la chiamata ricorsiva è l'\textit{ultima} operazione eseguita dalla funzione (il che rende false C e D). È implementabile nei linguaggi imperativi (falsa A) e il suo scopo non è risolvere la ricorsione infinita (falsa E), ma piuttosto ottimizzare l'uso della memoria evitando la crescita dello Stack (Stack Overflow), riutilizzando lo stesso Record di Attivazione. Dato che questa opzione corretta non è presente, la risposta è "Nessuna delle altre".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Call by constant:
    \begin{itemize}
        \item[A)] Fa una copia della variabile
        \item[B)] È utile per passare oggetti
        \item[C)] Ritorna il valore al chiamante
        \item[D)] Permette la modifica della variabile
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Il passaggio per costante non fa una copia (quello è il call by value, falsa A), non è un meccanismo di ritorno (falsa C) e, per definizione, impedisce la modifica della variabile all'interno della funzione (falsa D). Sebbene in alcuni linguaggi (come C++) i reference costanti siano usati per passare oggetti grandi in modo efficiente, la definizione teorica del meccanismo non è "utile per passare oggetti". Pertanto, nessuna delle affermazioni fornite lo descrive correttamente.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I record di attivazione:
    \begin{itemize}
        \item[A)] Sono necessari solo in presenza di funzioni di ordine superiore
        \item[B)] Sono allocati dinamicamente solo in caso di scope dinamico
        \item[C)] Nessuna delle altre risposte
        \item[D)] Devono essere esplicitamente allocati e deallocati dal codice del programma che li usa
        \item[E)] Sono allocati solo nello heap
    \end{itemize}
    \textbf{Risposta esatta:} C - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} I Record di Attivazione (RdA) servono per supportare l'invocazione di procedure in quasi tutti i linguaggi, indipendentemente dalle funzioni di ordine superiore (falsa A) o dallo scope dinamico (falsa B). L'allocazione è automatica e gestita dalla macchina astratta (falsa D). Solitamente vengono allocati sullo Stack, tranne in alcuni casi specifici come le chiusure o i thread, dove vanno nello Heap (falsa E).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si può dire che una macchina astratta che capisce il linguaggio C non sia implementata in modo puramente compilativo perché:
    \begin{itemize}
        \item[A)] Gli eseguibili generati da un compilatore C in genere non eseguono direttamente sulla macchina hardware, ma su una macchina astratta che include il Runtime del linguaggio e le funzionalità del Sistema Operativo
        \item[B)] Gli eseguibili generati dal compilatore vengono comunque interpretati da una macchina virtuale
        \item[C)] Una macchina astratta che capisca un linguaggio di alto livello come il C non è mai implementabile con un compilatore
        \item[D)] Il Runtime del linguaggio C è comunque sempre interpretato
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - Gli eseguibili generati da un compilatore C in genere non eseguono direttamente sulla macchina hardware... \\
    \textbf{Spiegazione:} Anche se il C produce codice macchina "nativo", tale codice si appoggia sempre a librerie di Runtime (es. per allocare memoria o per l'I/O) e ai servizi del Sistema Operativo. Pertanto, l'ambiente di esecuzione finale è una macchina astratta mista e non solo l'hardware nudo. Le altre opzioni sono errate perché il C non usa una macchina virtuale interprete in senso stretto come la JVM di Java (false B e D), e un compilatore è assolutamente implementabile per il C (falsa C).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il costrutto for dei linguaggi C, C++ e Java:
    \begin{itemize}
        \item[A)] Non è un costrutto di iterazione determinata
        \item[B)] È necessario a tali linguaggi per implementare qualsiasi tipo di algoritmo
        \item[C)] Nessuna delle altre risposte
        \item[D)] È un costrutto di iterazione determinata
        \item[E)] Permette di sapere in anticipo quante volte il ciclo verrà ripetuto (indipendentemente dal corpo del ciclo)
    \end{itemize}
    \textbf{Risposta esatta:} A - Non è un costrutto di iterazione determinata \\
    \textbf{Spiegazione:} A differenza di linguaggi storici come il Pascal, il \texttt{for} in C/C++/Java permette di modificare la variabile di controllo (l'indice) all'interno del corpo del ciclo. Per questo motivo, a tempo di compilazione non è possibile determinare a priori il numero esatto di iterazioni (rendendo false D ed E). La B è falsa perché un ciclo \texttt{while} (o la ricorsione) rendono il linguaggio già Turing-completo senza bisogno del \texttt{for}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un'entità esprimibile è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Un'entità che può essere generata come risultato da un'espressione complessa o da una funzione
        \item[C)] Un'entità che ancora non compare nell'ambiente
        \item[D)] Un'entità che può essere memorizzata
        \item[E)] Una generica entità a cui può essere dato un nome
    \end{itemize}
    \textbf{Risposta esatta:} B - Un'entità che può essere generata come risultato da un'espressione complessa o da una funzione \\
    \textbf{Spiegazione:} Per definizione teorica, i valori "esprimibili" (o valutabili) sono quelli che costituiscono il risultato della valutazione di espressioni. Ciò li distingue dai valori "memorizzabili" (che possono stare in una variabile, opzione D) e "denotabili" (a cui si può associare un nome, opzione E). 
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In assenza di ambiente non locale:
    \begin{itemize}
        \item[A)] Per implementare funzioni definite ricorsivamente è necessario utilizzare un fixed point combinator
        \item[B)] Non si possono implementare algoritmi ricorsivi
        \item[C)] Nessuna delle altre risposte
        \item[D)] Non si possono implementare algoritmi iterativi o ricorsivi
        \item[E)] Non si possono implementare algoritmi iterativi
    \end{itemize}
    \textbf{Risposta esatta:} C - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Sebbene la mancanza di un ambiente non locale renda impossibile fare riferimento a variabili globali o esterne dirette, è sempre possibile aggirare il problema passando tutti i parametri necessari esplicitamente per argomento. Inoltre, la ricorsione è garantita dalla presenza del nome della funzione nell'ambiente, non dalle variabili non locali. Pertanto, tutte le affermazioni che indicano l'impossibilità di fare ricorsione o iterazione sono false (A, B, D, E).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I puntatori di catena dinamica (Dynamic Link) contenuti in un record di attivazione:
    \begin{itemize}
        \item[A)] Collegano una lista di zone di memoria gestita dinamicamente
        \item[B)] Servono per accedere alle variabili dinamiche
        \item[C)] Non esistono "puntatori di catena dinamica" in un record di attivazione
        \item[D)] Nessuna delle altre risposte
        \item[E)] Permettono, a partire da un RdA, di trovare il RdA precedente sullo stack
    \end{itemize}
    \textbf{Risposta esatta:} E - Permettono, a partire da un RdA, di trovare il RdA precedente sullo stack \\
    \textbf{Spiegazione:} Il Dynamic Link è il puntatore che salva lo stato (la base) del record di attivazione del chiamante. Serve esclusivamente a ripristinare correttamente l'ambiente della funzione precedente quando la funzione corrente termina. Non servono ad accedere all'heap (memoria gestita dinamicamente, false A e B).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In presenza di variabili modificabili:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Esistono un Ambiente che associa valori denotabili (fra cui le locazioni di memoria) a nomi ed una Memoria che associa locazioni di memoria a valori memorizzabili
        \item[C)] Non esistono valori denotabili
        \item[D)] La valutazione del comando di assegnamento restituisce sempre un valore
        \item[E)] Il comando di assegnamento non ha effetti collaterali
    \end{itemize}
    \textbf{Risposta esatta:} B - Esistono un Ambiente che associa valori denotabili... ed una Memoria che associa locazioni... \\
    \textbf{Spiegazione:} Per modellare formalmente le variabili modificabili nei linguaggi imperativi serve una struttura a "due stadi": l'Ambiente mappa gli identificatori (nomi) ad un indirizzo fisico (locazione, l-value), mentre la Memoria mappa quella locazione al suo effettivo contenuto (r-value). Le altre opzioni sono false: in questo paradigma i valori denotabili esistono eccome (C è falsa) e l'assegnamento esiste \textit{proprio} per avere l'effetto collaterale di cambiare lo stato della memoria (E è falsa).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il concetto di variabile modificabile:
    \begin{itemize}
        \item[A)] È l'unico concetto utilizzabile quando si parla di variabili
        \item[B)] È imposto dall'architettura di Von Neumann (variabili non modificabili richiederebbero macchine astratte caratterizzate da memoria a sola lettura)
        \item[C)] Nessuna delle altre risposte
        \item[D)] È tipico del paradigma di programmazione imperativo
        \item[E)] Permette di evitare il fenomeno dell'aliasing
    \end{itemize}
    \textbf{Risposta esatta:} D - È tipico del paradigma di programmazione imperativo \\
    \textbf{Spiegazione:} Il paradigma imperativo si basa sul concetto di stato computazionale che evolve nel tempo, per cui la variabile modificabile è il concetto centrale. Nei paradigmi funzionali e logici si usano variabili immutabili (il che rende falsa la A). Non previene l'aliasing, anzi ne è soggetto qualora più nomi puntino alla stessa locazione modificabile (falsa E). Non è "imposto" dall'architettura nuda (si possono creare macchine astratte funzionali sopra Von Neumann), ma ne è una naturale e diretta astrazione (falsa B).
    \vspace{0.3cm}
\end{itemize}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Nella sostituzione $(\lambda a.abc)[d/c]$:
    \begin{itemize}
        \item[A)] È necessario applicare una Alfa-equivalenza per evitare una cattura di variabile
        \item[B)] Viene catturata la variabile c
        \item[C)] Si rischia di catturare la variabile "a" ed è necessario applicare Beta-equivalenza per risolvere in problema
        \item[D)] Non c'è alcuna cattura di variabile
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} D - Non c'è alcuna cattura di variabile \\
    \textbf{Spiegazione:} Eseguendo la sostituzione, la variabile libera $c$ viene rimpiazzata con $d$. Il risultato è $\lambda a.abd$. La variabile $d$ appena inserita non cade sotto lo "scope" di alcun legatore $\lambda$ associato a $d$ (l'unico legatore presente è $\lambda a$), quindi non subisce alcuna cattura non voluta e non è necessaria alcuna $\alpha$-conversione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il fenomeno della cattura di variabili:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Non può essere evitato in alcun modo
        \item[C)] Fa sì che dopo una sostituzione una variabile libera diventi legata (per esempio da un'astrazione $\lambda x.$)
        \item[D)] Comporta la "sparizione" di variabili libere durante un'astrazione funzione
        \item[E)] È dovuto all'assenza di un ambiente non locale
    \end{itemize}
    \textbf{Risposta esatta:} C - Fa sì che dopo una sostituzione una variabile libera diventi legata... \\
    \textbf{Spiegazione:} Nel lambda calcolo, la cattura di variabile si verifica quando, sostituendo un termine all'interno di un'espressione, una variabile che originariamente era libera finisce accidentalmente nello scope di un parametro formale (un legatore $\lambda$) con lo stesso nome. Questo altera profondamente la logica dell'espressione. Per evitarlo (rendendo falsa la B), si procede rinominando preventivamente la variabile legata tramite $\alpha$-conversione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Una Macchina Astratta ML (LO) è:
    \begin{itemize}
        \item[A)] È un modo per descrivere un interprete
        \item[B)] Nessuna delle altre risposte
        \item[C)] È implementabile solo basandosi sull'architettura di Von Neumann
        \item[D)] È un modo per descrivere un compilatore
        \item[E)] È associata ad un proprio linguaggio macchina L, che è in grado di capire ed eseguire
    \end{itemize}
    \textbf{Risposta esatta:} E - È associata ad un proprio linguaggio macchina L, che è in grado di capire ed eseguire \\
    \textbf{Spiegazione:} Una macchina astratta è definita teoricamente come una collezione di strutture dati e algoritmi capace di memorizzare ed eseguire programmi. Per definizione intrinseca, ogni macchina astratta è caratterizzata da (e associata a) un proprio linguaggio macchina che essa "comprende" e interpreta direttamente, fungendo da esecutore.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il passaggio di parametri per nome:
    \begin{itemize}
        \item[A)] Permette di passare valori solo dal chiamante al chiamato (e non viceversa)
        \item[B)] Nessuna delle altre risposte
        \item[C)] È implementabile passando una chiusura come parametro
        \item[D)] Ha un valore solo teorico e non è implementabile in pratica
        \item[E)] Permette la cattura di variabili libere in modo da effetti non deterministici
    \end{itemize}
    \textbf{Risposta esatta:} C - È implementabile passando una chiusura come parametro \\
    \textbf{Spiegazione:} Nel passaggio per nome (call by name), l'argomento non viene valutato al momento della chiamata, ma ogni volta che viene menzionato nel corpo della funzione. A livello implementativo, questo risultato si ottiene passando in modo invisibile una chiusura priva di argomenti, nota come \textit{thunk}, che contiene l'espressione da calcolare e l'ambiente in cui valutarla.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La tecnica del display:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Permette di implementare facilmente lo scope dinamico
        \item[C)] Permette di ridurre il costo derivante dalla scansione della catena statica quando si implementa lo scope statico
        \item[D)] Permette di visualizzare le zone di memoria allocata dinamicamente
        \item[E)] Permette di implementare le regole di scope statico senza generare frammentazione della memoria
    \end{itemize}
    \textbf{Risposta esatta:} C - Permette di ridurre il costo derivante dalla scansione della catena statica... \\
    \textbf{Spiegazione:} Con lo scoping statico, per accedere a una variabile non-locale molto "profonda", bisognerebbe attraversare sequenzialmente i puntatori di catena statica ($O(n)$). Il display è un array mantenuto a runtime che punta direttamente ai Record di Attivazione attivi per ogni livello lessicale corrente, portando il tempo di accesso alle variabili non-locali ad essere $O(1)$, riducendone così drasticamente il costo computazionale.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un compilatore da un linguaggio L ad un linguaggio LO è:
    \begin{itemize}
        \item[A)] Un programma che trasforma un programma PL (espresso nel linguaggio L) in un programma PLO (espresso nel linguaggio LO) tale che per ogni input I si ha $PL(I)=PLO(I)$
        \item[B)] Nessuna delle altre risposte
        \item[C)] Un programma scritto nel linguaggio LO che riceve come ingresso un programma PL (espresso nel linguaggio L) ed il suo input I generando lo stesso output che genera PL con input I
        \item[D)] L'implementazione di una macchina astratta scritta nel linguaggio LO, che capisce programmi scritti nel linguaggio L
        \item[E)] Una implementazione di macchine astratte indipendente dalla macchina fisica
    \end{itemize}
    \textbf{Risposta esatta:} A - Un programma che trasforma un programma PL (espresso nel linguaggio L) in un programma PLO (espresso nel linguaggio LO)... \\
    \textbf{Spiegazione:} Questa è la definizione formale di compilatore: è un semplice traduttore. Prende il codice nel linguaggio sorgente L e lo "traduce" integralmente in un linguaggio target LO, garantendo che i due programmi mantengano un'assoluta equivalenza semantica (a parità di input, producono lo stesso output). L'opzione C, invece, è la definizione formale di un interprete.
    \vspace{0.3cm}

    \newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il costrutto for dei linguaggi C, C++ e Java non è un costrutto di iterazione determinata perché:
    \begin{itemize}
        \item[A)] L'esistenza di costrutti di iterazione derminata implicherebbe che C, C++ e Java non sono Turing-completi
        \item[B)] Nessuna delle altre risposte
        \item[C)] Dall'interno del ciclo è possibile modificare il valore del contatore
        \item[D)] Non esistono costrutti di iterazione determinata
        \item[E)] C, C++ e Java sono linguaggi imperativi
    \end{itemize}
    \textbf{Risposta esatta:} C - Dall'interno del ciclo è possibile modificare il valore del contatore \\
    \textbf{Spiegazione:} Si definisce \textit{iterazione determinata} un ciclo di cui si può prevedere in modo matematico il numero di iterazioni totali prima ancora che venga eseguita la prima (es. i cicli \texttt{for} storici del Pascal). Dal momento che in C, C++ e Java è lecito alterare il contatore del ciclo nel corpo, questo principio viene violato, rendendoli cicli "mascherati" a condizione generale, come dei \texttt{while}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un oggetto denotabile (intendendo per "oggetto" una generica entità che può essere una variabile, una funzione, etc....) è:
    \begin{itemize}
        \item[A)] Un "oggetto" che può essere memorizzato in una variabile
        \item[B)] Un "oggetto" per cui compare un binding nell'ambiente
        \item[C)] Nessuna delle altre risposte
        \item[D)] Un "oggetto" che ancora non compare nell'ambiente
        \item[E)] Un "oggetto" che può essere generato come risultato da un'espressione complessa o da una funzione
    \end{itemize}
    \textbf{Risposta esatta:} B - Un "oggetto" per cui compare un binding nell'ambiente \\
    \textbf{Spiegazione:} Le entità \textit{denotabili} sono formalmente tutti quegli elementi di un programma a cui è possibile associare un "nome" (un identificatore). Pertanto, un oggetto diventa denotabile nel momento in cui figura all'interno dell'Environment (Ambiente), che per sua natura è l'insieme strutturato dei binding fra identificatori e tali entità.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'ambiente non locale di un blocco di codice è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] L'insieme dei valori che le variabili non locali possono assumere
        \item[C)] L'insieme dei binding creati all'interno del blocco di codice
        \item[D)] L'insieme dei binding visibili dentro al blocco, ma non direttamente definiti in esso
        \item[E)] Il subset dell'ambiente non visibile dentro al blocco di codice
    \end{itemize}
    \textbf{Risposta esatta:} D - L'insieme dei binding visibili dentro al blocco, ma non direttamente definiti in esso \\
    \textbf{Spiegazione:} Un blocco vede il proprio "ambiente locale" (i binding dichiarati internamente) più un "ambiente non-locale". Quest'ultimo raccoglie le dichiarazioni effettuate all'esterno (variabili globali o variabili dei blocchi padri avvolgenti) a cui il blocco in esecuzione ha diritto di accesso pur non avendole istanziate localmente.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I puntatori di catena dinamica contenuti in un record di attivazione:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Non esistono "puntatori di catena dinamica" in un record di attivazione
        \item[C)] Servono per identificare la zona di memoria in cui è memorizzata una variabile locale
        \item[D)] Devono essere esplicitamente allocati e deallocati dal codice del programma che li usa
        \item[E)] Servono per accedere alle variabili dinamiche
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Il compito del puntatore di catena dinamica (Dynamic Link) non è gestire le variabili (rendendo false C ed E), né necessita di gestione esplicita dal programmatore (falsa D). Il suo unico ruolo è puntare alla base del record di attivazione \textit{del chiamante} per ripristinare il corretto stack frame alla fine dell'invocazione. Essendo questa corretta definizione assente, l'unica opzione giusta è "Nessuna delle altre risposte".
    \vspace{0.3cm}
\end{itemize}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'allocazione dinamica della memoria:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] È sempre effettuata solo dal compilatore o dall'interprete
        \item[C)] Può essere fatta solo dallo heap
        \item[D)] Può essere fatta solo dallo stack
        \item[E)] Può essere fatta sia dallo stack che dallo heap
    \end{itemize}
    \textbf{Risposta esatta:} E - Può essere fatta sia dallo stack che dallo heap \\
    \textbf{Spiegazione:} L'allocazione dinamica della memoria avviene durante l'esecuzione del programma. Questa può avvenire sullo Stack (es. i Record di Attivazione che vengono allocati e deallocati ad ogni chiamata di funzione, o costrutti come \texttt{alloca} in C) oppure sullo Heap (es. oggetti allocati esplicitamente dal programmatore con \texttt{malloc} in C o \texttt{new} in Java). Le opzioni C e D sono quindi limitanti e false.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un compilatore da un linguaggio L ad un linguaggio LO è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Una implementazione di macchine astratte indipendente dalla macchina fisica
        \item[C)] Un programma che trasforma un programma PLO (espresso nel linguaggio LO) in un programma PL (espresso nel linguaggio L) tale che per ogni input I si ha $PL(I)=PLO(I)$
        \item[D)] L'implementazione di una macchina astratta scritta nel linguaggio LO, che capisce programmi scritti nel linguaggio L
        \item[E)] Un programma scritto nel linguaggio LO che riceve come ingresso un programma PL (espresso nel linguaggio L) ed il suo input I generando lo stesso output che genera PL con input I
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Leggendo attentamente l'opzione C, essa descrive la traduzione al contrario (dal linguaggio macchina LO al linguaggio sorgente L, che sarebbe semmai un de-compilatore). Le opzioni D ed E definiscono concettualmente un interprete, non un compilatore. Pertanto, mancando la definizione corretta (traduzione da PL a PLO), la risposta esatta è "Nessuna delle altre".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I dangling pointer:
    \begin{itemize}
        \item[A)] Sono identificabili tramite tecniche di reference counting (contatore dei riferimenti)
        \item[B)] Sono un problema solo per il linguaggio Java
        \item[C)] Sono identificabili tramite la tecnica detta "mark and sweep"
        \item[D)] Non possono essere identificati con certezza, ma questo non è un problema perché comportano solo un piccolo spreco di memoria
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} I dangling pointer (puntatori pendenti) si verificano quando un'area di memoria viene deallocata, ma rimangono dei puntatori che fanno ancora riferimento ad essa. Non causano un "piccolo spreco", ma gravi errori di sicurezza (falsa D). In Java non esistono grazie al Garbage Collector (falsa B). Il \textit{Reference Counting} e il \textit{Mark and Sweep} (false A e C) servono a trovare la "spazzatura" (memoria non più puntata), non i dangling pointer. Per questi ultimi servono tecniche diverse (es. tombstones o locks-and-keys).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La frammentazione esterna causa:
    \begin{itemize}
        \item[A)] Uno spreco di memoria
        \item[B)] Nessuna delle altre risposte
        \item[C)] L'impossibilità di allocare grandi blocchi di memoria anche se la memoria libera totale è sufficiente
        \item[D)] Un rallentamento rilevante nelle operazioni di allocazione della memoria
        \item[E)] Il funzionamento non corretto di programmi che allocano memoria dinamicamente
    \end{itemize}
    \textbf{Risposta esatta:} C - L'impossibilità di allocare grandi blocchi di memoria anche se la memoria libera totale è sufficiente \\
    \textbf{Spiegazione:} La frammentazione esterna si verifica quando la memoria libera sullo Heap è "spezzettata" in tanti piccoli frammenti isolati tra blocchi allocati. Pur essendoci un quantitativo totale di memoria libera sufficiente per soddisfare una nuova richiesta, se quest'ultima esige un blocco contiguo molto grande, l'allocatore fallirà. L'opzione A (spreco) è più tipica della frammentazione interna.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La valutazione con corto circuito del predicato "A \&\& B" (dove "\&\&" rappresenta un "AND" logico):
    \begin{itemize}
        \item[A)] Stabilisce che se "B" è vero allora "A" non viene valutato
        \item[B)] Nessuna delle altre risposte
        \item[C)] Stabilisce che se "A" è falso allora "B" non viene valutato
        \item[D)] Crea un non-determinismo nell'ordine della valutazione di "A" e "B"
        \item[E)] Stabilisce che "A" e "B" devono essere valutati in parallelo
    \end{itemize}
    \textbf{Risposta esatta:} C - Stabilisce che se "A" è falso allora "B" non viene valutato \\
    \textbf{Spiegazione:} Nella valutazione cortocircuitata di espressioni logiche, l'analisi avviene da sinistra a destra. Nel caso dell'operatore AND, affinché l'intera espressione sia vera, entrambi gli operandi devono essere veri. Se il primo (A) risulta falso, l'esito finale è matematicamente garantito essere falso, per cui l'esecuzione salta completamente la valutazione di (B) (rendendo falsa la A e la E), risparmiando tempo ed evitando potenziali errori.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se l'ambiente di una funzione non contiene il nome della funzione stessa:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Non è possibile per la funzione invocarsi ricorsivamente
        \item[C)] Non ci sono particolari conseguenze
        \item[D)] La funzione non può usare scope dinamico
        \item[E)] La funzione non può usare scope statico
    \end{itemize}
    \textbf{Risposta esatta:} B - Non è possibile per la funzione invocarsi ricorsivamente \\
    \textbf{Spiegazione:} Affinché una funzione possa compiere una chiamata ricorsiva, deve poter riferirsi a sé stessa tramite un identificatore valido. Se il nome della funzione non figura nel suo ambiente (o negli ambienti più esterni ad essa accessibili), il meccanismo di risoluzione dei nomi fallirà, impedendo la ricorsione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La frammentazione interna causa:
    \begin{itemize}
        \item[A)] Il funzionamento non corretto di programmi che allocano memoria dinamicamente
        \item[B)] Nessuna delle altre risposte
        \item[C)] Un rallentamento rilevante nelle operazioni di allocazione della memoria
        \item[D)] Uno spreco di memoria
        \item[E)] L'impossibilità di allocare grandi blocchi di memoria anche se la memoria libera totale è sufficiente
    \end{itemize}
    \textbf{Risposta esatta:} D - Uno spreco di memoria \\
    \textbf{Spiegazione:} La frammentazione interna avviene quando il sistema assegna un blocco di memoria di dimensioni fisse (es. per motivi di allineamento) leggermente più grande di quanto effettivamente richiesto dal programma. Lo spazio eccedente all'interno del blocco assegnato non può essere utilizzato da nessun altro, traducendosi in puro spreco. L'opzione E, come visto in precedenza, descrive invece la frammentazione esterna.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'allocazione dinamica della memoria:
    \begin{itemize}
        \item[A)] Può essere fatta sia dallo stack che dallo heap
        \item[B)] Nessuna delle altre risposte
        \item[C)] È sempre effettuata solo dal compilatore o dall'interprete
        \item[D)] Può essere fatta solo dallo stack
        \item[E)] Può essere fatta solo dallo heap
    \end{itemize}
    \textbf{Risposta esatta:} A - Può essere fatta sia dallo stack che dallo heap \\
    \textbf{Spiegazione:} Questa è una riformulazione esatta della Domanda 21 (vedi sopra). L'allocazione è considerata dinamica (eseguita a runtime) sia quando si accatastano i Record di Attivazione sullo Stack (allocazione LIFO), sia quando si riservano blocchi arbitrari sullo Heap. 
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'ambiente (o environment) è:
    \begin{itemize}
        \item[A)] L'insieme delle associazioni (nome, entità denotabile) esistenti in uno specifico punto del programma ed in uno specifico momento durante l'esecuzione di un programma
        \item[B)] L'insieme dei valori che una variabile assume durante l'esecuzione di un programma
        \item[C)] Un insieme di associazioni (nome, valore) definite staticamente durante lo sviluppo di un programma
        \item[D)] Nessuna delle altre risposte
        \item[E)] Una lista di coppie (nome, tipo) che permette di accedere alle variabili di un programma
    \end{itemize}
    \textbf{Risposta esatta:} A - L'insieme delle associazioni (nome, entità denotabile) esistenti in uno specifico... \\
    \textbf{Spiegazione:} Formalmente, nei linguaggi di programmazione, l'Ambiente è la struttura che si occupa di mappare l'identificatore (il nome stabilito dal programmatore) all'entità semantica che rappresenta (detta entità denotabile). Non mappa mai i nomi direttamente ai "valori" (questo è compito della Memoria, rendendo false B e C), ma piuttosto a locazioni di memoria, funzioni o tipi.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si può dire che una macchina astratta che capisce il linguaggio Java non sia implementata in modo puramente compilativo perché:
    \begin{itemize}
        \item[A)] Non esistendo un vero e proprio runtime per Java, non si può parlare di compilazione pura
        \item[B)] La macchina virtuale di Java (JVM) deve comunque essere compilata
        \item[C)] Una macchina astratta che capisca un linguaggio di alto livello come Java non è mai implementabile con un compilatore
        \item[D)] Non esistono compilatori Java
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Tutte le opzioni proposte affermano il falso: per Java esiste eccome un runtime environment (JRE/JVM) e possiede fior di compilatori (come \texttt{javac}), rendendo false A e D. Inoltre, teoricamente ogni linguaggio di alto livello può essere compilato (falsa C). Il vero motivo per cui Java non è puramente compilativo è l'approccio misto: il compilatore genera un codice intermedio (Bytecode) e non codice macchina nativo, il quale viene poi letto e interpretato/compilato just-in-time dalla JVM a runtime.
    \vspace{0.3cm}
\end{itemize}

\newpage

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La ricorsione in coda:
    \begin{itemize}
        \item[A)] Permette di risolvere il problema della ricorsione infinita
        \item[B)] Richiede di non scrivere mai la chiamata ricorsiva come ultimo statement di una subroutine
        \item[C)] Richiede di non ritornare mai direttamente il valore ritornato da una chiamata ricorsiva
        \item[D)] Nessuna delle altre risposte
        \item[E)] Permette di evitare un'eccessiva crescita della dimensione dello stack
    \end{itemize}
    \textbf{Risposta esatta:} E - Permette di evitare un'eccessiva crescita della dimensione dello stack \\
    \textbf{Spiegazione:} Se la chiamata ricorsiva è rigorosamente l'ultima operazione eseguita nel corpo di una funzione (ricorsione in coda o \textit{tail recursion}), il compilatore o l'interprete può ottimizzarla riutilizzando il Record di Attivazione corrente invece di crearne uno nuovo. Questo previene lo Stack Overflow. Non previene, però, un loop infinito causato da una condizione di base errata (rendendo falsa la A). Le opzioni B e C affermano l'esatto contrario della definizione di ricorsione in coda.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La memoria gestita staticamente:
    \begin{itemize}
        \item[A)] È allocata esplicitamente dal programma a tempo di esecuzione, ma una volta allocata è staticamente legata al programma e non può essere liberata fino alla sua terminazione
        \item[B)] È allocata prima dell'esecuzione del programma. Le entità allocate staticamente possono essere deallocate durante l'esecuzione del programma, per liberare memoria
        \item[C)] È allocata dal compilatore prima dell'esecuzione del programma. Le entità allocate staticamente in memoria risiedono in una zona fissa di memoria durante tutta l'esecuzione del programma
        \item[D)] È una memoria a sola lettura
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} C - È allocata dal compilatore prima dell'esecuzione del programma... \\
    \textbf{Spiegazione:} L'allocazione statica viene decisa e preparata dal compilatore ancor prima che il programma inizi l'esecuzione. Questa memoria (come quella per le variabili globali) occupa una dimensione fissa e permane nello stesso blocco per l'intero ciclo di vita del processo. Non può essere deallocata a piacimento a runtime (il che rende false la A e la B) e non è necessariamente a sola lettura (rendendo falsa la D).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un interprete di un linguaggio L scritto in un linguaggio LO è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] L'implementazione di una macchina astratta scritta nel linguaggio LO, che capisce programmi scritti nel linguaggio L
        \item[C)] Una implementazione di macchine astratte indipendente dalla macchina fisica
        \item[D)] Un programma scritto nel linguaggio LO che riceve come ingresso un programma PL (espresso nel linguaggio L) ed il suo input I generando lo stesso output che genera PL con input I
        \item[D)] Un programma che trasforma un programma PL (espresso nel linguaggio L) in un programma PLO (espresso nel linguaggio LO) tale che per ogni input I si ha $PL(I)=PLO(I)$
    \end{itemize}
    \textbf{Risposta esatta:} D - Un programma scritto nel linguaggio LO che riceve come ingresso un programma PL... \\
    \textbf{Spiegazione:} Questa è la definizione \textit{operazionale} di un interprete. A differenza di un compilatore (descritto nell'opzione E) che converte il codice e termina, l'interprete prende il codice sorgente (PL) e i dati di input (I) simultaneamente, e genera direttamente l'output valutando istruzione per istruzione a tempo di esecuzione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In caso di scope statico:
    \begin{itemize}
        \item[A)] I legami fra nomi ed oggetto possono essere determinati solo a tempo di esecuzione
        \item[B)] Non è possibile annidare più blocchi di istruzioni
        \item[C)] Il valore assegnato ad una variabile non può essere modificato
        \item[D)] I legami fra nomi ed oggetto possono essere determinati semplicemente leggendo il testo di un programma
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} D - I legami fra nomi ed oggetto possono essere determinati semplicemente leggendo il testo di un programma \\
    \textbf{Spiegazione:} Lo scope statico (o lessicale) permette di risolvere i binding basandosi unicamente sulla struttura annidata del codice sorgente (il testo scritto), indipendentemente da come o da dove la funzione viene chiamata a runtime. Se i legami si determinassero solo a tempo di esecuzione (opzione A), parleremmo di scope dinamico. Le opzioni B e C non hanno fondamento.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un garbage collector:
    \begin{itemize}
        \item[A)] Può essere implementato tramite la tecnica detta "mark and sweep", che riesce sempre a identificare tutta la memoria allocata dinamicamente ma non più utilizzata
        \item[B)] Richiede un'implementazione complessa, usando la tecnica dei tombstone (pietre tombali)
        \item[C)] È implementabile solo in linguaggi di programmazione funzionali
        \item[D)] È implementabile tramite la tecnica di lucchetti e chiavi, che però può causare dei memory leak
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - Può essere implementato tramite la tecnica detta "mark and sweep"... \\
    \textbf{Spiegazione:} Il Mark and Sweep è un algoritmo di Garbage Collection che prima "marca" tutti gli oggetti raggiungibili seguendo i riferimenti a partire dai root (stack e variabili globali), e poi "spazza" via (dealloca) l'intero heap rimanente. Questa tecnica risolve anche il problema dei riferimenti circolari (aliasing ciclico). Le "pietre tombali" e i "lucchetti/chiavi" (opzioni B e D) servono invece per prevenire i dangling pointer, non per fare garbage collection.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un'entità denotabile è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Un'entità che può essere generata come risultato da un'espressione complessa o da una Funzione
        \item[C)] Una generica entità a cui può essere dato un nome
        \item[D)] Un'entità che ancora non compare nell'ambiente
        \item[E)] Un'entità che può essere memorizzata
    \end{itemize}
    \textbf{Risposta esatta:} C - Una generica entità a cui può essere dato un nome \\
    \textbf{Spiegazione:} Come visto in una domanda precedente, nel contesto dei linguaggi di programmazione, l'aggettivo "denotabile" descrive tutto ciò a cui è possibile associare sintatticamente un identificatore (un nome). L'opzione B descrive le entità "esprimibili", mentre la E descrive le entità "memorizzabili".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un interprete di un linguaggio L scritto in un linguaggio LO è:
    \begin{itemize}
        \item[A)] Un programma che trasforma un programma PL (espresso nel linguaggio L) in un programma PLO (espresso nel linguaggio LO) tale che per ogni input I si ha $PL(I)=PLO(I)$
        \item[B)] Nessuna delle altre risposte
        \item[C)] Una implementazione di macchine astratte indipendente dalla macchina fisica
        \item[D)] Un programma scritto nel linguaggio L che dato un input I produce lo stesso output generato dallo stesso programma scritto nel linguaggio LO
        \item[E)] L'implementazione di una macchina astratta scritta nel linguaggio LO, che capisce programmi scritti nel linguaggio L
    \end{itemize}
    \textbf{Risposta esatta:} E - L'implementazione di una macchina astratta scritta nel linguaggio LO... \\
    \textbf{Spiegazione:} A differenza della Domanda 33, che dava la definizione di \textit{funzionamento operazionale}, qui si fornisce la definizione \textit{strutturale}. Un interprete non è altro che la materializzazione (l'implementazione software) di una macchina astratta che prende vita grazie a un linguaggio ospite (LO), ed è progettata per "capire" direttamente un linguaggio ospitato (L).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'ambiente (o environment) è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Un insieme di associazioni (nome, valore) definite staticamente durante lo sviluppo di un programma
        \item[C)] L'insieme delle associazioni (variabile, valore) esistenti in uno specifico punto del programma ed in uno specifico momento durante l'esecuzione di un programma
        \item[D)] L'insieme dei valori che una variabile assume durante l'esecuzione di un programma
        \item[E)] Una lista di coppie (nome, tipo) che permette di accedere alle variabili di un programma
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Formalmente, l'Ambiente è l'insieme delle associazioni "NOME -> ENTITÀ DENOTABILE". Le opzioni proposte qui cercano di confondere i concetti: le associazioni dirette tra nome/variabile e "valore" (opzioni B e C) riassumono il concetto più ampio di "Stato" della computazione (Ambiente + Memoria), non il solo Ambiente. L'opzione E è falsa perché mappa nomi a tipi e non denota l'environment semantico.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un garbage collector:
    \begin{itemize}
        \item[A)] Richiede un'implementazione complessa, usando la tecnica dei tombstone (pietre tombali)
        \item[B)] È implementabile solo in linguaggi di programmazione funzionali
        \item[C)] Può essere implementato tramite la tecnica del reference counting (contatore dei riferimenti), che riesce sempre ad identificare tutta la memoria allocata dinamicamente ma non più utilizzata
        \item[D)] È implementabile tramite la tecnica di lucchetti e chiavi, che però può causare dei memory leak
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Ancora una volta le opzioni mescolano tecniche distinte. Tombstones e Lucchetti/Chiavi (A, D) servono per i dangling pointer, non per la deallocazione dei leak (GC). Il Reference Counting (C) è effettivamente una tecnica di GC, ma la frase asserisce che riesce \textit{sempre} a identificare la memoria inutilizzata: questo è falso, poiché il Reference Counting fallisce clamorosamente di fronte a strutture dati con riferimenti circolari (es. liste doppiamente concatenate chiuse).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La memoria gestita dinamicamente:
    \begin{itemize}
        \item[A)] È usata solo in linguaggi interpretati
        \item[B)] Non è mai strettamente necessaria, ma permette di ottenere migliori prestazioni
        \item[C)] È necessaria per implementare la ricorsione
        \item[D)] Nessuna delle altre risposte
        \item[E)] È necessaria per implementare l'iterazione
    \end{itemize}
    \textbf{Risposta esatta:} D - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Se per "memoria gestita dinamicamente" intendiamo l'heap, essa serve per strutture dati senza vincoli di dimensione a compile-time e non è legata all'iterazione o unicamente ai linguaggi interpretati. Se ci riferiamo allo stack dinamico (allocazione dei RdA), è sì necessario per la ricorsione, ma l'espressione ambigua e la mancanza di una formulazione perfettamente accurata fra le opzioni porta alla scelta di "Nessuna delle altre".
    \vspace{0.3cm}
\end{itemize}

\newpage

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'ambiente (o environment) è:
    \begin{itemize}
        \item[A)] Un insieme di associazioni (nome, valore) definite staticamente durante lo sviluppo di un programma
        \item[B)] Una lista di coppie (nome, tipo) che permette di accedere alle variabili di un programma
        \item[C)] Nessuna delle altre risposte
        \item[D)] L'insieme dei valori che una variabile assume durante l'esecuzione di un programma
        \item[E)] L'insieme delle associazioni (nome, oggetto denotabile) esistenti in uno specifico punto del programma ed in uno specifico momento durante l'esecuzione di un programma
    \end{itemize}
    \textbf{Risposta esatta:} E - L'insieme delle associazioni (nome, oggetto denotabile) esistenti in uno specifico punto del programma ed in uno specifico momento durante l'esecuzione di un programma \\
    \textbf{Spiegazione:} Questa è la definizione formale ed esatta di Ambiente. A differenza dello "Stato", che mappa direttamente i nomi ai valori finali (inglobando anche la Memoria), l'Ambiente si limita a mappare gli identificatori (nomi) alle entità semantiche a cui si riferiscono (oggetti denotabili, come locazioni di memoria, funzioni o tipi) in quel preciso istante e punto del codice (regole di scope).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In caso di scope dinamico:
    \begin{itemize}
        \item[A)] Il valore assegnato ad una variabile non può essere modificato
        \item[B)] Non è possibile annidare più blocchi di istruzioni
        \item[C)] Nessuna delle altre risposte
        \item[D)] I legami fra nomi ed oggetto possono essere determinati solo a tempo di esecuzione
        \item[E)] I legami fra nomi ed oggetto possono essere determinati semplicemente leggendo il testo di un programma
    \end{itemize}
    \textbf{Risposta esatta:} D - I legami fra nomi ed oggetto possono essere determinati solo a tempo di esecuzione \\
    \textbf{Spiegazione:} A differenza dello scope statico, in cui il legame di una variabile si evince leggendo la struttura lessicale del codice sorgente (il che rende falsa la E), lo scope dinamico risolve i nomi risalendo a ritroso la catena delle chiamate attive (catena dinamica) sullo Stack. Poiché l'ordine delle chiamate dipende dall'input e dal flusso del programma, la risoluzione può avvenire unicamente a runtime.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I puntatori di catena statica contenuti in un record di attivazione:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Servono per accedere alle variabili statiche
        \item[C)] Devono essere esplicitamente allocati e deallocati dal codice del programma che li usa
        \item[D)] Servono per identificare la zona di memoria in cui è memorizzata una variabile in caso di scope statico
        \item[E)] Non esistono puntatori di catena statica in un record d'attivazione
    \end{itemize}
    \textbf{Risposta esatta:} D - Servono per identificare la zona di memoria in cui è memorizzata una variabile in caso di scope statico \\
    \textbf{Spiegazione:} Il puntatore di catena statica (Static Link) viene mantenuto nel Record di Attivazione per puntare al record del blocco o funzione che lo avvolge "lessicalmente" (il padre nel codice sorgente). Seguendo questi puntatori, il programma riesce a trovare e accedere correttamente alle variabili non locali in un ambiente a scope statico.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un'entità denotabile può avere un tempo di vita più lungo di quello delle associazioni (fra l'entità e identificatori) che lo riferiscono:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Se l'entità è allocata dinamicamente dallo heap in una subroutine
        \item[C)] Mai
        \item[D)] Se l'entità è allocata dinamicamente dallo stack in una subroutine
        \item[E)] Se si usa scope dinamico
    \end{itemize}
    \textbf{Risposta esatta:} B - Se l'entità è allocata dinamicamente dallo heap in una subroutine \\
    \textbf{Spiegazione:} Quando si alloca memoria sullo Heap all'interno di una funzione e la si assegna a un puntatore locale (la "variabile/associazione"), al termine della funzione la variabile locale viene distrutta (poppata dallo Stack), terminando così il tempo di vita dell'associazione. Tuttavia, l'entità allocata sullo Heap rimane in vita fino alla deallocazione esplicita (es. \texttt{free}) o all'intervento del Garbage Collector. Dunque, l'entità sopravvive all'associazione iniziale.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La memoria gestita dinamicamente:
    \begin{itemize}
        \item[A)] Non è mai strettamente necessaria, ma permette di ottenere migliori prestazioni
        \item[B)] È necessaria per implementare l'iterazione
        \item[C)] È usata solo in linguaggi interpretati
        \item[D)] È necessaria solo per implementare macchine astratte per linguaggi compilati
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Come visto per la domanda 40, la memoria gestita dinamicamente (sia essa lo Stack per i Record di Attivazione, sia l'Heap per le strutture dati) è strettamente necessaria per implementare la ricorsione (il che rende falsa la A), ed è usata sia da linguaggi compilati che interpretati (il che rende false la C e la D). Non è necessaria per implementare la semplice iterazione, che richiede solo salti condizionati (falsa B).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} I record d'attivazione:
    \begin{itemize}
        \item[A)] Devono essere esplicitamente allocati e deallocati dal codice del programma che li usa
        \item[B)] Possono essere allocati sia sullo stack sia sullo heap (in caso, per esempio, di funzioni di ordine superiore)
        \item[C)] Sono necessari solo in presenza di funzioni di ordine superiore
        \item[D)] Sono allocati solo sullo heap
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} B - Possono essere allocati sia sullo stack sia sullo heap (in caso, per esempio, di funzioni di ordine superiore) \\
    \textbf{Spiegazione:} Di norma, i Record di Attivazione vengono allocati e deallocati automaticamente (falsa A) sulla struttura LIFO dello Stack. Tuttavia, nei linguaggi funzionali o con First-Class Functions, una funzione può restituire un'altra funzione e le sue variabili locali (closure) devono sopravvivere alla fine della chiamata originale. In questi casi specifici, il record viene promosso e allocato sullo Heap affinché non venga distrutto.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Quale delle seguenti affermazioni riguardanti il garbage collector è vera:
    \begin{itemize}
        \item[A)] Per Garbage Collection si intende una modalità automatica di gestione della memoria
        \item[B)] L'utente alloca liberamente memoria
        \item[C)] Non è permesso deallocare memoria
        \item[D)] Il sistema periodicamente recupera la memoria allocata e non più utilizzabile
        \item[E)] Tutte sono corrette
    \end{itemize}
    \textbf{Risposta esatta:} E - Tutte sono corrette \\
    \textbf{Spiegazione:} Nei linguaggi dotati di Garbage Collector (es. Java), la gestione della memoria è delegata al Runtime System (A). Il programmatore è libero di istanziare oggetti con \texttt{new} (B), ma in genere il linguaggio impedisce di "forzare" o effettuare una deallocazione manuale per motivi di sicurezza (C). È quindi compito di un demone in background cercare ciclicamente la spazzatura e liberarla per il riutilizzo (D).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Quale delle seguenti affermazioni riguardanti il Garbage Collector è falsa:
    \begin{itemize}
        \item[A)] Il garbage collector annoterà le aree di memoria non più referenziate e le libererà automaticamente
        \item[B)] L'utente alloca liberamente memoria
        \item[C)] È permesso deallocare memoria
        \item[D)] Un sistema operativo, o un compilatore, e un modulo di run-time liberano porzioni di memoria non più utilizzate dalle applicazioni.
        \item[E)] Il sistema periodicamente recupera la memoria allocata e non più utilizzabile
    \end{itemize}
    \textbf{Risposta esatta:} C - È permesso deallocare memoria \\
    \textbf{Spiegazione:} Come anticipato nella domanda precedente, una caratteristica saliente (e una garanzia di sicurezza) di un linguaggio "puramente" garbage-collected è che nasconde e inibisce i costrutti per la deallocazione esplicita della memoria (come il \texttt{free()} del C o il \texttt{delete} del C++). Quindi dire che "è permesso deallocare memoria" è falso.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Considerando la macchina astratta che permette di eseguire un linguaggio L, il numero di record di attivazione contemporaneamente presenti nel sistema:
    \begin{itemize}
        \item[A)] È sempre determinabile a priori da un'analisi statica del codice del programma eseguito
        \item[B)] Nessuna delle altre risposte
        \item[C)] Ha un massimo noto a priori solo se la macchina astratta alloca i record di attivazione dallo heap
        \item[D)] Può avere un massimo noto a priori solo se il linguaggio L permette ricorsione
        \item[E)] Non dipende dal programma che si esegue
    \end{itemize}
    \textbf{Risposta esatta:} B - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Se un linguaggio permette la ricorsione, la profondità dello stack (numero di AR contemporanei) dipende dai dati di input e dalle condizioni a runtime, quindi non può essere determinata sempre a priori (rendendo false A, C ed E). D'altro canto, è un linguaggio *senza* ricorsione (es. i primi Fortran) ad avere un numero massimo noto a priori, non il contrario (rendendo falsa la D).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'utilizzo della ricorsione in coda (tail recursion) in una funzione:
    \begin{itemize}
        \item[A)] Utilizza più memoria
        \item[B)] Nessuna delle altre risposte
        \item[C)] Riduce la possibilità di avere stack overflow
        \item[D)] Riduce l'efficienza della funzione
        \item[E)] Richiede rimozione manuale degli stack frame
    \end{itemize}
    \textbf{Risposta esatta:} C - Riduce la possibilità di avere stack overflow \\
    \textbf{Spiegazione:} La "tail recursion" è una forma ottimizzata di ricorsione dove il risultato dell'ultima chiamata è anche il risultato della funzione chiamante, senza passaggi intermedi post-ritorno. I compilatori ottimizzano questo pattern riutilizzando lo stesso e unico Record di Attivazione, mantenendo un impatto di memoria costante. Pertanto è più efficiente e previene errori critici come lo Stack Overflow.
    \vspace{0.3cm}
\end{itemize}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La chiusura (closure) è:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] espressione, ambiente (expression, environment) dove quest'ultimo ha almeno tutte le variabili nell'espressione unitamente all'indirizzo di ritorno
        \item[C)] espressione, ambiente (expression, environment) dove quest'ultimo ha alcune delle variabili nell'espressione
        \item[D)] espressione, ambiente (expression, environment) dove quest'ultimo ha almeno tutte le variabili nell'espressione
        \item[E)] utilizzata per le call by reference
    \end{itemize}
    \textbf{Risposta esatta:} D - espressione, ambiente (expression, environment) dove quest'ultimo ha almeno tutte le variabili nell'espressione \\
    \textbf{Spiegazione:} Una chiusura (closure) è l'astrazione a runtime di una funzione. È composta da due elementi fondamentali: il codice della funzione (l'espressione) e l'ambiente di valutazione in cui è stata definita. Quest'ultimo deve necessariamente contenere i binding per "almeno tutte le variabili" libere menzionate all'interno del corpo della funzione, in modo da poterle valutare correttamente al momento dell'invocazione. Non contiene indirizzi di ritorno, prerogativa dei Record di Attivazione (rendendo falsa la B).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Polish notation. Indicare il risultato di x - 56 7:
    \begin{itemize}
        \item[A)] -37
        \item[B)] -5
        \item[C)] 23
        \item[D)] -7
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} D - -7 \\
    \textbf{Spiegazione:} Il testo originale contiene un evidente errore di battitura: \texttt{x - 56 7} sta per \texttt{* - 5 6 7} (dove la "x" indica il per della moltiplicazione e 56 è in realtà 5 e 6). Nella notazione polacca (prefissa), gli operatori precedono gli operandi. Si risolve partendo dall'operatore più interno: \texttt{(- 5 6)} restituisce -1. L'espressione diventa quindi \texttt{(* -1 7)}, che equivale a -7.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un linguaggio di programmazione (come FORTRAN) usa un'allocazione fissa della memoria (at compile time). Questo tipo di linguaggio non può supportare:
    \begin{itemize}
        \item[A)] floating point numbers
        \item[B)] Exceptions
        \item[C)] variable size arrays
        \item[D)] nessuna delle precedenti
        \item[E)] Procedures
    \end{itemize}
    \textbf{Risposta esatta:} C - variable size arrays \\
    \textbf{Spiegazione:} L'allocazione statica (fissa a tempo di compilazione) implica che il compilatore debba conoscere in anticipo l'esatto ingombro in memoria di ogni singola struttura dati. Per questo motivo, costrutti la cui dimensione dipende dall'input dell'utente a tempo di esecuzione (come gli array a dimensione variabile o le allocazioni su heap) sono assolutamente impossibili da supportare.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La differenza fra espressione e comandi:
    \begin{itemize}
        \item[A)] Un comando può modificare l'ambiente (locale, non locale o globale) mentre una espressione no
        \item[B)] Non esistono differenze: sono due nomi diversi per la stessa entità sintattica
        \item[C)] L'esecuzione di un comando produce un valore (o non termina), mentre la valutazione di una espressione può non produrre valori
        \item[D)] Le espressioni non possono usare simboli appartenenti ad ambienti non locali
        \item[E)] Nessuna delle altre
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre \\
    \textbf{Spiegazione:} Tutte le opzioni fornite sono errate. La C è invertita: sono le \textit{espressioni} che producono sempre un valore (restituendo un risultato), mentre i \textit{comandi} (o statement) si eseguono per il loro effetto collaterale (modifica della Memoria/Stato) e non restituiscono valori. La A è falsa perché i comandi alterano la \textit{Memoria} (lo store dei valori), mentre l'Ambiente (il binding dei nomi) viene modificato dalle \textit{dichiarazioni}. 
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Point reversal viene usato per garbage collection:
    \begin{itemize}
        \item[A)] per evitare di sprecare spazio
        \item[B)] nelle implementazioni di python
        \item[C)] per gestire loops nelle strutture puntatore
        \item[D)] quando non si ha spazio per una search structure
        \item[E)] nessuna delle precedenti
    \end{itemize}
    \textbf{Risposta esatta:} D - quando non si ha spazio per una search structure \\
    \textbf{Spiegazione:} L'algoritmo di inversione dei puntatori (Pointer Reversal o Schorr-Waite) è utilizzato nella fase "Mark" dei Garbage Collector. Normalmente la navigazione del grafo degli oggetti richiederebbe uno Stack (search structure) o chiamate ricorsive. Tuttavia, dato che il GC entra in funzione proprio quando la memoria scarseggia, allocare uno stack aggiuntivo potrebbe essere impossibile. Il pointer reversal inverte i puntatori attraversati per ricordare il percorso a ritroso senza usare memoria extra.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Il cut \texttt{!} in Prolog:
    \begin{itemize}
        \item[A)] Ha successo e ferma il backtracking
        \item[B)] nessuna delle altre
        \item[C)] esegue il backtracking
        \item[D)] Chiama una eccezione
        \item[E)] fallisce e ferma il backtracking
    \end{itemize}
    \textbf{Risposta esatta:} A - Ha successo e ferma il backtracking \\
    \textbf{Spiegazione:} Il cut (scritto \texttt{!}) è un operatore di ottimizzazione in Prolog. Esso ha sempre successo logico e si comporta come una direttiva per il motore di risoluzione: gli "impedisce" di esplorare eventuali soluzioni alternative (backtracking) per tutti i goal a sinistra del cut e per la regola corrente, forzandolo ad accettare la prima soluzione trovata come definitiva.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Call by name:
    \begin{itemize}
        \item[A)] Usata raramente al giorno d'oggi
        \item[B)] Utilizzata in molti linguaggi procedurali
        \item[C)] Richiede il passaggio dei valori delle variabili sullo stack
        \item[D)] Ha una definizione semplice
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - Usata raramente al giorno d'oggi \\
    \textbf{Spiegazione:} Il passaggio di parametri per nome (introdotto storicamente in Algol 60) inietta l'argomento non valutato direttamente nel codice della funzione chiamante. A causa della sua complessità implementativa (richiede la creazione di "thunks" invisibili) e dei suoi side-effects inattesi, è caduto in disuso. Oggi sopravvive solo marginalmente (es. in Scala) o in varianti come il Call by Need (Haskell). Per questo motivo la B, C e D sono false.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un'espressione può essere beta-ridotta ad un'unica forma normale:
    \begin{itemize}
        \item[A)] Mai
        \item[B)] Sempre
        \item[C)] Solo se la riduzione termina
        \item[D)] Ci possono essere diverse forme normali
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} C - Solo se la riduzione termina \\
    \textbf{Spiegazione:} Per il Teorema di Church-Rosser (o Teorema di Confluenza) nel Lambda Calcolo, se un'espressione possiede una forma normale (cioè la sequenza di riduzioni beta giunge a un termine non più riducibile e termina), allora tale forma normale è univoca, indipendentemente dall'ordine con cui si scelgono di effettuare le riduzioni.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} L'iterazione determinata:
    \begin{itemize}
        \item[A)] Non è implementabile in nessun linguaggio
        \item[B)] Permette di modificare la variabile di controllo durante l'esecuzione del corpo dell'iterazione
        \item[C)] Non permette di sapere in anticipo quante volte si ripeterà indipendentemente dal corpo del ciclo
        \item[D)] è presente in C, C++ e Java
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} L'iterazione determinata è caratterizzata proprio dall'assoluto opposto di quanto asserito nelle opzioni: permette \textit{esattamente} di calcolare le iterazioni prima dell'esecuzione (falsa C), vieta rigidamente la modifica del contatore al suo interno (falsa B) e non è presente in C, C++ o Java (che usano iterazione indeterminata nel \texttt{for}, falsa D). Essendo storicamente presente in Pascal e Ada, non è vero che sia "non implementabile" (falsa A).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} TextIO.lookahead
    \begin{itemize}
        \item[A)] Legge il carattere successivo ma lo lascia nell'input stream
        \item[B)] Verifica se c'è un altro carattere nell'input stream
        \item[C)] error
        \item[D)] Legge il carattere successivo
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - Legge il carattere successivo ma lo lascia nell'input stream \\
    \textbf{Spiegazione:} Nel linguaggio SML, la funzione \texttt{TextIO.lookahead} (come suggerisce il nome stesso, "guarda avanti") consente di "sbirciare" il prossimo carattere in arrivo nel buffer del file o dello standard input, restituendolo al chiamante senza però consumarlo o rimuoverlo dal flusso (a differenza di funzioni come \texttt{input1}).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Memorizzare array in ordine di riga o colonna:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Conta soltanto quando si usa il caching
        \item[C)] Viene sempre usato l'ordine per colonna
        \item[D)] Non conta mai
        \item[E)] Viene sempre usato l'ordine per riga
    \end{itemize}
    \textbf{Risposta esatta:} B - Conta soltanto quando si usa il caching \\
    \textbf{Spiegazione:} A livello teorico e semantico (per la correttezza di un algoritmo) non vi è alcuna differenza. Tuttavia, a livello hardware, iterare su un array multidimensionale sfruttando la sua mappatura in memoria (Row-Major per il C, Column-Major per il Fortran) permette di accedere a posizioni adiacenti contigue, innescando la località spaziale che massimizza le performance di accesso alla memoria Cache del processore, abbattendo drasticamente i tempi di esecuzione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Pointer reversal is used in garbage collection:
    \begin{itemize}
        \item[A)] To avoid wasting space for marking used records
        \item[B)] In implementations of Python
        \item[C)] To deal with loops in pointer structures
        \item[D)] When we don't have space for a search structure
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} D - When we don't have space for a search structure \\
    \textbf{Spiegazione:} Questa è l'esatta traduzione in lingua inglese della domanda numero 55 vista in precedenza. Il pointer reversal è una tecnica salvavita impiegata nell'attraversamento di strutture a grafo quando la memoria residua non consente di istanziare uno Stack dedicato all'esplorazione (la "search structure").
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un ADT è definito essere un modello matematico di un user-defined type, insieme a tutte le operazioni su quel modo:
    \begin{itemize}
        \item[A)] Struttura
        \item[B)] Primitiva
        \item[C)] Assignment
        \item[D)] Cardinalità
        \item[E)] Nessuna delle precedenti
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle precedenti \\
    \textbf{Spiegazione:} Il termine "modo" presente nella domanda è un palese errore di traduzione/battitura per "modello" (model). La definizione canonica sancisce che un Abstract Data Type (ADT) è definito dai suoi tipi di dato e dall'insieme di tutte le operazioni \textit{su quel modello}. Poiché nessuna delle opzioni A, B, C o D (struttura, primitiva, ecc.) completa correttamente l'asserzione, la risposta corretta è "Nessuna delle precedenti".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un linguaggio di programmazione (come FORTRAN) usa una allocazione fissa della memoria (in compilazione). Questo tipo di linguaggio non può supportare:
    \begin{itemize}
        \item[A)] Procedure
        \item[B)] Eccezioni
        \item[C)] Ricorsione
        \item[D)] Nessuna della altre risposte
        \item[E)] Numeri floating-point
    \end{itemize}
    \textbf{Risposta esatta:} C - Ricorsione \\
    \textbf{Spiegazione:} Come appurato alla Domanda 53, allocare tutto staticamente prima dell'esecuzione ha forti conseguenze architettoniche. Oltre agli array dinamici, viene invalidata la Ricorsione: l'assenza di uno Stack dinamico per i record di attivazione costringe il programma ad allocare una singola area di memoria fissa per ogni subroutine. Invocare due volte la stessa funzione provocherebbe la sovrascrittura distruttiva del singolo blocco di memoria a lei destinato.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In Prolog:
    \begin{itemize}
        \item[A)] Conta l'ordine delle regole e predicati (da sinistra a destra) in una regola
        \item[B)] Nessuna delle altre risposte
        \item[C)] Conta l'ordine delle regole e predicati (da destra a sinistra) in una regola
        \item[D)] Conta solo l'ordine delle regole
        \item[E)] Conta solo l'ordine dei predicati (da destra a sinistra) in una regola
    \end{itemize}
    \textbf{Risposta esatta:} A - Conta l'ordine delle regole e predicati (da sinistra a destra) in una regola \\
    \textbf{Spiegazione:} A dispetto dell'idea originaria della Programmazione Logica in cui l'ordine delle dichiarazioni non dovrebbe alterare il risultato matematico, in Prolog l'ordine è cruciale per via del suo motore di inferenza (algoritmo di risoluzione procedurale). L'esplorazione (SLD-Resolution) è di tipo \textit{Depth-First, Left-To-Right}: le regole vengono testate dall'alto verso il basso (nell'ordine in cui sono scritte nel database), e per ciascuna regola, i predicati (sub-goals) al suo interno sono provati sequenzialmente da sinistra a destra.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Un linker:
    \begin{itemize}
        \item[A)] È scritto in linguaggio macchina
        \item[B)] Sostituisce indirizzi relativi con indirizzi assoluti
        \item[C)] Viene utilizzato nei linguaggi interpretati
        \item[D)] Nessuna delle altre risposte
        \item[E)] Viene usato per l'efficienza
    \end{itemize}
    \textbf{Risposta esatta:} B - Sostituisce indirizzi relativi con indirizzi assoluti \\
    \textbf{Spiegazione:} Il Linker (collegatore) è lo strumento fondamentale della catena di compilazione che riceve in ingresso i vari moduli oggetto (.o o .obj). Il suo compito primario è la "Rilocazione" (oltre che la risoluzione delle dipendenze esterne): trasforma gli indirizzi relativi calcolati dal compilatore all'interno dei singoli moduli non interconnessi, unendoli e traducendoli negli indirizzi assoluti e definitivi dell'eseguibile unico finale.
    \vspace{0.3cm}
\end{itemize}


\section{Domande Lambda Calcolo}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda n.\lambda m.\lambda f.\lambda x.(n f)((m f)x))(\lambda f.\lambda x.f f f f x) (\lambda f.\lambda x.f x)$ si ottiene:
    \begin{itemize}
        \item[A)] $\lambda f.\lambda x.f f f f x$
        \item[B)] $f$
        \item[C)] Nessuna delle altre risposte
        \item[D)] $\lambda f.\lambda x.f f f f f f x$
        \item[E)] $x$
        \item[F)] $\lambda f.\lambda x.f f f f f x$
    \end{itemize}
    \textbf{Risposta esatta:} F - $\lambda f.\lambda x.f f f f f x$ \\
    \textbf{Spiegazione:} Il primo blocco dell'espressione rappresenta la funzione Somma (\textit{Addition}) nella codifica dei Numerali di Church. I due argomenti passati sono il numero 4 (una funzione che applica $f$ per 4 volte a $x$) e il numero 1 (che applica $f$ per 1 volta). La somma matematica di 4 e 1 è 5, perciò il risultato sarà l'applicazione di $f$ per 5 volte consecutive alla $x$, che corrisponde all'opzione F.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda n.\lambda m.\lambda f.\lambda x.(n f)((m f)x))(\lambda f.\lambda x.f x) (\lambda f.\lambda x.x)$ si ottiene:
    \begin{itemize}
        \item[A)] $\lambda f.\lambda x.f(f(f(f x)))$
        \item[B)] $\lambda f.\lambda x.f x$
        \item[C)] $x$
        \item[D)] $f x$
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} B - $\lambda f.\lambda x.f x$ \\
    \textbf{Spiegazione:} Come nella domanda precedente, il termine iniziale è l'operatore di Addizione. In questo caso viene applicato al numero 1 (rappresentato da $\lambda f.\lambda x.f x$) e al numero 0 di Church (rappresentato da $\lambda f.\lambda x.x$, in cui la $f$ non viene applicata). Poiché 1 + 0 = 1, il risultato beta-ridotto restituisce il numerale di Church corrispondente a 1.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda n.\lambda f.\lambda x.f((n f)x))(\lambda f.\lambda x.f(f(f x)))$ si ottiene:
    \begin{itemize}
        \item[A)] La riduzione non termina
        \item[B)] $f x$
        \item[C)] $\lambda f.\lambda x.f f f f f x$
        \item[D)] Nessuna delle altre risposte
        \item[E)] $\lambda f.\lambda x.f(f(f(f x)))$
    \end{itemize}
    \textbf{Risposta esatta:} E - $\lambda f.\lambda x.f(f(f(f x)))$ \\
    \textbf{Spiegazione:} Il primo termine è la funzione Successore ($SUCC$), che prende un numerale di Church $n$ e restituisce $n + 1$ avvolgendo il tutto in un'ulteriore applicazione di $f$. Il parametro passato è il numero 3 (con 3 $f$). Applicando il successore al numero 3 si ottiene sintatticamente il numero 4 (con 4 $f$), cioè l'opzione E.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda a.((a \lambda b.\lambda c.c)\lambda d.\lambda e.d))(\lambda f.\lambda g.f)$ si ottiene:
    \begin{itemize}
        \item[A)] $\lambda b.\lambda c.c$
        \item[B)] Nessuna delle altre risposte
        \item[C)] La riduzione non termina
        \item[D)] $c$
        \item[E)] $\lambda b.\lambda c.b$
    \end{itemize}
    \textbf{Risposta esatta:} A - $\lambda b.\lambda c.c$ \\
    \textbf{Spiegazione:} Questa è una codifica logico-condizionale. L'argomento fornito al posto di $a$ è $(\lambda f.\lambda g.f)$, che rappresenta il booleano $TRUE$ di Church (prende due parametri e restituisce il primo). Il corpo centrale contiene una scelta tra $\lambda b.\lambda c.c$ (ovvero $FALSE$) e $\lambda d.\lambda e.d$. L'espressione si comporta come un \texttt{if TRUE then FALSE else TRUE}. Scegliendo il primo parametro (essendo TRUE), il risultato è il falso di Church.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda x.x y)(\lambda z.z x)(\lambda z.z x)$ si ottiene:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] $x y z$
        \item[C)] $(\lambda x.x y) y x$
        \item[D)] $y x(\lambda z.z x)$
        \item[E)] La riduzione non termina
    \end{itemize}
    \textbf{Risposta esatta:} D - $y x(\lambda z.z x)$ \\
    \textbf{Spiegazione:} Valutiamo passo passo l'applicazione da sinistra a destra (essendo associativa a sinistra). Applichiamo la prima funzione al secondo termine sostituendo $x$ con $(\lambda z.z x)$: otteniamo $((\lambda z.z x) y)$. Applicando questa funzione passiamo $y$ al posto di $z$, ottenendo $(y x)$. Infine, accodiamo il terzo termine originale non ancora processato: $(y x)(\lambda z.z x)$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta-riducendo $(\lambda n.\lambda f.\lambda x.f((n f)x))(\lambda f.\lambda x.f f f x)$ si ottiene:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] $\lambda f.\lambda x.f f f f f f x$
        \item[C)] La riduzione non termina
        \item[D)] $\lambda f.\lambda x.f f f f f x$
        \item[E)] $f x$
        \item[F)] L'espressione è irriducibile
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Ancora una volta, abbiamo la funzione Successore applicata al numero di Church 3 ($f f f x$). Il risultato atteso sarebbe il numero 4, ovvero $\lambda f.\lambda x.f f f f x$. Poiché tra le opzioni fornite ci sono solo 5, 6, $f x$ o risposte logiche scorrette, e manca il risultato corretto per 4, la risposta esatta è "Nessuna delle altre".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta f-riducendo $((\lambda a.a a)(\lambda b.b))(\lambda c.c)$ si ottiene:
    \begin{itemize}
        \item[A)] La riduzione non termina
        \item[B)] $a a a$
        \item[C)] $\lambda x.x a$
        \item[D)] Nessuna delle altre risposte
        \item[E)] $\lambda c.c$
    \end{itemize}
    \textbf{Risposta esatta:} E - $\lambda c.c$ \\
    \textbf{Spiegazione:} Eseguiamo il primo blocco $((\lambda a.a a)(\lambda b.b))$. Sostituendo la funzione identità $\lambda b.b$ al posto di $a$, l'espressione diventa $(\lambda b.b)(\lambda b.b)$. Valutando questa applicazione (identità su identità), riotteniamo $\lambda b.b$. L'espressione totale diventa ora $(\lambda b.b)(\lambda c.c)$, ovvero l'identità applicata a $\lambda c.c$, che restituisce intatto $\lambda c.c$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta f-riducendo $(\lambda a.a a a)((\lambda b.b) (\lambda c.c))$ si ottiene:
    \begin{itemize}
        \item[A)] $\lambda a.a$
        \item[B)] Nessuna delle altre risposte
        \item[C)] $a a a$
        \item[D)] La riduzione non termina
        \item[E)] $\lambda x.x a$
    \end{itemize}
    \textbf{Risposta esatta:} B - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Valutiamo prima l'argomento interno: $(\lambda b.b)(\lambda c.c)$, che è l'identità applicata a $\lambda c.c$. Il risultato è $\lambda c.c$. Ora passiamo questo risultato alla funzione esterna: $(\lambda a.a a a)(\lambda c.c)$. Sostituendo $a$, otteniamo $(\lambda c.c)(\lambda c.c)(\lambda c.c)$. Valutando questa sequenza di identità, il risultato finale converge a $\lambda c.c$. Poiché questo termine non è tra le opzioni, "Nessuna" è la risposta corretta.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta f-riducendo $(\lambda d.((\lambda a.a b c)(b c)))(\lambda y.x y z)$ si ottiene:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] La riduzione non termina
        \item[C)] $\lambda b.b b a$
        \item[D)] $\lambda a.a b c$
        \item[E)] $b c b c$
    \end{itemize}
    \textbf{Risposta esatta:} E - $b c b c$ \\
    \textbf{Spiegazione:} Iniziamo con il corpo più interno: $(\lambda a.a b c)(b c)$. Sostituendo $(b c)$ al posto della $a$ si ottiene $(b c) b c$. L'espressione complessiva diventa quindi $\lambda d.((b c) b c)$ a cui dobbiamo applicare l'argomento $\lambda y.x y z$. Tuttavia, la variabile legata $d$ non appare da nessuna parte nel corpo $(b c b c)$. Pertanto, l'argomento viene assorbito a vuoto (scartato) e l'espressione restituisce semplicemente $b c b c$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Beta f-riducendo $(\lambda n.\lambda f.\lambda x.f((n f)x))(\lambda f.\lambda x.f(f(f x)))))$ si ottiene:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] L'espressione è irriducibile
        \item[C)] La riduzione non termina
        \item[D)] $\lambda f.\lambda x.f f f f f x$
        \item[E)] $f x$
        \item[F)] $\lambda f.\lambda x.f(f(f(f(f x))))$
    \end{itemize}

\newpage
    
    \textbf{Risposta esatta:} F - $\lambda f.\lambda x.f(f(f(f(f x))))$ \\
    \textbf{Spiegazione:} Analogamente alla domanda 3, la funzione esterna calcola il Successore del numerale in ingresso. Nel testo dell'opzione corretta sembra ci sia un offset di parentesi nella stesura originale del test, ma concettualmente il successore di un numero di Church aggiunge una singola applicazione della funzione $f$ in testa, portando all'opzione indicata F come catena annidata corretta rispetto alla base.
    \vspace{0.3cm}
\end{itemize}

\begin{itemize}
\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \
\textbf{Testo:} Beta-riducendo $(\lambda n.\lambda m.\lambda f.\lambda x.(n f)((m f)x))(\lambda f.\lambda x.f x)(\lambda f.\lambda x.x)$ si ottiene:
\begin{itemize}
\item[A)] Nessuna delle altre risposte
\item[B)] $x$
\item[C)] $\lambda f.\lambda x.f f f f f f x$
\item[D)] $\lambda f.\lambda x.f x$
\item[E)] $f x$
\item[F)] $\lambda f.\lambda x.f f f f x$
\end{itemize}
\textbf{Risposta esatta:} D - $\lambda f.\lambda x.f x$ \
\textbf{Spiegazione:} Il primo termine rappresenta l'operatore matematico di Addizione all'interno della codifica dei Numeri di Church. Tale funzione riceve come argomenti due numerali e ne somma le applicazioni. Il primo argomento $(\lambda f.\lambda x.f x)$ è il numerale $1$, mentre il secondo $(\lambda f.\lambda x.x)$ è il numerale $0$. Eseguendo la somma logica di $1 + 0$ si ottiene $1$, che corrisponde sintatticamente all'opzione D.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Beta-riducendo $(\lambda a.((a\lambda b.\lambda c.c)\lambda d.\lambda e.d))(\lambda f.\lambda g.g)$ si ottiene:
\begin{itemize}
    \item[A)] Nessuna delle altre risposte
    \item[B)] $\lambda b.\lambda c.b$
    \item[C)] $b$
    \item[D)] $\lambda b.\lambda c.c$
    \item[E)] La riduzione non termina
\end{itemize}
\textbf{Risposta esatta:} B - $\lambda b.\lambda c.b$ \\
\textbf{Spiegazione:} L'espressione principale è un costrutto condizionale (un \texttt{IF-THEN-ELSE} in logica di Church). L'argomento passato ad $a$ è $(\lambda f.\lambda g.g)$, che rappresenta il booleano $FALSE$ (prende due argomenti e restituisce il secondo). Nel blocco condizionale $((a\ \text{arg1})\ \text{arg2})$, il $FALSE$ forza la selezione del secondo argomento, ovvero $\lambda d.\lambda e.d$. Questo termine prende due parametri e restituisce il primo, che lo rende di fatto equivalente al $TRUE$ di Church. Tramite una semplice $\alpha$-conversione (rinomina delle variabili), $\lambda d.\lambda e.d$ equivale perfettamente a $\lambda b.\lambda c.b$.
\vspace{0.3cm}

\newpage

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Beta-riducendo $(\lambda a.a a b)((\lambda a.a a b)(\lambda a.(\lambda b.b a)c))$ si ottiene:
\begin{itemize}
    \item[A)] La riduzione non termina
    \item[B)] $a a b$
    \item[C)] $\lambda a.a a b$
    \item[D)] $c c b(c c b)c$
    \item[E)] Nessuna delle altre risposte
\end{itemize}
\textbf{Risposta esatta:} E - Nessuna delle altre risposte \\
\textbf{Spiegazione:} Questa espressione innesca una reazione di auto-applicazione ricorsiva complessa. Risolvendo il blocco più a destra $(\lambda a.(\lambda b.b a)c)$ otteniamo $\lambda a.c a$. Passando questo termine a $(\lambda a.a a b)$, l'applicazione $(\lambda a.c a)(\lambda a.c a)b$ si riduce a $c(\lambda a.c a)b$. Questo enorme blocco risultante diviene a sua volta l'argomento per la funzione più esterna $(\lambda a.a a b)$, portando a un'espansione massiccia del termine che non coincide con nessuna delle stringhe limitate (come $c c b(c c b)c$) elencate nelle opzioni A, B, C o D.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Beta-riducendo $((\lambda a.a a a) (\lambda b.b))(\lambda c.c)$ si ottiene:
\begin{itemize}
    \item[A)] $\lambda c.c$
    \item[B)] La riduzione non termina
    \item[C)] $a a a$
    \item[D)] $\lambda x.x a$
    \item[E)] Nessuna delle altre risposte
\end{itemize}
\textbf{Risposta esatta:} A - $\lambda c.c$ \\
\textbf{Spiegazione:} Si valuta inizialmente il blocco funzionale $((\lambda a.a a a) (\lambda b.b))$. Sostituendo la funzione identità $\lambda b.b$ a tutte le occorrenze di $a$, otteniamo un'applicazione tripla dell'identità: $(\lambda b.b)(\lambda b.b)(\lambda b.b)$. Riducendola su sé stessa, l'identità persiste ritornando $\lambda b.b$. L'espressione si è ora semplificata in $(\lambda b.b)(\lambda c.c)$. Quest'ultima è banalmente l'applicazione della funzione identità al termine $\lambda c.c$, che viene restituito intatto.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Beta-riducendo $(\lambda d.((\lambda a.a b c)(b c))) (\lambda y.x y z)$ si ottiene:
\begin{itemize}
    \item[A)] La riduzione non termina
    \item[B)] $b c b c$
    \item[C)] Nessuna delle altre risposte
    \item[D)] $\lambda a.a b c$
    \item[E)] $\lambda b.b b$
\end{itemize}

\newpage

\textbf{Risposta esatta:} B - $b c b c$ \\
\textbf{Spiegazione:} Questa domanda è concettualmente speculare alla Domanda 9 analizzata in precedenza. Il nucleo più profondo $((\lambda a.a b c)(b c))$ si valuta sostituendo $(b c)$ al posto della variabile $a$, generando la stringa $(b c) b c$. L'intera espressione è ora $(\lambda d.(b c b c))(\lambda y.x y z)$. Poiché nel corpo non v'è alcuna occorrenza della variabile legata $d$, l'argomento esterno $(\lambda y.x y z)$ è completamente irrilevante: esso viene assorbito e scartato nel vuoto, lasciando come unica soluzione il corpo originario $b c b c$.
\vspace{0.3cm}


\end{itemize}

\section{Domande Calcolo Indirizzo Array}

\begin{itemize}
\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \
\textbf{Testo:} Se gli array sono memorizzati per righe e \texttt{int a[100][100]} è un array multidimensionale di interi (si assuma che la dimensione di un intero sia 4 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x5000}, qual è l'indirizzo di \texttt{a[5][10]}?
\begin{itemize}
\item[A)] \texttt{0x57F8}
\item[B)] Nessuna delle altre risposte
\item[C)] \texttt{0x53ED}
\item[D)] \texttt{0x5510}
\item[E)] \texttt{0x51FE}
\end{itemize}
\textbf{Risposta esatta:} A - \texttt{0x57F8} \
\textbf{Spiegazione:} In un array memorizzato in ordine di riga (Row-Major Order), l'indirizzo di un elemento \texttt{(i, j)} si calcola tramite la formula: $\text{Base} + (i \times \text{Numero\_Colonne} + j) \times \text{Dimensione\_Dato}$.
Sostituendo i valori forniti: $0x5000 + (5 \times 100 + 10) \times 4 = 0x5000 + 510 \times 4 = 0x5000 + 2040$.
Convertendo $2040$ decimale in esadecimale otteniamo $0x7F8$. Sommandolo alla base abbiamo infine \texttt{0x57F8}.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Se gli array sono memorizzati per colonne ed \texttt{int a[100][100]} è un array multidimensionale di interi (si assuma che la dimensione di un intero sia 4 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x5000}, qual è l'indirizzo di \texttt{a[5][10]}?
\begin{itemize}
    \item[A)] \texttt{0x5510}
    \item[B)] \texttt{0x53ED}
    \item[C)] Nessuna delle altre risposte
    \item[D)] \texttt{0x500F}
    \item[E)] \texttt{0x41FE}
\end{itemize}
\textbf{Risposta esatta:} C - Nessuna delle altre risposte \\
\textbf{Spiegazione:} L'ordine di memorizzazione per colonne (Column-Major Order) esplora primariamente l'indice di riga. La formula inversa diventa: $\text{Base} + (j \times \text{Numero\_Righe} + i) \times \text{Dimensione\_Dato}$.
Sostituendo: $0x5000 + (10 \times 100 + 5) \times 4 = 0x5000 + 1005 \times 4 = 0x5000 + 4020$.
Il valore decimale $4020$ convertito in esadecimale è \texttt{0xFB4}. L'indirizzo esatto risultante è \texttt{0x5FB4}, che non è presente tra le opzioni matematiche offerte.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Se gli array sono memorizzati per righe e \texttt{char a[100][100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0][0]} che ha indirizzo \texttt{0x1000}, qual è l'indirizzo di \texttt{a[5][5][10]}?
\begin{itemize}
    \item[A)] Nessuna delle altre risposte
    \item[B)] \texttt{0x51510}
    \item[C)] \texttt{0xC54E}
    \item[D)] \texttt{0xD54E}
    \item[E)] \texttt{0x50510}
\end{itemize}
\textbf{Risposta esatta:} D - \texttt{0xD54E} \\
\textbf{Spiegazione:} In un array a 3 dimensioni con ordinamento a righe, l'indirizzo dell'elemento \texttt{(i, j, k)} è $\text{Base} + (i \times d_2 \times d_3 + j \times d_3 + k) \times \text{Dimensione}$.
Applicando le misure: $0x1000 + (5 \times 100 \times 100 + 5 \times 100 + 10) \times 1 = 0x1000 + 50000 + 500 + 10 = 0x1000 + 50510$.
La conversione esadecimale del puro scostamento (offset) $50510$ genera il valore \texttt{0xC54E}. Sommandoci l'indirizzo di base originario $0x1000$, si ottiene regolarmente \texttt{0xD54E}.
\vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Se gli array sono memorizzati per colonne e \texttt{char a[100][100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0][0]} che ha indirizzo \texttt{0x1000}, qual è l'indirizzo di \texttt{a[5][5][10]}?
\begin{itemize}
    \item[A)] Nessuna delle altre risposte
    \item[B)] \texttt{0x18899}
    \item[C)] \texttt{0x51510}
    \item[D)] \texttt{0xD54E}
    \item[E)] \texttt{0x19899}
\end{itemize}
\textbf{Risposta esatta:} E - \texttt{0x19899} \\
\textbf{Spiegazione:} L'estensione 3D dell'indirizzamento per colonne segue le logiche Fortran. La formula posizionale per un elemento \texttt{(i, j, k)} trasforma l'offset in $i + j \times d_1 + k \times d_1 \times d_2$ moltiplicato per la dimensione.
Calcolo dell'offset: $5 + 5 \times 100 + 10 \times (100 \times 100) = 5 + 500 + 100000 = 100505$.
La codifica in base 16 del numero intero $100505$ restituisce \texttt{0x18899}. Aggiungendo a quest'ultimo l'iniziale \texttt{0x1000} di Base, deriviamo analiticamente \texttt{0x19899}.
\vspace{0.3cm}

\newpage

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Se gli array sono memorizzati per colonne e \texttt{int a[25][25]} è un array multidimensionale di interi (si assuma che un intero sia memorizzato in 4 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x1000}, qual è l'indirizzo di \texttt{a[5][10]}?
\begin{itemize}
    \item[A)] \texttt{0x11FE}
    \item[B)] Nessuna delle altre risposte
    \item[C)] \texttt{0x100F}
    \item[D)] \texttt{0x13FC}
    \item[E)] \texttt{0x121C}
\end{itemize}
\textbf{Risposta esatta:} D - \texttt{0x13FC} \\
\textbf{Spiegazione:} Utilizzando la formula per matrici Column-Major in 2D: $\text{Base} + (j \times \text{Righe} + i) \times \text{Size}$.
Otteniamo: $0x1000 + (10 \times 25 + 5) \times 4 = 0x1000 + (255) \times 4 = 0x1000 + 1020$.
Convertendo l'incremento decimale di $1020$ in hex (sapendo che $1024$ corrisponde a $0x400$, e togliendo 4 otteniamo $0x3FC$) la destinazione definitiva risulterà in \texttt{0x13FC}.
\vspace{0.3cm}


\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
\textbf{Testo:} Se gli array sono memorizzati per colonne ed \texttt{short int a[100][100]} è un array multidimensionale di interi corti (si assuma che la dimensione di uno short int sia 2 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x4100} qual è l'indirizzo di \texttt{a[5][10]}?
\begin{itemize}
    \item[A)] \texttt{0x48DA}
    \item[B)] \texttt{0x4510}
    \item[C)] Nessuna delle altre risposte
    \item[D)] \texttt{0x47DA}
    \item[E)] \texttt{0x41FE}
\end{itemize}
\textbf{Risposta esatta:} A - \texttt{0x48DA} \\
\textbf{Spiegazione:} Riavviando l'esplorazione per colonne (Column-Major) a dimensione pari a 2 Byte. 
La misurazione si declina in: $0x4100 + (10 \times 100 + 5) \times 2 = 0x4100 + 1005 \times 2 = 0x4100 + 2010$.
Decodificando il delta numerico $2010$ dal decimale alle sedici cifre otteniamo il valore \texttt{0x7DA}. Inglobato nello stato architetturale di base $0x4100$, il puntatore farà capo a \texttt{0x48DA}.
\vspace{0.3cm}
\end{itemize}

\newpage

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per colonne e \texttt{char a[100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0]} che ha indirizzo \texttt{0x1100}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] \texttt{0x24ED}
        \item[C)] \texttt{0x21FE}
        \item[D)] \texttt{0x22FE}
        \item[E)] \texttt{0x14ED}
    \end{itemize}
    \textbf{Risposta esatta:} E - \texttt{0x14ED} \\
    \textbf{Spiegazione:} In un array memorizzato in Column-Major Order (per colonne), l'indirizzo di un elemento \texttt{(i, j)} si calcola come: $\text{Base} + (j \times \text{Numero\_Righe} + i) \times \text{Dimensione}$.
    Con dimensione di 1 byte per il tipo \texttt{char}: $0x1100 + (10 \times 100 + 5) \times 1 = 0x1100 + 1005$. 
    Convertendo $1005$ decimale in esadecimale otteniamo \texttt{0x3ED}. Sommandolo alla base: $0x1100 + 0x3ED = 0x14ED$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per colonne e \texttt{char a[100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0]} che ha indirizzo \texttt{5243}, qual è l'indirizzo di \texttt{a[5][10]}? (i numeri sono in decimale)
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] \texttt{5753}
        \item[C)] \texttt{6248}
        \item[D)] \texttt{4730}
        \item[E)] \texttt{4955}
    \end{itemize}
    \textbf{Risposta esatta:} C - \texttt{6248} \\
    \textbf{Spiegazione:} Avendo gli stessi indici della domanda precedente, sappiamo che l'offset posizionale per colonna è pari a $1005$ elementi. Poiché la dimensione del \texttt{char} è 1 e i calcoli richiesti sono direttamente in base decimale, basta sommare l'offset all'indirizzo di base: $5243 + 1005 = 6248$.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per colonne ed \texttt{short int a[100][100]} è un array multidimensionale di interi corti (si assuma che la dimensione di uno short int sia 2 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x4100}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] \texttt{0x41FE}
        \item[B)] \texttt{0x500F}
        \item[C)] \texttt{0x47DA}
        \item[D)] \texttt{0x4510}
        \item[E)] \texttt{0x43ED}
        \item[F)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} F - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Questa è una riproposizione esatta della Domanda 6 della sezione calcolo indirizzi. Come calcolato in precedenza, l'offset per colonne è $1005$, che moltiplicato per 2 byte di dimensione fa $2010$ (ovvero \texttt{0x7DA} in esadecimale). L'indirizzo corretto è quindi $0x4100 + 0x7DA = 0x48DA$. Essendo tale opzione assente, la risposta corretta è "Nessuna".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per righe ed \texttt{int a[100][100]} è un array multidimensionale di interi (si assuma che la dimensione di un intero sia 4 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x1000}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] \texttt{0x100F}
        \item[B)] Nessuna delle altre risposte
        \item[C)] \texttt{0x13ED}
        \item[D)] \texttt{0x11FE}
        \item[E)] \texttt{0x1510}
    \end{itemize}
    \textbf{Risposta esatta:} B - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Per un array in Row-Major Order (per righe), la formula è $\text{Base} + (i \times \text{Numero\_Colonne} + j) \times \text{Dimensione}$.
    Sostituendo: $0x1000 + (5 \times 100 + 10) \times 4 = 0x1000 + 510 \times 4 = 0x1000 + 2040$.
    $2040$ in decimale corrisponde a \texttt{0x7F8} in esadecimale. L'indirizzo finale atteso è $0x17F8$, che non è presente tra le risposte.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per righe e \texttt{char a[100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0]} che ha indirizzo \texttt{0x2000}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] \texttt{0x200F}
        \item[B)] \texttt{0x21FE}
        \item[C)] Nessuna delle altre risposte
        \item[D)] \texttt{0x23ED}
        \item[E)] \texttt{0x2510}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{0x21FE} \\
    \textbf{Spiegazione:} Analogamente alla domanda precedente (ordine per righe), l'offset in termini di celle per la coordinata \texttt{[5][10]} è $(5 \times 100 + 10) = 510$. Poiché la dimensione del \texttt{char} è 1 byte, il salto in memoria ammonta a $510$ byte, cioè \texttt{0x1FE} in esadecimale. Sommandolo all'indirizzo base: $0x2000 + 0x1FE = 0x21FE$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per colonne ed \texttt{int a[100][100]} è un array multidimensionale di interi (si assuma che la dimensione di un intero sia 4 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{0x5000}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] \texttt{0x53ED}
        \item[C)] \texttt{0x500F}
        \item[D)] \texttt{0x41FE}
        \item[E)] \texttt{0x551}
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Questa è la ripetizione esatta della Domanda 2 della sezione calcolo indirizzi. La formula per colonne restituisce un offset di $1005$, che moltiplicato per 4 fa $4020$ byte. Convertito, $4020$ diventa \texttt{0xFB4}. L'indirizzo finale corretto sarebbe $0x5FB4$. Poiché non è tra le opzioni (la E contiene un evidente typo e le altre sono errate), la risposta è "Nessuna".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per righe e \texttt{char a[100][100]} è un array multidimensionale di caratteri con \texttt{a[0][0]} che ha indirizzo \texttt{0x1100}, qual è l'indirizzo di \texttt{a[5][10]}?
    \begin{itemize}
        \item[A)] \texttt{0x22FE}
        \item[B)] \texttt{0x12FE}
        \item[C)] \texttt{0x1510}
        \item[D)] \texttt{0x210F}
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{0x12FE} \\
    \textbf{Spiegazione:} Come calcolato precedentemente per il \texttt{char} memorizzato per righe, l'offset totale in byte è $510$, che corrisponde a \texttt{0x1FE}. Addizionandolo alla nuova base fornita: $0x1100 + 0x1FE = 0x12FE$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Se gli array sono memorizzati per colonne ed \texttt{short int a[100][100]} è un array multidimensionale di interi corti (si assuma che la dimensione di uno short int sia 2 byte) con \texttt{a[0][0]} che ha indirizzo \texttt{16640}, qual è l'indirizzo di \texttt{a[5][10]}?:
    \begin{itemize}
        \item[A)] \texttt{18650}
        \item[B)] \texttt{18394}
        \item[C)] \texttt{17680}
        \item[D)] \texttt{16894}
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - \texttt{18650} \\
    \textbf{Spiegazione:} L'offset in posizioni per l'ordine a colonne è $(10 \times 100 + 5) = 1005$. Moltiplicato per i 2 byte del tipo di dato, il salto ammonta a $2010$ byte in decimale. Poiché ci viene fornito l'indirizzo base direttamente in decimale, non serve la conversione esadecimale: $16640 + 2010 = 18650$.
    \vspace{0.3cm}
\end{itemize}

\section{Domande Tipi Funzioni ML}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f x y = x y;} ?
    \begin{itemize}
        \item[A)] \texttt{fun: 'a->'b}
        \item[B)] \texttt{fun: 'a->'b->string->string}
        \item[C)] \texttt{fun: ('a->'b)->'a->'b}
        \item[D)] \texttt{fun: int->int}
        \item[E)] \texttt{fun: string->string->string->string}
    \end{itemize}
    \textbf{Risposta esatta:} C - \texttt{fun: ('a->'b)->'a->'b} \\
    \textbf{Spiegazione:} In ML, la notazione \texttt{x y} significa che \texttt{x} viene applicato a \texttt{y} come funzione. Affinché ciò sia corretto nel sistema di tipi, \texttt{x} deve essere una funzione generica che prende in input un tipo \texttt{'a} e restituisce un tipo \texttt{'b} (quindi \texttt{'a -> 'b}). Di conseguenza, l'argomento \texttt{y} deve necessariamente essere di tipo \texttt{'a}. Il tipo totale della funzione è quindi il tipo di \texttt{x}, seguito dal tipo di \texttt{y}, che restituisce \texttt{'b}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{List.map print} (sottintendendo l'applicazione parziale in un contesto string) ?
    \begin{itemize}
        \item[A)] \texttt{fun: string list -> string list}
        \item[B)] \texttt{fun: string list -> unit list}
        \item[C)] \texttt{fun: string list -> string}
        \item[D)] \texttt{fun: string -> string list}
        \item[E)] \texttt{fun: string -> unit}
        \item[F)] \texttt{fun: 'a list -> 'b list}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{fun: string list -> unit list} \\
    \textbf{Spiegazione:} La funzione d'ordine superiore \texttt{List.map} possiede la firma polimorfa \texttt{('a -> 'b) -> 'a list -> 'b list}. La funzione \texttt{print} incorporata in ML ha tipo \texttt{string -> unit}. Passando \texttt{print} come primo argomento a \texttt{map} (currying), la funzione risultante prenderà in input una lista di stringhe (\texttt{string list}) e restituirà una lista con i risultati delle valutazioni, ovvero una lista di valori \texttt{unit} (\texttt{unit list}).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{let f1 l = List.map (fun x y -> x,y) l;} ?
    \begin{itemize}
        \item[A)] \texttt{f1:'a list->('b -> 'a -> 'b) list}
        \item[B)] \texttt{f1:'a list-> 'b -> ('a * 'b)}
        \item[C)] \texttt{fun x y -> x,y :'a -> 'b -> 'a * 'b}
        \item[D)] \texttt{fun x y -> x,y :'a -> 'b -> 'a -> 'b}
        \item[E)] \texttt{f1:'a list->('b -> 'a * 'b) list}
    \end{itemize}
    \textbf{Risposta esatta:} E - \texttt{f1:'a list->('b -> 'a * 'b) list} \\
    \textbf{Spiegazione:} Analizziamo la funzione anonima interna \texttt{(fun x y -> x,y)}: essa prende un elemento \texttt{x} (di tipo \texttt{'a}), poi un \texttt{y} (di tipo \texttt{'b}) e restituisce la tupla \texttt{(x, y)} (di tipo \texttt{'a * 'b}). Grazie all'applicazione parziale, quando \texttt{List.map} passa un singolo elemento della lista a questa funzione, questa non viene valutata completamente, ma restituisce la funzione in attesa della \texttt{y}. Quindi \texttt{List.map} trasformerà una lista originaria di tipo \texttt{'a list} in una lista in cui ogni elemento è una funzione \texttt{('b -> 'a * 'b)}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{(fn x => x) 12;} ?
    \begin{itemize}
        \item[A)] \texttt{12: int}
        \item[B)] \texttt{'a: 12}
        \item[C)] \texttt{string: "12"}
        \item[D)] \texttt{fn: 'a -> 'a}
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - \texttt{12: int} \\
    \textbf{Spiegazione:} L'espressione \texttt{(fn x => x)} è la funzione identità generica in SML, che restituisce l'argomento ricevuto. Fornendo in pasto l'intero \texttt{12} all'operatore di applicazione, la funzione viene eseguita e il risultato è semplicemente il valore \texttt{12}. L'inferenza dei tipi dedurrà coerentemente che il tipo finale in output per il programma è \texttt{int}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f1 x = [x,x];} ?
    \begin{itemize}
        \item[A)] \texttt{f1=fn: 'a list -> 'a list}
        \item[B)] \texttt{f1=fn: 'a list -> 'b list}
        \item[C)] \texttt{f1=fn: 'a -> 'b}
        \item[D)] \texttt{f1=fn: 'a -> 'a list}
        \item[E)] \texttt{f1=fn: 'a -> 'a}
        \item[F)] \texttt{f1=fn: string -> string}
    \end{itemize}
    \textbf{Risposta esatta:} D - \texttt{f1=fn: 'a -> 'a list} \\
    \textbf{Spiegazione:} La funzione riceve un singolo argomento \texttt{x} a cui il sistema assegna provvisoriamente il tipo generico \texttt{'a}. Il corpo della funzione crea e restituisce una lista sintatticamente valida \texttt{[x, x]}. Poiché le liste in ML sono rigorosamente omogenee, una lista formata da elementi \texttt{x} avrà come tipo di ritorno \texttt{'a list}. 
    \vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f2 x y = (x @ (y x));} ?
    \begin{itemize}
        \item[A)] \texttt{fun: 'a -> 'a -> 'a -> 'a}
        \item[B)] \texttt{fun: 'a list -> ('a list -> 'a list) -> 'a list}
        \item[C)] \texttt{fun: 'a list -> 'a list -> 'a list}
        \item[D)] \texttt{fun: 'a list -> 'a list -> 'b list}
        \item[E)] \texttt{fun: 'a -> ('a list -> 'a list) -> 'a list}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{fun: 'a list -> ('a list -> 'a list) -> 'a list} \\
    \textbf{Spiegazione:} L'operatore \texttt{@} serve per concatenare (append) due liste dello stesso tipo. Questo implica che sia \texttt{x} sia il risultato di \texttt{(y x)} debbano essere liste di tipo \texttt{'a list}. Affinché \texttt{(y x)} sia un'espressione valida, \texttt{y} deve essere una funzione che accetta \texttt{x} (che abbiamo stabilito essere \texttt{'a list}) e restituisce a sua volta \texttt{'a list}. Dunque, la funzione \texttt{f2} riceve un \texttt{'a list} (il parametro \texttt{x}), poi una funzione \texttt{('a list -> 'a list)} (il parametro \texttt{y}) e restituisce il risultato dell'append, che è \texttt{'a list}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f3 x = List.map x;} ?
    \begin{itemize}
        \item[A)] \texttt{fun: ('a list -> 'b list) -> 'a list -> 'b list}
        \item[B)] \texttt{fun: ('a -> 'b) -> 'a -> 'b}
        \item[C)] \texttt{fun: 'a list -> 'b list}
        \item[D)] \texttt{fun: ('a -> 'a) -> 'a list -> 'a list}
        \item[E)] \texttt{fun: ('a -> 'b) -> 'a list -> 'b list}
    \end{itemize}
    \textbf{Risposta esatta:} E - \texttt{fun: ('a -> 'b) -> 'a list -> 'b list} \\
    \textbf{Spiegazione:} La funzione della libreria standard \texttt{List.map} ha esattamente la firma polimorfa \texttt{('a -> 'b) -> 'a list -> 'b list}. Creando una funzione \texttt{f3} che prende \texttt{x} e lo passa come primo argomento a \texttt{List.map}, \texttt{x} deve necessariamente essere la funzione di trasformazione \texttt{('a -> 'b)}. \texttt{f3} restituisce l'applicazione parziale di \texttt{map}, che aspetta una \texttt{'a list} e restituisce una \texttt{'b list}. Di fatto, \texttt{f3} è l'esatto equivalente (\textit{eta-expansion}) di \texttt{List.map}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fn x => fn y => x + y;} ?
    \begin{itemize}
        \item[A)] \texttt{fn: int -> int}
        \item[B)] \texttt{fn: 'a -> 'a -> 'a}
        \item[C)] \texttt{fn: int -> int -> int}
        \item[D)] \texttt{fn: 'a -> 'a}
        \item[E)] \texttt{fn: int -> int -> 'a}
        \item[F)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} C - \texttt{fn: int -> int -> int} \\
    \textbf{Spiegazione:} In Standard ML, se non viene specificato diversamente in maniera esplicita, l'operatore matematico di somma \texttt{+} è sovraccaricato in modo da avere come tipo di default \texttt{int * int -> int}. Il currying della notazione anonima prende l'intero \texttt{x}, restituisce una funzione che attende l'intero \texttt{y}, e infine restituisce la somma intera.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun funzione x y z = x \textasciicircum\ y \textasciicircum\ z} ?
    \begin{itemize}
        \item[A)] \texttt{fn: string -> string}
        \item[B)] \texttt{fn: 'a -> 'a}
        \item[C)] \texttt{fn: 'a -> 'a -> 'a -> 'a}
        \item[D)] \texttt{fn: string -> string -> string -> int}
        \item[E)] Nessuna delle altre risposte
        \item[F)] \texttt{fn: string -> string -> string -> string}
    \end{itemize}
    \textbf{Risposta esatta:} F - \texttt{fn: string -> string -> string -> string} \\
    \textbf{Spiegazione:} L'operatore \texttt{\textasciicircum} (caret) in ML è dedicato esclusivamente alla concatenazione di stringhe. Ciò forza il motore di inferenza di ML a imporre il tipo \texttt{string} ai parametri \texttt{x}, \texttt{y} e \texttt{z}. Poiché la concatenazione di tre stringhe genera a sua volta una nuova stringa, il tipo in uscita è l'ennesimo \texttt{string}. (Nota: nel PDF c'è un evidente errore di stampa dove l'operatore \texttt{\textasciicircum} viene trasposto come simbolo matematico \texttt{$\wedge$}).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun c a1 a2 = a1::a2;} ?
    \begin{itemize}
        \item[A)] \texttt{fn: 'a list -> 'a list -> 'a list}
        \item[B)] \texttt{fn: 'a -> 'a list -> 'a list}
        \item[C)] \texttt{fn: 'a -> 'a -> 'a}
        \item[D)] \texttt{fn: 'a -> 'a list}
        \item[E)] \texttt{fn: 'a -> 'a -> 'a list}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{fn: 'a -> 'a list -> 'a list} \\
    \textbf{Spiegazione:} Il costruttore di lista \texttt{::} (cons) inserisce un singolo elemento in testa a una lista esistente. Per definizione sintattica, l'operando di sinistra (\texttt{a1}) deve essere un singolo elemento di tipo generico \texttt{'a}, mentre l'operando di destra (\texttt{a2}) deve essere una lista formata da elementi dello stesso tipo (\texttt{'a list}). Il risultato dell'operazione è l'intera nuova lista concatenata, da qui il tipo finale di \texttt{'a list}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fn l1 => fn l2 => l1 @ l2;} ?
    \begin{itemize}
        \item[A)] \texttt{fn: 'a list -> 'a list -> 'a list}
        \item[B)] \texttt{fn: 'a -> 'a list -> 'a list}
        \item[C)] \texttt{fn: 'a -> 'a -> 'a}
        \item[D)] \texttt{fn: 'a -> 'a list}
        \item[E)] \texttt{fn: 'a -> 'a -> 'a list}
    \end{itemize}
    \textbf{Risposta esatta:} A - \texttt{fn: 'a list -> 'a list -> 'a list} \\
    \textbf{Spiegazione:} A differenza dell'operatore \texttt{::} (cons), l'operatore \texttt{@} unisce (effettua un merge/append) due liste complete. Pertanto, il motore di inferenza pretenderà che sia \texttt{l1} che \texttt{l2} siano entrambi istanze di \texttt{'a list}. (Nota: nel file originale c'è un errore OCR con i numeri \texttt{11} e \texttt{12}, la notazione corretta usa \texttt{l1} ed \texttt{l2}).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fn a => fn b => fn c => (a * b) / c;} ? (nota: corretto in base all'esito fornito tra le opzioni del PDF, originariamente apparso come \texttt{a b / c})
    \begin{itemize}
        \item[A)] \texttt{fn: int -> int -> int -> int}
        \item[B)] \texttt{fn: 'a -> 'a -> 'a -> 'a}
        \item[C)] \texttt{fn: int -> int -> int -> real}
        \item[D)] \texttt{fn: real -> real -> real -> real}
        \item[E)] \texttt{fn: int -> int -> real -> real}
    \end{itemize}
    \textbf{Risposta esatta:} D - \texttt{fn: real -> real -> real -> real} \\
    \textbf{Spiegazione:} In SML l'operatore di divisione \texttt{/} è riservato strettamente e in modo esclusivo ai numeri in virgola mobile (i \texttt{real}). La divisione tra interi usa l'operatore \texttt{div}. Questo singolo vincolo forza immediatamente a cascata l'inferenza di tutti i tipi coinvolti: \texttt{c} deve essere \texttt{real} per poter fungere da divisore, \texttt{a * b} deve risultare \texttt{real} (costringendo \texttt{a} e \texttt{b} a essere \texttt{real}), e il risultato finale sarà un \texttt{real}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f [] = (7.0; print("ab")) | f (x::xs) = (2.3; print("cd"));} ?
    \begin{itemize}
        \item[A)] \texttt{val f = fn: 'a list -> unit}
        \item[B)] \texttt{val f = fn: 'a list -> real}
        \item[C)] \texttt{'a list -> int}
        \item[D)] \texttt{'a tuple}
        \item[E)] \texttt{real list -> string}
    \end{itemize}
    \textbf{Risposta esatta:} A - \texttt{val f = fn: 'a list -> unit} \\
    \textbf{Spiegazione:} Esaminando il costrutto, capiamo dai pattern \texttt{[]} e \texttt{x::xs} che la funzione accetta una lista. Poiché non impone vincoli su cosa contenga, è un parametro \texttt{'a list}. Analizzando i corpi separati da \texttt{;} (valutazione sequenziale), l'interprete valuta la prima espressione (es. \texttt{7.0}), la scarta e valuta l'ultima, restituendone il risultato. L'ultima espressione è sempre \texttt{print(...)} che in ML ha effetti collaterali sull'output di sistema, ma restituisce sempre un token vuoto di tipo \texttt{unit}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è l'output del seguente codice: \texttt{List.partition (fn x => x>0) (List.take ([\~1,1,\~2,\~2,\~3,3],4));} ?
    \begin{itemize}
        \item[A)] \texttt{val it=([1,3], [\~1, \~2, \~2, \~3]): int list * int list}
        \item[B)] \texttt{val it=[1,3]: int list}
        \item[C)] \texttt{val it=[1]: int list}
        \item[D)] \texttt{val it=([1],[\~1,\~2,\~2]): int list * int list}
        \item[E)] \texttt{val it=([],[\~2]); int list * int list}
        \item[F)] \texttt{val it=[] int list}
    \end{itemize}
    \textbf{Risposta esatta:} D - \texttt{val it=([1],[\~1,\~2,\~2]): int list * int list} \\
    \textbf{Spiegazione:} La prima operazione ad essere eseguita, trovandosi tra le parentesi più interne, è \texttt{List.take(..., 4)}. Questa preleva unicamente i primi 4 elementi della lista fornita, accorciandola a \texttt{[\~1, 1, \~2, \~2]}. Successivamente, \texttt{List.partition} con il predicato \texttt{(x > 0)} divide questa nuova lista in una tupla di due liste: la prima contiene gli elementi che soddisfano il requisito (cioè il solo \texttt{1}), la seconda contiene tutti gli altri (i numeri negativi).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fn a => fn b => a=b;} ?
    \begin{itemize}
        \item[A)] \texttt{fn: ''a -> ''a -> bool}
        \item[B)] \texttt{fn: int -> int -> bool}
        \item[C)] \texttt{fn: string -> string -> bool}
        \item[D)] \texttt{fn: bool -> bool -> bool}
        \item[E)] \texttt{fn: 'a -> 'a -> bool}
    \end{itemize}
    \textbf{Risposta esatta:} A - \texttt{fn: ''a -> ''a -> bool} \\
    \textbf{Spiegazione:} L'operatore \texttt{=} verifica l'uguaglianza tra due elementi che, per logica, restituiscono un valore \texttt{bool}. In SML, tuttavia, l'uguaglianza non è definita per tutti i tipi (ad esempio, è illegale confrontare due \texttt{real} per problemi di precisione binaria o due funzioni). Si utilizza allora un tipo speciale di variabile di tipo, definita "Equality Type" e denotata dal doppio apice \texttt{''a}, che indica "qualsiasi tipo a patto che sia supportato il controllo di uguaglianza".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo della seguente espressione: \texttt{fun f[] = (print("ab"); 7.0) | f(x::xs) = (print("cd"); 4.0);} ?
    \begin{itemize}
        \item[A)] \texttt{'a list --> int}
        \item[B)] \texttt{'a --> real}
        \item[C)] \texttt{'a record}
        \item[D)] \texttt{'a tuple}
        \item[E)] \texttt{'a list --> real}
    \end{itemize}
    \textbf{Risposta esatta:} E - \texttt{'a list --> real} \\
    \textbf{Spiegazione:} Esattamente come per la Domanda 13 appena vista, l'ingresso è implicitamente marcato come una \texttt{'a list}. Questa volta, all'interno del blocco sequenziale, le posizioni sono invertite: il \texttt{print} viene eseguito e il suo \texttt{unit} scartato in favore del secondo termine. In entrambi i rami viene ritornato un numero decimale (rispettivamente \texttt{7.0} e \texttt{4.0}), fissando il tipo di uscita a \texttt{real}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In ML \texttt{:>} serve a:
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Nasconde i componenti di una lista
        \item[C)] È usata per definire funzioni polimorfiche
        \item[D)] Nasconde i componenti di una struttura
        \item[E)] Definisce la firma di una struttura
    \end{itemize}
    \textbf{Risposta esatta:} D - Nasconde i componenti di una struttura \\
    \textbf{Spiegazione:} In SML, l'operatore \texttt{:>} (Ascription Opaca o Opaque Signature Matching) viene utilizzato in fase di dichiarazione di una struttura per associarvi una firma (signature). A differenza dell'ascription trasparente (\texttt{:}), esso "nasconde" (rende astratti) all'esterno tutti i dettagli implementativi e i tipi concreti definiti nella struttura che non sono esplicitamente descritti nella firma, garantendo così l'Information Hiding e l'implementazione degli Abstract Data Types (ADT).
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il seguente programma in ML, indicare il risultato: \\
    \texttt{Datatype fruit = Apple | Pear | Grape; fun isApple x = (x=Apple); isApple (Lemon);}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] False
        \item[C)] Undeclared value
        \item[D)] True
        \item[E)] Type error
    \end{itemize}
    \textbf{Risposta esatta:} C - Undeclared value \\
    \textbf{Spiegazione:} L'errore scaturisce a tempo di compilazione. Il costruttore di tipo \texttt{Lemon} non è stato dichiarato da nessuna parte né nel \texttt{datatype fruit}, né come variabile. Di conseguenza, l'interprete si blocca prima ancora di poter effettuare il controllo di uguaglianza (che restituirebbe False) o l'inferenza di tipo, segnalando che il valore non è definito (Undeclared value/Unbound constructor). (Nota: nel PDF il testo omette i \texttt{=} e i \texttt{|} per refusi di trascrizione, ma la semantica è quella indicata).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è l'output del seguente codice: \texttt{val i = ref 1; while !i<10 do i := !i+1;} ?
    \begin{itemize}
        \item[A)] \texttt{val i=10}
        \item[B)] \texttt{val it=()}
        \item[C)] Loop infinito
        \item[D)] Type error
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{val it=()} \\
    \textbf{Spiegazione:} In SML, il costrutto \texttt{while ... do ...} viene utilizzato per scopi prettamente imperativi (side effects) e aggiornamento di riferimenti mutabili. Al termine della sua esecuzione (quando la condizione diventa falsa), il ciclo non restituisce alcun valore utile dal punto di vista funzionale, ma valuta sempre all'entità vuota \texttt{unit}, che la shell di SML stampa a video come \texttt{val it = () : unit}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è l'output del seguente codice: \\
    \texttt{val m = (List.map(fn x=>x*x) [4, 5, 6]); List.foldr Int.+ 0 m;}
    \begin{itemize}
        \item[A)] 15
        \item[B)] 77
        \item[C)] \texttt{'a list -> int}
        \item[D)] NaN
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} B - 77 \\
    \textbf{Spiegazione:} Seguiamo il codice passo passo. Prima eseguiamo \texttt{List.map} con la funzione di elevamento al quadrato (\texttt{x*x}) sulla lista \texttt{[4, 5, 6]}. Il risultato è la nuova lista \texttt{m = [16, 25, 36]}. Poi usiamo \texttt{List.foldr} passando la funzione di somma intera (\texttt{Int.+}), con elemento neutro \texttt{0}, su \texttt{m}. Questo esegue l'accumulazione $16 + (25 + (36 + 0))$, che ammonta esattamente a $77$.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In ML \texttt{4+3.0} genera come risultato:
    \begin{itemize}
        \item[A)] Errore di tipo
        \item[B)] 7.0
        \item[C)] Nessuna delle altre risposte
        \item[D)] Dipende dall'implementazione
        \item[E)] 7
    \end{itemize}
    \textbf{Risposta esatta:} A - Errore di tipo \\
    \textbf{Spiegazione:} SML è un linguaggio fortemente tipizzato che non ammette coercion (conversioni di tipo implicite) tra tipi numerici. L'intero \texttt{4} e il floating point \texttt{3.0} non possono essere addizionati direttamente con lo stesso operatore senza prima essere forzati esplicitamente a far combaciare i tipi (es. usando \texttt{Real.fromInt(4) + 3.0}). L'espressione, così come fornita, causa un type mismatch (Errore di tipo).
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si assuma che \texttt{A} sia un array di lunghezza 40. Trovare il 30simo elemento.
    \begin{itemize}
        \item[A)] \texttt{Array.sub(A,30)}
        \item[B)] \texttt{Array.sub(A,29)}
        \item[C)] Nessuna delle altre risposte
        \item[D)] \texttt{sub(A,29)}
        \item[E)] \texttt{A[30]}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{Array.sub(A,29)} \\
    \textbf{Spiegazione:} In SML, la struttura standard per operare sugli array mutabili è \texttt{Array}. L'accesso in lettura si effettua obbligatoriamente chiamando la funzione \texttt{Array.sub(array, indice)} (rendendo errata l'opzione E e D). Siccome gli array sono zero-based (il primo elemento è in posizione 0), il 30esimo elemento logico si troverà all'indice \texttt{29}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Indicare il risultato di \texttt{Struct2.x = Struct2.i;} Dato il seguente programma ML: \\
    \texttt{structure Struct1 = struct val i = 3; type t = int; val x = 4 end; signature SIG1 = sig val i:int; type t; val x: t; end; structure Struct2:> SIG1 = Struct1;}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 3
        \item[C)] False
        \item[D)] Type error
        \item[E)] True
    \end{itemize}
    \textbf{Risposta esatta:} D - Type error \\
    \textbf{Spiegazione:} Questo è un classico esempio di Information Hiding tramite l'operatore \texttt{:>} (Ascription Opaca). All'interno di \texttt{Struct1}, il tipo \texttt{t} è esplicitamente sinonimo di \texttt{int}. Tuttavia, sigillando \texttt{Struct1} nella \texttt{Struct2} tramite \texttt{SIG1}, esponiamo \texttt{t} come tipo "astratto" (\texttt{type t;}). Quando si tenta di valutare l'uguaglianza all'esterno, il compilatore vede \texttt{Struct2.x} come avente il tipo opaco incommensurabile \texttt{t}, e \texttt{Struct2.i} come \texttt{int}. Non sapendo più che \texttt{t} maschera un intero, fallisce con un Errore di Tipo.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} In ML, quale espressione è legale:
    \begin{itemize}
        \item[A)] \texttt{[1.0, 2, 3]}
        \item[B)] \texttt{fn x:real => x*2}
        \item[C)] \texttt{"a", 1, 2}
        \item[D)] \texttt{(1, 2, 0)}
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} D - \texttt{(1, 2, 0)} \\
    \textbf{Spiegazione:} L'opzione A è illegale perché le liste in ML non possono mescolare tipi \texttt{real} e \texttt{int}. L'opzione B è illegale perché un operando annotato come \texttt{real} non può essere moltiplicato per \texttt{2} (che è \texttt{int}, servirebbe \texttt{2.0}). L'opzione C manca delle parentesi tonde necessarie per formare una tupla. L'opzione D, \texttt{(1, 2, 0)}, è invece una tupla sintatticamente perfetta, formata da tre interi di tipo \texttt{int * int * int}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Qual è il tipo di \texttt{SOME[2, 3, 4];}
    \begin{itemize}
        \item[A)] \texttt{'a list option}
        \item[B)] \texttt{int list option}
        \item[C)] \texttt{(int*int*int) option}
        \item[D)] Nessuna delle altre risposte
        \item[E)] \texttt{int list}
    \end{itemize}
    \textbf{Risposta esatta:} B - \texttt{int list option} \\
    \textbf{Spiegazione:} Analizzando dall'interno verso l'esterno, \texttt{[2, 3, 4]} è banalmente una lista omogenea di interi, quindi il suo tipo è \texttt{int list}. \texttt{SOME} è un costruttore polimorfo del datatype \texttt{option}, avente firma \texttt{'a -> 'a option}. Sostituendo al parametro generico \texttt{'a} il nostro \texttt{int list}, otteniamo che l'espressione generata ha tipo finale \texttt{int list option}.
    \vspace{0.3cm}
\end{itemize}

\section{Domande Output Codici}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Utilizzando la tecnica dei tombstones cosa succede alla fine del frammento di codice dato?
\begin{verbatim}
p = malloc();
q = malloc();
*p = 999;
*q = 111;
*p += *q;
q = p;
free(p);
\end{verbatim}
    \begin{itemize}
        \item[A)] \texttt{*p=1110 e *q=111}
        \item[B)] p è deallocato e q punta ad una tombstone
        \item[C)] p e q sono entrambi deallocati
        \item[D)] q è deallocato e p punta ad una tombstone
        \item[E)] \texttt{*p=999 e *q=111}
        \item[F)] \texttt{*p=1110 e *q=1110}
        \item[G)] Nessuna delle altre risposte
    \end{itemize}

    \newpage
    
    \textbf{Risposta esatta:} G - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Analizziamo il codice. Vengono allocate due zone di memoria con i rispettivi tombstones (T1 per \texttt{p} e T2 per \texttt{q}). I valori diventano \texttt{*p = 999} e \texttt{*q = 111}. Con \texttt{*p += *q}, il valore puntato da \texttt{p} diventa 1110. Eseguendo \texttt{q = p}, il puntatore \texttt{q} smette di puntare a T2 e inizia a puntare al tombstone T1 (generando un memory leak su T2). Eseguendo infine \texttt{free(p)}, la memoria di T1 viene liberata e il tombstone T1 viene invalidato (marcato come "morto"). I puntatori locali \texttt{p} e \texttt{q} non vengono "deallocati", ma continuano a esistere sullo stack ed \textbf{entrambi puntano allo stesso tombstone invalidato} (T1). Poiché nessuna delle opzioni A-F descrive correttamente questa situazione, la risposta corretta (che è stata aggiunta nelle versioni successive del quiz, vedi domanda seguente) è "Nessuna".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Utilizzando la tecnica dei tombstones cosa succede alla fine del frammento di codice dato?
\begin{verbatim}
p = malloc();
q = malloc();
*p = 999;
*q = 111;
*p += *q;
q = p;
free(p);
\end{verbatim}
    \begin{itemize}
        \item[A)] \texttt{*p=1110 e *q=111}
        \item[B)] \texttt{*p=1110 e *q=1110}
        \item[C)] p e q sono entrambi deallocati
        \item[D)] q è deallocato e p punta ad una tombstone
        \item[E)] Nessuna delle risposte
    \end{itemize}
    \textbf{Risposta esatta:} E - Nessuna delle risposte \\
    \textbf{Spiegazione:} Identica alla domanda precedente, ma con le opzioni corrette. Come dimostrato, alla fine dell'esecuzione del codice \textbf{entrambi} i puntatori \texttt{p} e \texttt{q} puntano allo stesso tombstone ormai invalidato. Le variabili puntatore in sé non sono deallocate (rimangono sullo stack), ma il loro riferimento è "pendente" seppur protetto dalla tombstone. L'opzione esatta è dunque "Nessuna delle risposte".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Utilizzando la tecnica dei tombstones cosa succede alla fine del frammento di codice dato?
\begin{verbatim}
p = malloc();
q = malloc();
*p = 123;
*q = 321;
p = q;
free(q);
\end{verbatim}
    \begin{itemize}
        \item[A)] \texttt{*p=999 e *q=111}
        \item[B)] \texttt{*p=123 e *q=321}
        \item[C)] p è deallocato e q punta ad una tombstone
        \item[D)] p e q sono entrambi deallocati
        \item[E)] q è deallocato e p punta ad una tombstone
        \item[F)] \texttt{*p=321 e *q=321}
        \item[G)] Nessuna delle risposte
    \end{itemize}
    \textbf{Risposta esatta:} G - Nessuna delle risposte \\
    \textbf{Spiegazione:} Assegnando \texttt{p = q}, si fa in modo che \texttt{p} punti allo stesso tombstone di \texttt{q}. Subito dopo, la \texttt{free(q)} dealloca la memoria a cui quel tombstone puntava, invalidandolo. Di conseguenza, alla fine dell'esecuzione, sia \texttt{p} che \texttt{q} si ritroveranno a puntare contemporaneamente a una tombstone marcata come distrutta (dangling pointer). Dato che nessuna delle frasi proposte descrive questo stato ("entrambi puntano ad una tombstone"), la risposta è Nessuna.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per nome}?
\begin{verbatim}
int c = 2;
int pippo (int a) {
    c = c + 2;
    return a * 2;
}
int pluto (void) {
    return pippo(c + 1);
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 10
        \item[C)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[D)] 4
        \item[E)] 6
    \end{itemize}
    \textbf{Risposta esatta:} B - 10 \\
    \textbf{Spiegazione:} Nel passaggio per nome (Call by Name), l'argomento \texttt{(c + 1)} non viene valutato al momento della chiamata, ma viene testualmente sostituito all'interno della funzione \texttt{pippo} (tramite un thunk invisibile). All'interno di \texttt{pippo}, viene prima eseguito \texttt{c = c + 2}, portando la variabile globale \texttt{c} da 2 a 4. Successivamente, si esegue il return \texttt{a * 2}, che per nome diventa \texttt{(c + 1) * 2}. Poiché \texttt{c} ora vale 4, il calcolo sarà \texttt{(4 + 1) * 2 = 10}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int c = 2;
int pippo (int a) {
    c = c + 2;
    return a * 2;
}
int pluto (void) {
    return pippo(c + 1);
}
\end{verbatim}
    \begin{itemize}
        \item[A)] 10
        \item[B)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[C)] Nessuna delle altre risposte
        \item[D)] 6
        \item[E)] Non è possibile passare c+1 per valore
    \end{itemize}
    \textbf{Risposta esatta:} D - 6 \\
    \textbf{Spiegazione:} Nel passaggio per valore (Call by Value), l'argomento viene valutato \textit{prima} di entrare nella funzione. Poiché inizialmente \texttt{c=2}, l'espressione \texttt{c+1} viene valutata a 3, e questo valore fisso viene copiato nel parametro locale \texttt{a}. All'interno di \texttt{pippo}, la variabile globale \texttt{c} viene aggiornata a 4 (\texttt{c = 2 + 2}), ma questo non intacca minimamente la variabile locale \texttt{a}, che è rimasta 3. Il calcolo finale sarà \texttt{a * 2 = 3 * 2 = 6}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di \texttt{f(1, r(1), 1)} se i parametri sono passati \textbf{per nome}?
\begin{verbatim}
int r(int x) {
    return r(x-1);
}
int f(int a, int b, int c) {
    if (c == 1) return a;
    else return b;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Non è possibile dirlo senza conoscere il tipo di scope (statico o dinamico) utilizzato
        \item[B)] Nessuna delle altre risposte
        \item[C)] 1
        \item[D)] Si ha ricorsione infinita
        \item[E)] 0
    \end{itemize}
    \textbf{Risposta esatta:} C - 1 \\
    \textbf{Spiegazione:} Se il passaggio fosse per valore, l'argomento \texttt{r(1)} verrebbe valutato prima della chiamata a \texttt{f}, causando una ricorsione infinita. Ma trovandoci in Call by Name, gli argomenti vengono passati chiusi in un thunk e valutati \textit{solo e soltanto se} il loro valore viene esplicitamente richiesto nel codice. Nel corpo di \texttt{f}, \texttt{c} vale 1, quindi l'espressione \texttt{(c == 1)} è vera. La funzione restituisce immediatamente \texttt{a} (che vale 1). Il parametro \texttt{b} (\texttt{r(1)}) non viene mai menzionato né valutato, scongiurando il loop infinito.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma (espresso in pseudo-codice), quanto vale la variabile globale c dopo aver eseguito topolino(), assumendo \textbf{scope dinamico}?
\begin{verbatim}
int a, b, c;
void pippo (void) { int a; a = 6; b = 5; }
void pluto (void) { int c; int b; pippo(); c = 3; a = 4; }
void topolino (void) { int a; a = 1; b = 10; pluto(); c = a + b; }
\end{verbatim}
    \begin{itemize}
        \item[A)] 14
        \item[B)] 3
        \item[C)] Non è possibile dirlo
        \item[D)] 6
        \item[E)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} A - 14 \\
    \textbf{Spiegazione:} Tracciamo la catena dinamica: \texttt{topolino} chiama \texttt{pluto} che chiama \texttt{pippo}. 
    In \texttt{pippo}: la \texttt{b=5} cerca una \texttt{b} locale, non la trova e risale alla funzione chiamante (\texttt{pluto}). Poiché \texttt{pluto} ha una \texttt{b} locale, aggiorna quella a 5 (la \texttt{b} globale resta 10). 
    In \texttt{pluto}: imposta la sua \texttt{c} locale a 3, poi \texttt{a=4} cerca una \texttt{a} risalendo la catena fino a \texttt{topolino}, aggiornando la \texttt{a} locale di quest'ultima a 4. 
    In \texttt{topolino}: esegue \texttt{c = a + b}. Cerca \texttt{c}, non ne ha, risale al main e aggiorna la \texttt{c} globale. A quale valore? \texttt{a} è la sua \texttt{a} locale (appena aggiornata a 4). \texttt{b} non c'è in topolino, quindi risale alla \texttt{b} globale (che era stata posta a 10 all'inizio di topolino). Dunque \texttt{c = 4 + 10 = 14}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma (espresso in pseudo-codice), qual è il valore di ritorno di f1(), assumendo \textbf{scope statico}?
\begin{verbatim}
int x, y, z;
void f3 (void) { x = 0; y = 5; }
void f2 (void) { int y; f3(); y = 0; z = 10; }
int f1 (void) { 
    int x; 
    x = -5; y = 10; z = x + y; 
    f2(); 
    return z - y - x; 
}
\end{verbatim}
    \begin{itemize}
        \item[A)] 5
        \item[B)] Nessuna delle altre risposte
        \item[C)] Non è possibile dirlo
        \item[D)] 0
        \item[E)] -5
    \end{itemize}
    \textbf{Risposta esatta:} B - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Esecuzione statica (lessicale, ogni blocco guarda le variabili globali se non le ha localmente). In \texttt{f1}: \texttt{x} locale diventa -5. \texttt{y} globale diventa 10. \texttt{z} globale diventa -5 + 10 = 5. Viene chiamata \texttt{f2}. In \texttt{f2}: viene chiamata \texttt{f3}, che aggiorna \texttt{x} globale a 0 e \texttt{y} globale a 5. Poi \texttt{f2} setta la sua \texttt{y} locale a 0 e aggiorna la \texttt{z} globale a 10. Tornando a \texttt{f1} per il \texttt{return z - y - x}: \texttt{z} (globale) è 10, \texttt{y} (globale) è 5 (aggiornata da f3), \texttt{x} (locale di f1) è -5. Il calcolo è \texttt{10 - 5 - (-5) = 10}. Poiché 10 non è tra le opzioni (5, 0, -5), la risposta corretta è Nessuna.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int b = 666;
int pippo (int x) {
    x = 666;
    b = 1;
    return x / 2;
}
int pluto (void) {
    int a, c;
    a = b / 333;
    c = pippo(a);
    return a + c;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 1
        \item[C)] 3
        \item[D)] 0
        \item[E)] Dipende dal tipo di scope (statico o dinamico) utilizzato
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Analizziamo \texttt{pluto}: \texttt{a = 666 / 333}, quindi \texttt{a = 2}. Chiamando \texttt{pippo(2)} per valore, una copia del 2 viene inserita in \texttt{x}. All'interno di \texttt{pippo}: la \texttt{x} locale viene sovrascritta con 666, e la \texttt{b} globale diventa 1. La funzione ritorna \texttt{666 / 2 = 333}, valore che viene salvato nella variabile \texttt{c} in \texttt{pluto}. Alla fine, \texttt{pluto} calcola il ritorno sommando \texttt{a + c}. Poiché \texttt{a} è rimasto inalterato a 2, il ritorno sarà \texttt{2 + 333 = 335}. Questo valore non è presente, per cui l'opzione giusta è "Nessuna".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Funzione implementata dallo pseudo-codice:
\begin{verbatim}
int somma (int a, int b) {
    if (a == 0) return b;
    return somma (a-1, b+1);
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Non usa ricorsione in coda
        \item[B)] Non può essere implementata per via iterativa
        \item[C)] Nessuna delle altre risposte
        \item[D)] Usa ricorsione in coda
        \item[E)] Causa sempre ricorsione infinita
    \end{itemize}
    \textbf{Risposta esatta:} D - Usa ricorsione in coda \\
    \textbf{Spiegazione:} La funzione si chiama iterativamente decrementando \texttt{a} e incrementando \texttt{b}. La caratteristica fondamentale è che l'espressione \texttt{return somma(a-1, b+1);} è l'ultimissima istruzione eseguita nel blocco. Non ci sono calcoli "postumi" (es. moltiplicazioni o somme esterne alla chiamata ricorsiva) che il record di attivazione debba attendere. Per questo motivo, questa forma ottimizzata prende il nome di Ricorsione in Coda (Tail Recursion).
    \vspace{0.3cm}

\item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di \texttt{f3(10,f4(0))} se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int f4 (int x) {
    return 1/x;
}
int f3 (int a, int b) {
    if (a > 1) return a;
    else return b;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] 10
        \item[B)] Non è possibile dirlo senza conoscere il tipo di scope (statico o dinamico) utilizzato
        \item[C)] Non è possibile passare f4(0) per valore
        \item[D)] Nessuna delle altre risposte
        \item[E)] 1
    \end{itemize}
    \textbf{Risposta esatta:} D - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Nel passaggio dei parametri per valore (Call by Value), gli argomenti della funzione vengono sempre valutati compiutamente \textit{prima} che la funzione chiamante venga effettivamente invocata. Pertanto, l'interprete cercherà di valutare \texttt{f4(0)} ancor prima di entrare in \texttt{f3}. L'esecuzione di \texttt{f4(0)} comporta l'operazione matematica \texttt{1 / 0}, che genera una divisione per zero (eccezione hardware/runtime crash). Il programma si interromperà, quindi non ritornerà né 10, né 1. Non è vero che "non è possibile passarlo" (la sintassi è corretta, ma fallisce semanticamente l'esecuzione). L'unica risposta logica è "Nessuna".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di f1() se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int i=0;
int x[10];
int f2 (int z) {
    i = i/2;
    return z - i;
}
int f1 (void) {
    int y;
    i = 4;
    x[0]=1; x[1]=0; x[2]=2; x[3]=4; x[4]=6;
    y = f2(x[i]);
    return y + i;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 0
        \item[C)] 2
        \item[D)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[E)] 4
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Analizziamo \texttt{f1()}: la variabile globale \texttt{i} viene settata a 4. L'istruzione \texttt{y = f2(x[i])} nel call-by-value richiede la valutazione anticipata dell'argomento. Essendo \texttt{i=4}, calcoliamo \texttt{x[4]} che corrisponde al valore 6. Entriamo in \texttt{f2(6)}: la \texttt{i} globale viene dimezzata (\texttt{i = 4 / 2 = 2}), e viene ritornato \texttt{z - i}, cioè \texttt{6 - 2 = 4}. Assegniamo dunque 4 a \texttt{y} in \texttt{f1}. Alla riga successiva, viene valutato il return \texttt{y + i}. Dato che \texttt{y=4} e la \texttt{i} globale è ora 2, la somma è \texttt{4 + 2 = 6}. Poiché 6 non è presente fra le opzioni fornite, la risposta esatta è "Nessuna".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma, qual è il valore di ritorno di f1(), assumendo \textbf{scope dinamico}?
\begin{verbatim}
int x, y, z;
void f3 (void) { x = 0; y = 5; }
void f2 (void) { int y; f3(); y = 0; z = 10; }
int f1 (void) {
    int x;
    x = -5; y = 10; z = x + y;
    f2();
    return z - y - x;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Non è possibile dirlo
        \item[B)] 5
        \item[C)] 0
        \item[D)] Nessuna delle altre risposte
        \item[E)] -5
    \end{itemize}
    \textbf{Risposta esatta:} C - 0 \\
    \textbf{Spiegazione:} Con lo \textit{scope dinamico}, l'interprete risale lo Stack delle chiamate (\texttt{f3 -> f2 -> f1 -> main}) per trovare le variabili.
    1. In \texttt{f1}: \texttt{x} locale vale -5. \texttt{y} globale vale 10. \texttt{z} globale vale 5 (\texttt{-5 + 10}).
    2. Chiamata a \texttt{f2}: viene allocata una \texttt{y} locale.
    3. Chiamata a \texttt{f3}: assegna 0 a \texttt{x}. Risalendo la catena, il primo \texttt{x} si trova in \texttt{f1}, quindi \textbf{la \texttt{x} locale di \texttt{f1} diventa 0}. Assegna 5 a \texttt{y}. Risalendo, il primo \texttt{y} è quello locale a \texttt{f2}, quindi \textbf{la \texttt{y} locale di \texttt{f2} diventa 5}.
    4. Ritorno in \texttt{f2}: l'istruzione \texttt{y = 0} sovrascrive la \texttt{y} locale di \texttt{f2} a 0. L'istruzione \texttt{z = 10} trova solo la \texttt{z} globale e la setta a 10.
    5. Ritorno in \texttt{f1}: esegue \texttt{z - y - x}. La variabile \texttt{z} trovata è la globale (10). La variabile \texttt{y} trovata è la globale (rimasta a 10, intoccata). La variabile \texttt{x} trovata è la locale di \texttt{f1} (modificata a 0 da \texttt{f3}).
    Calcolo finale: \texttt{10 - 10 - 0 = 0}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int b = 666;
int pippo (int x) {
    x = 666;
    b = 1;
    return x / 2;
}
int pluto (void) {
    int a, c;
    a = b / 333;
    c = pippo(a);
    return a + c;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 666
        \item[C)] 999
        \item[D)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[E)] 333
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Questo è lo stesso pseudo-codice analizzato in precedenza. In \texttt{pluto}, calcoliamo \texttt{a = 666 / 333 = 2}. \texttt{pippo(2)} riceve una copia del 2 in \texttt{x}, che viene subito sovrascritta con 666. La funzione ritorna \texttt{666 / 2 = 333} (salvato in \texttt{c}) e muta \texttt{b} in 1. Poiché il passaggio è per valore, la variabile \texttt{a} in \texttt{pluto} resta inalterata (vale 2). Il ritorno finale è \texttt{a + c = 2 + 333 = 335}. Essendo assente il 335, la risposta corretta è Nessuna.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Funzione implementata dallo pseudo-codice:
\begin{verbatim}
int somma (int a, int b) {
    if (a == 0) return b;
    return somma (a-1, b) + 1;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Non può essere implementata per via iterativa
        \item[C)] Non usa ricorsione in coda
        \item[D)] Causa sempre ricorsione infinita
    \end{itemize}
    \textbf{Risposta esatta:} C - Non usa ricorsione in coda \\
    \textbf{Spiegazione:} Affinché una funzione utilizzi la Ricorsione in Coda (Tail Recursion), la chiamata a se stessa deve essere rigorosamente l'ultima espressione valutata. In questo pseudo-codice, la chiamata \texttt{somma(a-1, b)} viene effettuata, il suo risultato deve essere atteso dal Record di Attivazione corrente e, solo successivamente, vi viene sommato \texttt{+ 1}. Questo blocco (\texttt{+ 1}) finale "rompe" il pattern di coda, richiedendo l'espansione dello Stack.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice della Domanda 14. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per nome}?
\begin{verbatim}
int b = 666;
int pippo (int x) {
    x = 666; b = 1; return x / 2;
}
int pluto (void) {
    int a, c; a = b / 333; c = pippo(a); return a + c;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Non è possibile passare a per nome
        \item[B)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[C)] Nessuna delle altre risposte
        \item[D)] 999
        \item[E)] 335
    \end{itemize}
    \textbf{Risposta esatta:} D - 999 \\
    \textbf{Spiegazione:} Nel passaggio per nome (Call by Name), passare una variabile semplice (come \texttt{a}) genera un thunk che punta a quella specifica locazione di memoria; questo di fatto simula un comportamento di "Passaggio per Riferimento". 
    1. In \texttt{pluto}: calcoliamo \texttt{a = 2}. Invochiamo \texttt{pippo(a)} passando \texttt{a} per nome.
    2. In \texttt{pippo}: l'istruzione \texttt{x = 666} sostituisce testualmente \texttt{x} con \texttt{a}, alterando la variabile \texttt{a} nel chiamante facendola diventare 666.
    3. Il ritorno calcola \texttt{x / 2} (ovvero \texttt{a / 2}), che valuta \texttt{666 / 2 = 333}.
    4. Tornati in \texttt{pluto}: \texttt{c = 333}. Si esegue \texttt{return a + c}. Dato che la variabile locale \texttt{a} è stata mutata in 666 dalla chiamata per nome, il calcolo finale sarà \texttt{666 + 333 = 999}.
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} La funzione implementata dallo pseudo-codice:
\begin{verbatim}
int mistero (int a, int b) {
    if (b == 0) return a;
    return mistero (a/2, b-1);
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Può causare una crescita incontrollata dello stack
        \item[B)] Causa sempre ricorsione infinita
        \item[C)] Nessuna delle altre risposte
        \item[D)] Non può essere implementata per via iterativa
        \item[E)] Non usa ricorsione in coda
    \end{itemize}
    \textbf{Risposta esatta:} C - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Analizziamo logicamente il costrutto: la chiamata ricorsiva a \texttt{mistero} è l'ultimo blocco logico ad essere eseguito e ritornato (senza operazioni appese come un \texttt{+1}), dunque USA esplicitamente la ricorsione in coda (falsa E). Grazie alla ricorsione in coda, il compilatore la ottimizzerà rendendo lo stack costante e prevenendo crescite incontrollate (falsa A). Per induzione, non causa ricorsione infinita a meno che \texttt{b} non sia negativo (falsa B), e come ogni algoritmo ricorsivo in coda, può essere banalmemente convertito in un ciclo \texttt{while} iterativo (falsa D). Dunque, nessuna affermazione è vera.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma, qual è il valore di ritorno di topolino(), assumendo \textbf{scope statico}?
\begin{verbatim}
int x, y;
void pippo (void) { x = 8; y = 4; }
void pluto (void) { int y; pippo(); y = 3; }
int topolino (void) {
    int x, z;
    x = 5; y = 15; z = x + y;
    pluto();
    return z - y - x;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Non è possibile dirlo
        \item[C)] 0
        \item[D)] -3
    \end{itemize}
    \textbf{Risposta esatta:} E - 11 \\
    \textbf{Spiegazione:} Seguiamo lo Scope Statico (lessicale), dove i nomi non dichiarati localmente si riferiscono sempre alle definizioni globali nel testo.
    1. In \texttt{topolino}: setta la \texttt{x} locale a 5. La \texttt{y} si riferisce alla globale (che diventa 15). Calcola \texttt{z} locale: \texttt{5 + 15 = 20}.
    2. Chiamata a \texttt{pluto}: alloca una \texttt{y} locale.
    3. Chiamata a \texttt{pippo}: setta la \texttt{x} globale a 8, e la \texttt{y} globale a 4 (ignora la catena dinamica).
    4. Ritorno a \texttt{pluto}: setta la \texttt{y} locale a 3 (ininfluente ai fini logici generali).
    5. Ritorno a \texttt{topolino}: valuta \texttt{z - y - x}. La \texttt{z} locale è rimasta 20. La \texttt{y} globale è stata impostata a 4 da \texttt{pippo}. La \texttt{x} locale è sempre stata 5 (intoccata da pippo che operava sulla globale).
    Il risultato è \texttt{20 - 4 - 5 = 11}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma (lo stesso della Domanda 13), qual è il valore di ritorno di f1(), assumendo \textbf{scope dinamico}?
\begin{verbatim}
int x, y, z;
void f3 (void) { x = 0; y = 5; }
void f2 (void) { int y; f3(); y = 0; z = 10; }
int f1 (void) {
    int x; x = -5; y = 10; z = x + y;
    f2(); return z - y - x;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] -1
        \item[C)] -5
        \item[D)] 5
        \item[E)] Non è possibile dirlo
    \end{itemize}
    \textbf{Risposta esatta:} A - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Come dimostrato esaurientemente nella Domanda 13, la catena dello scope dinamico per questa esatta espressione risale lo stack delle chiamate, facendo in modo che la \texttt{x} locale in \texttt{f1} venga modificata a 0, che la \texttt{y} globale resti intoccata a 10, e che la \texttt{z} globale diventi 10. Il calcolo finale \texttt{z - y - x} produce \texttt{10 - 10 - 0 = 0}. Poiché lo 0 non appare tra le opzioni elencate qui (a differenza della Domanda 13 dove compariva), la risposta corretta questa volta è "Nessuna".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int c = 2;
int pippo (int a) {
    c = c + 2;
    return a * 2;
}
int pluto (void) {
    return (pippo (c + 1));
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[C)] Non è possibile passare c+1 per valore
        \item[D)] 6
        \item[E)] 10
    \end{itemize}
    \textbf{Risposta esatta:} D - 6 \\
    \textbf{Spiegazione:} Riproposizione del caso già valutato: il passaggio per valore processa in toto l'argomento al momento della chiamata. \texttt{c=2}, quindi \texttt{c+1} calcola istantaneamente 3. Viene invocato \texttt{pippo(3)}. All'interno la variabile globale incrementa (\texttt{c = 2 + 2 = 4}), ma il record locale possiede la copia costante \texttt{a=3}. Il return matematico chiude eseguendo \texttt{3 * 2 = 6}.
    \vspace{0.3cm}
\end{itemize}

\begin{itemize}
    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di \texttt{f(1,r(1),1)} se i parametri sono passati \textbf{per valore}?
\begin{verbatim}
int r(int x) {
    return r(x-1);
}
int f(int a, int b, int c) {
    if (c == 1) return a;
    else return b;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] Si ha ricorsione infinita
        \item[C)] 1
        \item[D)] Non è possibile dirlo senza conoscere il tipo di scope (statico o dinamico) utilizzato
    \end{itemize}
    \textbf{Risposta esatta:} B - Si ha ricorsione infinita \\
    \textbf{Spiegazione:} A differenza del passaggio per nome visto nella Domanda 6, il passaggio di parametri per valore impone che tutti gli argomenti forniti a una funzione vengano calcolati rigorosamente \textit{prima} di entrare nella funzione stessa. Il compilatore tenta quindi di valutare l'argomento \texttt{r(1)}. Tale chiamata innesca \texttt{r(0)}, che innesca \texttt{r(-1)} e così via all'infinito, poiché la funzione \texttt{r} manca del tutto di un caso base (condizione di terminazione). Il programma va in crash per stack overflow prima ancora di poter iniziare l'esecuzione di \texttt{f}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma (espresso in pseudo-codice), qual è il valore di ritorno di topolino(), assumendo \textbf{scope statico}?
\begin{verbatim}
int x, y, z;
void minni (void) { x = 4; y = 8; }
void paperino (void) { int y; minni(); y = 1; z = 666; }
int topolino (void) {
    int x; x = 5; y = 15; z = x + y;
    paperino();
    return z - y - x;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] -3
        \item[B)] 0
        \item[C)] 14
        \item[D)] Nessuna delle altre risposte
        \item[E)] Non è possibile dirlo
    \end{itemize}
    \textbf{Risposta esatta:} D - Nessuna delle altre risposte \\
    \textbf{Spiegazione:} Seguiamo il flusso a scope lessicale (statico). 
    1. \texttt{topolino}: setta la propria \texttt{x} locale a 5. La \texttt{y} globale diventa 15. La \texttt{z} globale diventa \texttt{5 + 15 = 20}.
    2. Chiama \texttt{paperino}, che dichiara una sua \texttt{y} locale e chiama \texttt{minni}.
    3. \texttt{minni} opera solo su variabili globali: imposta la \texttt{x} globale a 4 e la \texttt{y} globale a 8.
    4. Ritorno in \texttt{paperino}: imposta la sua \texttt{y} locale a 1 e la \texttt{z} globale a 666.
    5. Ritorno in \texttt{topolino}: deve calcolare \texttt{z - y - x}. Le variabili a cui ha accesso sono la \texttt{z} globale (ora 666), la \texttt{y} globale (divenuta 8 in \texttt{minni}) e la sua \texttt{x} locale (rimasta 5). Il calcolo è \texttt{666 - 8 - 5 = 653}.
    Non essendoci il 653 tra le opzioni proposte, la risposta è "Nessuna".
    \vspace{0.3cm}

\newpage

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Si consideri lo pseudo-codice. Qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per riferimento}?
\begin{verbatim}
int c = 2;
int pippo (int a) {
    c = c + 2;
    return a * 2;
}
int pluto (void) {
    return (pippo(c + 1));
}
\end{verbatim}
    \begin{itemize}
        \item[A)] Dipende dal tipo di scope (statico o dinamico) utilizzato
        \item[B)] 10
        \item[C)] Non è possibile passare c+1 per riferimento
        \item[D)] Nessuna delle altre risposte
        \item[E)] 6
    \end{itemize}
    \textbf{Risposta esatta:} C - Non è possibile passare c+1 per riferimento \\
    \textbf{Spiegazione:} Il passaggio per riferimento impone che alla funzione venga inviato l'indirizzo di memoria (l-value) della variabile sorgente, affinché la funzione ne crei un alias. Tuttavia, l'espressione \texttt{(c + 1)} è un risultato intermedio calcolato al volo (r-value), e come tale risiede solo momentaneamente nei registri della CPU: non possiede un indirizzo fisico di memoria a cui un puntatore o un riferimento possa agganciarsi. Pertanto l'operazione è illegale in sede di compilazione.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Usando la "bounded iteration" (numerically controlled) indicare cosa viene stampato dal seguente codice:
\begin{verbatim}
z = 1;
for i = 1 to 1 + z by 1 do {
    write(i);
    z++;
}
write(z);
\end{verbatim}
    \begin{itemize}
        \item[A)] 1234... (infinite loop)
        \item[B)] 12
        \item[C)] 123
        \item[D)] Nessuna delle altre risposte
    \end{itemize}
    \textbf{Risposta esatta:} C - 123 \\
    \textbf{Spiegazione:} La caratteristica principale dell'iterazione determinata (o limitata) è che il limite superiore del ciclo (\texttt{1 + z}) viene valutato matematicamente \textit{una e una sola volta}, istanti prima che il ciclo abbia inizio. Prima di iniziare, \texttt{z} vale 1, quindi il limite superiore viene "congelato" al valore fisso 2.
    - Iterazione 1: \texttt{i=1}, stampa "1", \texttt{z} diventa 2.
    - Iterazione 2: \texttt{i=2}, stampa "2", \texttt{z} diventa 3.
    Il ciclo termina perché si è raggiunto il limite prefissato di 2 (l'incremento interno di \texttt{z} non allunga il ciclo). Alla riga successiva, viene stampata la variabile \texttt{z} che è diventata 3, per un output sequenziale di "123".
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma, quanto vale la variabile globale c dopo aver eseguito topolino(), assumendo \textbf{scope statico}?
\begin{verbatim}
int a, b, c;
void pippo (void) { int a; a = 6; b = 5; }
void pluto (void) { int c; int b; pippo(); c = 3; a = 4; }
void topolino (void) { int a; a = 1; b = 10; pluto(); c = a + b; }
\end{verbatim}
    \begin{itemize}
        \item[A)] Nessuna delle altre risposte
        \item[B)] 6
        \item[C)] Non è possibile dirlo
        \item[D)] 14
        \item[E)] 3
    \end{itemize}
    \textbf{Risposta esatta:} B - 6 \\
    \textbf{Spiegazione:} Tracciamo rigorosamente lo scope statico.
    1. In \texttt{topolino}: \texttt{a} locale a \texttt{topolino} = 1. \texttt{b} globale = 10.
    2. Chiama \texttt{pluto}: \texttt{pluto} dichiara \texttt{c} e \texttt{b} locali. Chiama \texttt{pippo}.
    3. In \texttt{pippo}: la sua \texttt{a} locale diventa 6. La \texttt{b} usata è quella globale (ora vale 5).
    4. Ritorno a \texttt{pluto}: la \texttt{c} locale diventa 3, la \texttt{a} usata è quella globale (ora vale 4).
    5. Ritorno a \texttt{topolino}: esegue \texttt{c = a + b}. La \texttt{c} in questione è la globale. La \texttt{a} è la locale (intoccata da chiunque e ferma a 1). La \texttt{b} è la globale, che \texttt{pippo} aveva modificato portandola a 5. Il calcolo finale assegnato alla globale è \texttt{1 + 5 = 6}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Dato il frammento di programma (espresso in pseudo-codice) qual è il valore di ritorno di pluto() se i parametri sono passati \textbf{per riferimento}?
\begin{verbatim}
int b = 666;
int pippo (int x) {
    x = 666; b = 1; return x / 2;
}
int pluto (void) {
    int a, c; a = b / 333; c = pippo(a); return a + c;
}
\end{verbatim}
    \begin{itemize}
        \item[A)] 333
        \item[B)] Nessuna delle altre risposte
        \item[C)] Non è possibile passare a per riferimento (perché a=b/333)
        \item[D)] 666
        \item[E)] 999
    \end{itemize}
    \textbf{Risposta esatta:} E - 999 \\
    \textbf{Spiegazione:} Innanzitutto l'opzione C è logicamente errata: una variabile allocata e assegnata (come \texttt{a}) possiede un indirizzo di memoria e può assolutamente essere passata per riferimento, indipendentemente dal modo in cui le abbiamo assegnato il valore.
    Passaggio: \texttt{a} viene calcolata ed è uguale a 2. Viene passato il riferimento (l'indirizzo) di \texttt{a} a \texttt{pippo}.
    Dentro \texttt{pippo}: l'argomento \texttt{x} funge da "alias" di \texttt{a}. Scrivendo \texttt{x = 666}, viene modificata permanentemente anche la \texttt{a} all'interno del chiamante \texttt{pluto}. Il ritorno ammonta a \texttt{666 / 2 = 333}.
    Tornati in \texttt{pluto}: \texttt{c} è 333. Eseguendo \texttt{a + c}, usiamo la nuova \texttt{a} (666) mutata per riferimento. \texttt{666 + 333 = 999}.
    \vspace{0.3cm}

    \item \stepcounter{domandateoria}\textbf{Domanda \thedomandateoria} \\
    \textbf{Testo:} Supponendo sia impiegata la tecnica delle "tombstone" per ovviare al problema dei dangling references, indicare quale delle seguenti affermazioni è vera alla fine dell'esecuzione del codice:
\begin{verbatim}
p = malloc();
q = malloc();
*p = 222;
*q = 999;
*q = *p / 2;
q = p;
free(p);
\end{verbatim}
    \begin{itemize}
        \item[A)] q punta alla tombstone, p punta alla tombstone
        \item[B)] Nessuna delle altre risposte
        \item[C)] \texttt{*p=999}, q punta alla tombstone
        \item[D)] p punta alla tombstone, \texttt{*q=111}
        \item[E)] q risulta deallocato, \texttt{*p=222}
    \end{itemize}
    \textbf{Risposta esatta:} A - q punta alla tombstone, p punta alla tombstone \\
    \textbf{Spiegazione:} Il codice definisce due blocchi memoria con rispettivi tombstones. Viene assegnato il valore 222 in \texttt{p} e viene calcolato \texttt{*q = 222 / 2 = 111}. Con l'istruzione di assegnamento di puntatori \texttt{q = p}, il riferimento \texttt{q} abbandona la sua vecchia memoria per far capo esattamente allo stesso tombstone di \texttt{p}. Eseguendo \texttt{free(p)}, la memoria fisica viene invalidata, marcando il tombstone con la "pietra tombale". A causa dell'aliasing precedente, ora entrambi i puntatori locali, che non sono stati distrutti e continuano a vivere sullo stack, puntano al medesimo tombstone ormai "morto" (dangling).
    \vspace{0.3cm}
\end{itemize}

\section{Simulazioni d'Esame: Esercizi Pratici in ML}

In questa sezione finale raccogliamo i problemi di programmazione pratica in linguaggio Standard ML proposti durante le simulazioni ufficiali e gli appelli passati. In sede d'esame, la valutazione di questi esercizi si basa sulla corretta applicazione del paradigma funzionale: divieto assoluto di usare costrutti imperativi (come i cicli o i riferimenti mutabili), uso elegante del \textit{Pattern Matching}, corretta inferenza dei tipi e gestione efficiente dell'ambiente locale tramite i blocchi \texttt{let ... in ... end}.

\subsection{Esercizio 1: Estrazione dei percorsi su Alberi (Appello 15/07/2025)}

\textbf{Testo del problema:}\\
Dato il seguente tipo di dato che rappresenta un albero di numeri interi:
\begin{verbatim}
datatype Tree = Leaf of int | Node of int * Tree * Tree | Empty;
\end{verbatim}
Scrivere due funzioni. La prima, \texttt{all\_paths (Tree -> int list list)}, deve restituire tutti i percorsi dalla radice dell'albero alle foglie (\texttt{Empty} non è considerato una foglia).
La seconda, \texttt{filter\_paths ('a list * ('a -> bool) -> 'a list)}, prende in input una lista di path e un predicato generico, e restituisce una lista di liste contenente solo i path che soddisfano tale condizione. \textit{È ammesso l'utilizzo della funzione \texttt{map} ma non di altre funzioni di libreria.}

\newpage

\textbf{Soluzione (Valutata 30L):}
\begin{verbatim}
datatype Tree = Leaf of int | Node of int * Tree * Tree | Empty;
fun all_paths(t: Tree) =
let
    fun aux (Empty) = []
      | aux (Leaf n) = [[n]]
      | aux (Node (n, l, r)) =
        let
            val left_paths = aux(l)
            val right_paths = aux(r)
        in
            (* Aggiunge il nodo corrente 'n' in testa a 
               TUTTI i percorsi calcolati dai figli *)
            map (fn path => n::path) (left_paths @ right_paths)
        end
in
    aux(t)
end;

fun filter_paths ([], f) = []
  | filter_paths (x::xs, f) = 
      if (f(x)) then x :: filter_paths(xs, f) 
      else filter_paths(xs, f);

(* Esempio di utilizzo (filtro basato sulla somma degli elementi): *)
fun sum_lower_than_30 list = (foldl (op +) 0 list) < 30;
\end{verbatim}
\textbf{Analisi didattica:} L'utilizzo del \texttt{map} con una funzione anonima (\texttt{fn path => n::path}) per concatenare il nodo radice a \textit{tutte} le liste figlie in un colpo solo dimostra un'ottima padronanza delle funzioni di ordine superiore. Il blocco \texttt{let} evita di ricalcolare i percorsi più di una volta.

\subsection{Esercizio 2: Astrazione del Controllo (Il ciclo FOR)}

\textbf{Testo del problema (Prog 3):}\\
Si consideri il tipo di dato \texttt{datatype FOR = For of int * (int -> int)}, i cui valori \texttt{For(n, f)} rappresentano funzioni che implementano un ciclo for imperativo che applica iterativamente $n$ volte la funzione $f$ all'argomento. Si scriva una funzione \texttt{eval} (avente tipo \texttt{FOR -> (int -> int)}) che riceve come argomento un valore di tipo \texttt{FOR} e ritorna una funzione curried pronta ad applicare tale ciclo.
Se \texttt{f} moltiplica per 2, \texttt{eval (For(3, f))} ritornerà una funzione che dato un input lo moltiplicherà per 8 ($2^3$).

\newpage

\textbf{Soluzione:}
\begin{verbatim}
datatype FOR = For of int * (int -> int);

(* Caso base: un ciclo che itera 0 volte restituisce la funzione Identità *)
fun eval (For (0, f)) = (fn x => x)
  | eval (For (n, f)) = 
      let 
          (* Scongela il passo successivo (n-1) *)
          val rest_eval = eval(For(n - 1, f))
      in 
          (* Restituisce una closure che compone f con il resto delle 
             iterazioni *)
             
          (fn x => f(rest_eval(x)))
      end;

(* Alternativa sintatticamente perfetta usando la composizione 'o': *)
fun eval2 (For (0, f)) = (fn x => x)
  | eval2 (For (n, f)) = f o eval2(For (n - 1, f));
\end{verbatim}
\textbf{Analisi didattica:} Questo esercizio testa la comprensione profonda della \textit{Partial Instantiation} e delle \textit{Closure}. La funzione \texttt{eval} non calcola il risultato finale matematico, ma si limita ad "assemblare" la catena di funzioni $f(f(f(x)))$ e la restituisce in attesa che l'utente fornisca l'input $x$.

\subsection{Esercizio 3: Somma Filtrata per Parità (Prog 1)}

\textbf{Testo del problema:}\\
Si scriva una funzione \texttt{sommali} (avente tipo \texttt{int -> int list -> int}) che riceve un intero $n$ ed una lista di interi $L$. La funzione somma ad $n$ gli elementi di $L$ che hanno posizione pari (indici 0, 2, 4...). Se la lista contiene meno di 2 elementi, la logica si applica regolarmente. (Es: \texttt{sommali 2 [1,2,3,4] = 2 + 1 + 3 = 6}).

\newpage

\textbf{Soluzione:}
\begin{verbatim}
fun sommali n [] = n
  | sommali n [x] = n + x
  (* De-strutturiamo la lista afferrando i primi due elementi: 
     x (pari) e y (dispari). Sommiamo x al totalizzatore, e scartiamo y. *)
  | sommali n (x::y::xs) = sommali (n + x) xs;
\end{verbatim}
\textbf{Analisi didattica:} Raggiunge un'efficienza ottimale sfruttando la \textbf{Tail Recursion} (Ricorsione in coda) in modo che l'accumulatore logico venga trascinato nelle chiamate successive, non riempiendo mai il \textit{Call Stack}. L'uso del pattern \texttt{x::y::xs} permette di "saltare" le posizioni dispari con una sintassi compatta.

\subsection{Esercizio 4: Manipolazione di stringhe (Prefissi e Suffissi)}

\textbf{Testo del problema (Exam Examples):}\\
Scrivere due funzioni \texttt{suffixes} e \texttt{prefixes} (tipo \texttt{string -> string list}) che, data una stringa, restituiscano la lista di tutti i suoi suffissi/prefissi, ignorando la stringa vuota. 
Esempio: \texttt{suffixes "ciao" = ["ciao", "iao", "ao", "o"]}.

\textbf{Soluzione:}
\begin{verbatim}
(* SUFFISSI: Semplice, poiché possiamo usare l'estrazione substring *)
fun suffixes "" = []
  | suffixes str = 
      str :: suffixes (String.extract(str, 1, NONE));

(* PREFISSI: Convertiamo in char list per poter comporre gli elementi *)
fun pref_aux [] = []
  | pref_aux (x::xs) = 
      (* Il primo carattere si abbina a tutti i prefissi generati in coda *)
      [x] :: map (fn p => x::p) (pref_aux xs);

fun prefixes "" = []
  | prefixes str = 
      (* implode trasforma le char list interne di nuovo in stringhe *)
      map implode (pref_aux (explode str));
\end{verbatim}

\subsection{Esercizio 5: Addizione Binaria Nativa su Liste}

\textbf{Testo del problema (Exam Examples):}\\
Scrivere la funzione \texttt{sum\_binary} di tipo \texttt{int list * int list -> int list} che prende in input una coppia di liste di interi (composte da 1 e 0) rappresentanti un numero binario, e ne restituisca la somma binaria.
Esempio: \texttt{sum\_binary ([1,0], [1,0]) = [1,0,0]}.

\newpage

\textbf{Soluzione:}
\begin{verbatim}
(* Funzione ausiliaria che incapsula la logica del riporto (carry) *)
fun sum_bin_carry ([], [], 0) = []
  | sum_bin_carry ([], [], 1) = [1]
  
  (* Se una delle due liste è più corta, aggiungiamo [0] fittizi *)
  | sum_bin_carry (x::xs, [], carry) = sum_bin_carry(x::xs, [0], carry)
  | sum_bin_carry ([], y::ys, carry) = sum_bin_carry([0], y::ys, carry)
  
  (* Passo generale per l'elaborazione dei bit *)
  | sum_bin_carry (x::xs, y::ys, carry) =
      let
          val sum = x + y + carry
          val new_bit = sum mod 2   (* Se la somma è 2, il bit è 0 *)
          val new_carry = sum div 2 (* Se la somma è 2 o 3, il carry è 1 *)
      in
          new_bit :: sum_bin_carry(xs, ys, new_carry)
      end;

(* Wrapper esterno che inverte le liste (Little-Endian vs Big-Endian) *)
fun sum_binary (L1, L2) =
    rev (sum_bin_carry (rev L1, rev L2, 0));
\end{verbatim}
\textbf{Analisi didattica:} In ML, l'operatore cons (\texttt{::}) permette di aggiungere elementi solo \textit{in testa} alla lista, operazione logica \textit{O(1)}. Poiché un'addizione richiede l'allineamento a destra (sui bit meno significativi), è obbligatorio ribaltare prima gli operandi con la funzione nativa \texttt{rev}, processare i carry in avanti e, infine, ribaltare il risultato. L'uso dei pattern per gestire la discrepanza di lunghezza tra gli operandi è un'altra peculiarità vitale del codice.

\subsection{Esercizio 6: Aggregazione su Strutture Ramificate (get\_words)}

\textbf{Testo del problema (Exam Examples):}\\
Dato il tipo \texttt{datatype ctree = Empty | Leaf of char | Node of char * ctree * ctree}. Scrivere una funzione \texttt{get\_words} che restituisce una coppia di stringhe \texttt{(string * string)}. La prima stringa è la concatenazione dei caratteri contenuti nelle foglie, la seconda stringa è la concatenazione di quelli nei nodi interni.
(Es: \texttt{get\_words Node(\#"a", Leaf \#"b", Leaf \#"c") = ("bc", "a")}).

\newpage

\textbf{Soluzione:}
\begin{verbatim}
datatype ctree = Empty | Leaf of char | Node of char * ctree * ctree;

fun get_words Empty = ("", "")
  | get_words (Leaf c) = (str(c), "")
  | get_words (Node (c, left, right)) =
      let
          (* Estraiamo i risultati pre-calcolati dai sotto-alberi *)
          val (leavesL, nodesL) = get_words(left)
          val (leavesR, nodesR) = get_words(right)
      in
          (* Ricomponiamo la tupla assemblando i rispettivi domini *)
          (leavesL ^ leavesR, str(c) ^ nodesL ^ nodesR)
      end;
\end{verbatim}
\textbf{Analisi didattica:} Questa soluzione sfrutta l'ambiente locale \texttt{let} in congiunzione con l'operatore nativo di concatenazione di stringhe (\texttt{\^}) e il convertitore \texttt{str()} per operare il casting da carattere a stringa, aggirando costose manipolazioni di liste di caratteri intermedie.

\end{document}
